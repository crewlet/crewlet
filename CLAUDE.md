# Crewlet — Project Conventions

## IMPORTANT — Read Docs First
Before starting ANY task, you MUST read the following project docs to understand the project context:
- `docs/concepts/overview.md` — project vision, design principles, and architecture
- `docs/concepts/organization-model.md` — hierarchy, roles, and identity
- `docs/concepts/agent-runtime.md` — agent lifecycle and execution
- `docs/getting-started/quickstart.md` — getting started guide

See `docs/index.md` for the full documentation index.

Do this at the start of every conversation, before writing or modifying any code. These docs are the source of truth for how the system should work.

## Overview
Crewlet is a Go engine for orchestrating hierarchically organized AI agent companies.
See `docs/index.md` for the full documentation index.

## Tech Stack
- **Go 1.27+** — the module pins its own toolchain; `GOTOOLCHAIN=auto` fetches it rather than failing on a mismatch. Everything is pure Go: `CGO_ENABLED=0` builds every release target, which is what makes the cross-compile matrix a plain `GOOS`/`GOARCH` loop — over the four platforms the store driver has a database engine for. It does NOT make the linux artifact static; see the Turso row below
- **The standard library first.** `net/http` for servers and clients, `log/slog` for logging, `context` for cancellation, `encoding/json`. A dependency has to earn its place against what `std` already does
- **Turso** (`turso.tech/database/tursogo`) — the embedded store, and the ONLY driver. The `modernc.org/sqlite` fallback is retired, along with the `store.driver` field and `CREWLET_STORE_DRIVER`, so there is no dialect intersection to write inside — use what Turso has. It is pure Go in the sense that matters — no cgo, no C toolchain — but it is not self-contained: its engine ships as a ~20 MB native library embedded in the driver and extracted at runtime into a shared per-user cache, which `internal/store/turso.go` prepares under a lock because upstream writes it non-atomically and PANICS on what a concurrent reader sees. That library is also what bounds the release matrix — upstream embeds it for linux and darwin on amd64/arm64 and windows/amd64 only, which is why there is no Windows target and why `internal/store/platform.go` turns an unsupported build into a compile error. And it is why the linux binary is NOT static despite `CGO_ENABLED=0`: purego declares its `dlopen` imports with `//go:cgo_import_dynamic`, so the artifact is dynamically linked against glibc and does not run on musl or `scratch`. Do NOT "fix" that with `-extldflags -static` — a static program cannot `dlopen`, and that build segfaults on its first query (measured; ci.yml's `cross` job asserts the artifact stays dynamic). `-tags musl` picks the embedded `.so`, not the binary's own linkage, so it does not produce an Alpine build either — there is no musl archive
- **NATS JetStream** (`github.com/nats-io/nats-server`) — the event stream, and the ONLY broker. Embedded by default, running *in the engine's own process* with no listener and no service to operate; a fleet either clusters those embedded members (`stream.cluster`) or dials an external NATS cluster (`stream.type: nats`). The Apache Pulsar backend is retired, along with `stream.type: pulsar`, its `tenant`/`namespace`/`admin_url`/`tls_trust_certs` fields and the whole `coordination.nats` estate block — the coordination KV rides the stream's own connection on every topology now, which is precisely what Pulsar could never do (it has no compare-and-set, so every Pulsar deployment ran a second NATS estate anyway)
- **`github.com/modelcontextprotocol/go-sdk`** — MCP client and server
- **OpenTelemetry** (`go.opentelemetry.io/otel` + the OTLP trace exporters) — tracing, and the one dependency here that `std` genuinely cannot supply: W3C trace context, a sampler, and a wire format three collectors already speak. Configured from the standard `OTEL_*` environment rather than Tier A, so an operator pastes their collector's own snippet. The provider is always installed and only the exporter is conditional, because a trace id is a column in the event store before it is ever an exporter's concern
- **Official vendor SDKs** where one exists; a thin typed client where one does not (Mattermost, GitLab, Confluence)
- **YAML** (`gopkg.in/yaml.v3`) — config parsing
- **`testing`** — the standard library's, with no assertion framework. Table tests where the cases vary, named tests where the reasoning does

## This Is a Public Open-Source Repository
Crewlet is MIT-licensed, developed in the open at `github.com/crewlet/crewlet`, ships as signed binaries and a container image from a `v*` tag, and its `docs/` tree is served at docs.crewlet.ai. Everything committed is published, permanently: git history keeps what a later commit deletes, and a released binary keeps what the working tree drops. Write every change as something a stranger will read, run, and depend on.

Rules:
- **Never commit a secret, and never fix one by deleting it.** No real API keys, tokens, PATs, webhook secrets, private hostnames, internal URLs, or customer/employee names — in code, tests, docs, examples, fixtures, or commit messages. Config examples use `${VAR}` references and placeholder values (`example.com`, `U0FOUNDER`, `sk-ant-...`). If a real credential does land in a commit, it is compromised the moment it is pushed: it must be **rotated**, and removing it in a follow-up commit is not a fix.
- **Commit subjects are semantic** — `type(scope): summary`, imperative, lowercase, no trailing period, ≤72 chars. Type is one of `feat` / `fix` / `docs` / `refactor` / `perf` / `test` / `ci` / `build` / `chore` / `revert`; scope is the *component*, normally the package directory under `internal/` (`agent`, `api`, `coord`, `engine`, `queue`, `sandbox`, `seat`, `store`, …), plus `dashboard` for the dashboard (source in `dashboard/`, built bundle in `static/dashboard/`) and `cli` for `cmd/crewlet/`; outside `internal/` it is the area (`docs`, `deps`, `examples`, `schema`, `scripts`, or the workflow name for `ci`). Unrelated fixes and tuning go in their own commit with their own scope. A pull request lands as either a squash (subject = the title) or a merge commit over the branch's own commits — `main` carries both — so the title takes the same form either way. Nothing enforces this and history is permanent — the full rules are in `CONTRIBUTING.md`.
- **Every commit is signed off** — `git commit -s`, which appends `Signed-off-by: Name <email>` from git's own `user.name`/`user.email`. That trailer certifies the `DCO` file at the repository root over the commit, and unlike the subject conventions above it IS enforced: `scripts/check-signoff.sh` runs in `make check` and in ci.yml's `sign-off` job, which judges a PULL REQUEST's own commits and nothing else. A commit without a trailer fails the build. What nothing checks any more is the commit the merge button WRITES: it composes that message itself, after every check has reported, and does not always carry the branch's trailers (`074e3a1` and `7d0e29e` are two that reached `main` unsigned from properly signed branches). The `sign-off (main)` job that used to report that after the fact was removed — the repository's squash and merge commit-message settings are the only thing keeping the trailers now, and they live in Settings where nothing in this tree can assert them. The gate has its own suite, `scripts/check-signoff_test.sh`, because no Go test can reach bash reading git history. Presence is what is checked, never a match against the author — the DCO's clauses (b) and (c) cover relaying somebody else's patch, so a commit legitimately carries a chain of sign-offs naming people who are not its author, and Dependabot's own commits sign off as `<support@github.com>` while GitHub authors them as `<…@users.noreply.github.com>`. Merge commits are exempt: nothing offers a chance to sign one, and it carries no change of its own. A missing trailer is repaired by REWRITING the commit (`git commit --amend -s --no-edit`, or `git rebase --signoff origin/main`), so it costs nothing before a push and a force-push after.
- **The pull request title is the release note.** There is no `CHANGELOG.md`: GitHub generates each Release body from the titles of the pull requests merged since the previous tag (grouped by `.github/release.yml`), and that generated body is the *only* record of what a release contains. So a title for a user-visible change — a new feature, a config or CLI change, a behavior change, a notable fix — must read as release notes for someone running Crewlet, not as a commit log. Nothing gates this at release time; a vague title is permanent once the pull request merges.
- **The root-level files are canonical for process**, and this file must not contradict them: `CONTRIBUTING.md` (dev setup, contributor conventions, dependency surfaces), `RELEASING.md` (the release runbook), `SECURITY.md` (private vulnerability reporting, operator scope notes), `CODE_OF_CONDUCT.md`, `DCO` (the Developer Certificate of Origin every commit's sign-off certifies — verbatim upstream text, never edited), `LICENSE`. When a rule here changes, change it there in the same commit — a contributor reading `CONTRIBUTING.md` and an agent reading this file must never get different answers.
- **Follow the repository's own templates.** PRs use `.github/pull_request_template.md` (its checklist is the actual gate); issues use the templates in `.github/ISSUE_TEMPLATE/`.
- **Third-party code must be MIT-compatible and attributed.** Do not paste in code whose license forbids it. Source files carry no license headers — do not start adding them.
- **Security issues never go in a public issue or a public PR description** — see `SECURITY.md`.

## No Band-Aids — Always Do the Proper Fix
Never apply band-aid fixes, workarounds, or partial solutions. Always implement the correct, complete fix — even if it is large, touches many files, or requires significant refactoring. A quick hack that "works for now" is not acceptable; it creates tech debt that compounds and makes the codebase harder to reason about.

**When suggesting a fix, the cost of the fix is never a factor.** We do not care how long the fix takes, how many lines of code it touches, how many files it spans, or how big the change ends up being. Always propose the proper fix — the one that actually solves the problem at the root — regardless of size or effort. Do not downgrade your suggestion to a smaller, cheaper, or faster alternative because the proper fix "feels too big". If the proper fix is a 2000-line refactor across 50 files, suggest that fix. If it requires rewriting a subsystem, suggest that. Never pre-filter your suggestions based on effort, time, or scope.

Rules:
- **Root-cause fixes only** — diagnose the actual problem and fix it at the source. Do not patch symptoms.
- **No workarounds** — do not add special cases, flags, or conditional logic to work around a broken abstraction. Fix the abstraction. A compatibility shim — an alias, a both-spellings fallback, an adapter — is that same move under a respectable name, and for a surface no release ever shipped there is nothing on the other side of it to be compatible with (see "Breaking Changes Are Free Until a Tag Ships Them" below).
- **Size is not an excuse, ever** — if the proper fix requires changing 20 files, change 20 files. If it requires changing 200 files, change 200 files. If it requires redesigning a subsystem, redesign it. The scope of the fix should match the scope of the problem — nothing less.
- **Time is not a factor** — do not consider how long a fix will take when deciding what to suggest or implement. A fix that takes a week is not worse than one that takes an hour; only the correctness and completeness of the fix matter.
- **Lines of code are not a factor** — do not prefer a smaller diff over a correct one. A one-line hack that papers over the issue is strictly worse than a thousand-line change that solves it properly.
- **Always suggest the proper fix first** — when proposing fixes, lead with the correct, complete one. Do not hide it behind cheaper alternatives or present "quick" options as the default. The user explicitly wants the proper fix no matter the size.
- **No "temporary" solutions** — there is no such thing as a temporary fix in this codebase. Every change should be one you're willing to maintain indefinitely.
- **Refactor when needed** — if implementing a feature correctly requires refactoring existing code first, do the refactoring. Do not bolt new functionality onto a broken foundation.
- **Update everything** — a proper fix includes updating all tests, docs, and dependent code. A fix that breaks tests or leaves stale docs is not done.

When in doubt, ask: "Is this the fix I would make if I had unlimited time and unlimited diff size?" If not, find the fix that is.

## Fix Every Bug You Find — Even Unrelated Ones
While working on a task you WILL sometimes notice bugs that have nothing to do with what you were asked to do. Fix them anyway. A discovered bug is never "out of scope" — relatedness to the current task has no bearing on whether it gets fixed. If it is broken and you found it, you fix it, in the same change. A bug you walked past is a bug you implicitly endorsed.

Rules:
- **See a bug, fix a bug** — whenever you discover a bug — while reading code, building a feature, debugging something else, or reviewing — fix it as part of your current change. Do not leave it for "later", a "follow-up", or someone else.
- **Unrelated is not an excuse** — the bug does not need to relate to your task, the file you are in, or what the user asked for. "That's not what I'm working on" is not a reason to leave a bug in place.
- **Never silently ignore a bug** — you may not knowingly leave a discovered bug unfixed. There is no "I noticed it but skipped it". Silence about a known bug is not allowed.
- **Fix it properly** — apply the same bar as everything else: root-cause fix, no band-aids, no deferred work, with tests and docs updated (see "No Band-Aids" and "No Deferred Work" above). Size, time, and diff length are not factors here either.
- **Surface what you fixed** — call out incidental bug fixes in your summary and commit message so they are visible and reviewable. Where it keeps history clean, put unrelated fixes in their own commit.
- **If a fix genuinely can't fit in this change** — e.g. it needs its own coordinated effort or a risky redesign — do not quietly drop it. Fix everything you safely can now and explicitly raise the remainder with the user; never let a known bug pass unmentioned (same rule as "No silent narrowing of scope" below).

When in doubt, ask: "Did I notice anything broken that I'm about to leave broken?" If yes, fix it.

## Tuning Knobs Are Findings Too — Don't Defer Them Either
This extends "Fix Every Bug You Find — Even Unrelated Ones" to the findings developers love to wave away. While working you WILL notice values that aren't crashes or wrong output but are plainly suboptimal: a hardcoded magic number, an arbitrary timeout, retry count, backoff constant, batch size, poll interval, cache TTL, concurrency cap, page size, buffer size, rate limit, log level, feature-flag default, temperature or model choice, a value that should be configurable but is baked in, or a knob exposed to callers that should just be a sane constant. The reflex is to label it "just a tuning knob, not a bug" and move on. That label is not a free pass — it is the exact moment the work gets dropped. A wrong knob is a slow-motion bug; it just waits for production load to detonate — a 1-second timeout that flaps under load, a retry count that hammers a dying dependency, a temperature that makes a judge non-deterministic. The only honest difference from a crash is that the right value is often a judgment call, and a judgment call is something you make, not something you skip. A knob you shrugged at is a default you shipped.

Rules:
- **A wrong knob is a finding, not a footnote** — "it's a tuning knob, not a bug" does not downgrade it, defer it, or excuse leaving it. Relatedness has no bearing either (see "Fix Every Bug You Find" above): if you read past a `batch_size=1` that should be batched, you found it, so you address it in the same change, to the same bar as a real fix — root-cause, no band-aids (see "No Band-Aids" above).
- **Tune to the justified value now** — pick the value you can defend from the code, the data, and the surrounding behavior, and set it. Do not leave a placeholder, a vague comment, or a stubbed-out "configurable later" hook (see "No Deferred Work" below).
- **Expose or collapse, deliberately** — a hardcoded value callers genuinely need to vary becomes real config; a knob with no honest reason to differ becomes a plain constant with the chosen value. Decide which direction is correct and do it — do not leave a half-configurable mess.
- **Tie the value to something real** — record why this value, not just what, where the next reader will see it (the config doc, a comment at the definition, or the commit message). Anchor it to the dependency's p99, the page-size limit, the SLA, the memory budget — so the next reader inherits the rationale, not a fresh mystery.
- **Surface what you tuned** — call out every knob you changed, exposed, or collapsed in your summary and commit message, with the old and new value and the reason, exactly as you would an incidental bug fix (see "Surface what you fixed" under "Fix Every Bug You Find"). Where it keeps history clean, put unrelated tuning in its own commit.
- **If the right value is genuinely a judgment call you can't settle here** — when the correct setting truly needs production data, a benchmark you can't run, or a product decision (a concurrency cap trading cost against latency, a cache TTL trading freshness against load), do not silently leave the bad value. Make your best-justified call, mark it clearly, and explicitly raise it with the user with a concrete recommendation and the tradeoff — never let a known-suspect knob pass unmentioned (same rule as "If a fix genuinely can't fit in this change" under "Fix Every Bug You Find").

When in doubt, ask: "Am I calling this 'just a knob' so I don't have to decide its value?" If yes, decide it — or escalate it with a concrete recommendation — but never walk past it.

## No Deferred Work — Always Deliver the Clean Implementation
Never defer work. Do not push problems forward with TODOs, "follow-up" tickets, stub functions, or "we'll fix this later" comments. When you start a task, you finish it — completely, cleanly, and in the same change. Long, hard, or sprawling tasks are not an excuse to ship something partial; they are a reason to commit to the full implementation.

Rules:
- **Decide and deliver** — when faced with a choice, make the call and implement it end-to-end. Do not stop halfway, hedge with multiple half-built paths, or leave the decision for "later".
- **No TODOs, no stubs, no placeholders** — do not commit `TODO`, `FIXME`, `XXX`, `pass  # implement later`, `raise NotImplementedError`, or empty function bodies pretending to be done. If it's in the diff, it's finished.
- **No "follow-up" tickets for the current scope** — if it is part of the task you accepted, it ships in this change. Do not split a coherent piece of work into a "first pass" plus a backlog item.
- **Long tasks still get finished** — if the task is large, work through it. Do not stop at "good enough for now" and call it done. The size of the task is not a license to defer parts of it.
- **No "phase 1 / phase 2" hand-waving** — do not invent artificial phases to justify shipping less than the task requires. If phasing is genuinely necessary (e.g. a real migration with data dependencies), confirm it explicitly with the user before splitting.
- **No silent narrowing of scope** — if you decide the task is bigger than expected, do not quietly shrink what you deliver. Either deliver the full scope or raise it with the user before cutting.
- **Tests, docs, migrations all included** — "delivered" means the implementation, its tests, its docs, and any required migrations or config updates are all in the change. A feature without tests or docs is not delivered.

When in doubt, ask: "If I stopped right now, would a reader of this diff consider the task done?" If not, keep going until the answer is yes.


## Code Conventions
- **Interfaces are defined by the consumer**, in the package that calls them, and kept to what that caller needs. A provider package exports a concrete type; the package that uses it declares the two-method interface it actually calls. There is no `interfaces.go`
- **`context.Context` first, always**, and threaded rather than stored in a struct. The one exception is a rollback or a teardown, which takes `context.WithoutCancel`: the failure it is undoing is often the cancellation itself, and a cleanup that inherits a dead context does nothing at all
- **Accept interfaces, return structs**
- **Zero values must be meaningful, or the type must refuse them.** A `pause_ttl_seconds` of 0 read as "never pause" and tore down the checkout of every seat that said nothing about the knob — a config field whose zero value is a valid *setting* needs a pointer or an explicit `Unset` sentinel, not a comment
- **Errors wrap with `%w`** and say what to do about it. An error a person will read names the field, the file or the variable they have to change — not just what failed. Sentinels are `errors.Is`-comparable; typed errors carry the identifier the caller needs
- **Every piece of state answers "who has to agree on it?"** before it is written anywhere. THREE answers cover almost everything: *this node alone* is the node's own database file; *every node, identically, derived from an ordered log* is the replicated estate, which ONLY a state log's applier writes; *the whole company, now* is the coordination store. There is a FOURTH and it is narrow — *whichever node holds this seat, and only the current value* is a COMPACTED CHANGELOG (`adr/0014`), which exists because a seat's memory was answered with the first and was forgotten every time placement moved a seat. What is not an answer is "it has always been in the store": nine tables took that default and cost five repair migrations (`internal/store/schema/node/0010`–`0013` and `0028`). The stream is the write-ahead log and the SQL estate is derived, never the reverse. `adr/0002`, `adr/0003`, `adr/0004` and `adr/0014` are the decisions; `internal/store`'s estate gates refuse a new table that skips the question
- **A three-valued answer is `(value, error)`**, never a bool. "Held", "definitively not held" and "the store could not be reached" are three different facts, and collapsing the last two into `false` is the single most incident-hardened lesson carried into this engine — see `internal/coord`
- **IDs are `uuid.UUID`**, timestamps are `time.Time` in UTC, durations are `time.Duration`. A config field that is a number of seconds is named `…Seconds` and converted once, at the edge
- **Enums are a named string type** with typed constants and a `Valid()` method, so an unknown value off the wire is a value rather than a panic
- **Concurrency**: a goroutine's lifetime belongs to whoever started it, and every one has a way to stop. Prefer channels for handoff and a mutex for state; `sync.OnceValue` over a `sync.Once` plus a variable. Anything shared runs under `-race` in CI
- **Comments explain WHY**, and especially why the obvious alternative is wrong. The diff shows what the code does. A package's rationale goes in its package doc, where `go doc` will find it

## Package Layout

```
cmd/crewlet/          # The one binary. run / validate / schema / migrate /
                      #   budgets / backup / retention / work / secrets /
                      #   config / chart / iam / llm / search, and the six
                      #   vendor CLIs
                      #   (gitlab, github, jira, slack, confluence,
                      #   mattermost).
                      #   Every command the switch dispatches must appear in
                      #   usage() — nothing connects them, and a test asserts it.
                      #   A command that talks to a running node
                      #   authenticates with CREWLET_API_TOKEN and NOTHING
                      #   ELSE — never a flag (a token typed as an argument
                      #   is in shell history and `ps`, so `-token` is an
                      #   unknown flag), and never Tier A's own list, which
                      #   is what a node ACCEPTS, not a wallet the CLI helps
                      #   itself from, and
                      #   the fallback that took the first entry authored an
                      #   operator's write under a name they had not chosen —
                      #   after reading a resolved ${VAR} out of the config
                      #   file in the clear to do it. The ONE exception is
                      #   `iam token -login`, which mints the caller's OWN
                      #   token and so signs in AS them for the one request
                      #   (a password, and a second-factor code where one
                      #   is held, off the terminal or piped lines — never
                      #   a flag, since a recovery code stays good in shell
                      #   history) and signs out after: a
                      #   person's token is theirs alone to mint, and the
                      #   only bearers this CLI otherwise holds are the
                      #   deployment's (owns no tokens) or a token (may not
                      #   mint). A cookie write states `api.external_url`'s
                      #   origin, and a bearer write states none — the
                      #   dialled address is never an origin the node admits
internal/             # Everything else. No package here is importable from
                      #   outside the module, which is deliberate: the engine
                      #   has no stable Go API to keep, only a CLI, a config
                      #   format and a wire protocol
dashboard/            # The dashboard's SOURCE: React 19 + TypeScript, built by
                      #   Vite. `make dashboard` builds it into static/dashboard
                      #   and THAT IS COMMITTED — `go build ./...` and
                      #   `go install …@latest` must work with no Node on the
                      #   machine, and an embed directive cannot run a bundler.
                      #   CI rebuilds and diffs the tree (`make dashboard-check`),
                      #   the same idiom as `go mod tidy -diff`
static/               # Embedded assets — static/dashboard/ is that build
                      #   output, served by internal/api and compiled into the
                      #   binary via embed. NEVER hand-edited
tests/dashboard/js/   # The e2e client replay only: internal/e2e captures a real
                      #   company's socket frames and pushes them through the
                      #   dashboard's own protocol module (the second build
                      #   target, static/dashboard/protocol.js) under plain
                      #   `node`. The dashboard's OWN suites are Vitest, in
                      #   dashboard/src, run by `make dashboard-test`
examples/             # The Nimbus example company — a working two-tier config
docs/                 # Product documentation, published to docs.crewlet.ai
schema/               # GENERATED JSON Schema for both config tiers. Emitted by
                      #   `crewlet schema <tier>`; a test regenerates and
                      #   compares, so never hand-edit — change the Go models
adr/                  # Architecture DECISION RECORDS, for a decision that
                      #   binds more than one package — the one thing a package
                      #   doc cannot hold. A decision inside one package stays
                      #   in that package's doc and gets no record. Each names
                      #   an Authority it is forbidden to restate and an
                      #   Enforced-by that is a test, a compile error or the
                      #   literal `nothing`; `internal/adr` checks that anchor
                      #   in BOTH directions inside `make check`
skills/               # FOUNDER-facing authoring skills for an AI assistant
scripts/              # The two vendor dev-loop bootstraps, and the DCO gate:
                      #   check-signoff.sh, run by `make check` and by ci.yml's
                      #   `sign-off` job (pull requests only), plus its own
                      #   suite check-signoff_test.sh — no Go test can reach
                      #   bash reading git history (bash)
```

### The packages, and the one thing each is for

**Every package states its own rationale in its package doc.** `go doc ./internal/coord` is the authority on coordination, not this file — what follows is a map, so you know which doc to read. Where a decision would be tempting to "fix" back to the obvious shape, the package doc is where that is written down, together with what the obvious alternative cost when it was tried. None of it binds: change the code and rewrite the comment in the same commit.

*The spine — what everything else is built on:*

| Package | What it is for |
|---|---|
| `queue` | THE EventQueue contract. One interface, two backends (`memory`, `jetstream`), both certified by ONE suite in `queuetest`. Fourteen verbs, and TWO of them are the opposite of every other: `Ask` and `Serve` are an EPHEMERAL scatter — core NATS request/reply on the real backend, no stream, no consumer, no ack, no retention, no publish listener — because a query is not an event, and a search fan-out on the durable verbs would write two records and an audit row per keystroke. Nothing above this package may branch on which backend is running. A durable subscription IS a seat's mailbox: it exists without a consumer and retains what is published while nothing is attached — and it outlives every record of its own name, a removed seat's handle included, so the BROKER has to be able to list them (`ListSubscriptions`) or a leaked mailbox can never be found again. Handlers have three outcomes — ack, nak, and *defer*. Attachment has four verbs of differing destructiveness, because `unsubscribe` never said which one it was |
| `coord` | Cross-process ownership: TTL leases with a monotonic fencing epoch, plus everything else a fleet has to agree on (`fleet.go`). THE DOCUMENT FAMILIES ARE GONE — both native backends left them for ordered logs, and what survived is the KEY GRAMMAR (`keys.go`), because every class the fleet still writes composes an id, a stream name or a node id into a subject token path and each of those can carry a byte a token cannot — the activation pointer (a COMPARE-AND-SET: there is no leader, so the flip is what stops two operators overwriting each other) and per-node apply status, the completion ledger, the delivery dedupe, the notification valve, the credential cooldowns, the token counter, the capacity window (ONE record that is both the operation and the exclusion, beside the per-node admissions and acknowledgements the handshake is made of) and the company's SECRETS — last-write-wins for an operator's rotation, with a version on every row and the conditional writes (`CreateSecret`, `UpdateSecret`, `DeleteSecretAt`) the engine's own key material needs, since a plain put of a person's key is how two minters, a rekey or a stale touch lose or resurrect one. A RESOURCE NAME IS SEGMENTED for the same reason — `seat:alice` is the two segments `seat` and `alice`, so its lease and epoch keys carry the CLASS as a subject token and `ListLive`/`PreferredResources` take a `Class` rather than a prefix string: the membership read asks the broker for the presence leases instead of every lease in the fleet, and the sweep's hints for the seats instead of every epoch the deployment has ever minted. There is deliberately NO all-classes listing and no empty segment, because both fail the same silent way — a filter matching nothing answers exactly like a class with no members, and `seat:` writes a key nothing can decode, so the lease lands and no listing ever returns it, which every node reads as a free seat. Every "do I hold this?" is three-valued — held / definitively not / **unknown** — and treating unknown as loss tears a healthy company down over a two-second store blip. Retention here is a BUCKET's age, never a per-write TTL. `NodeStatus` also carries each node's own `api.auth.max_grants` as a HASH — sorted and deduplicated, absent rather than empty on a node that serves no API — because the ceiling is applied at decision time and never written, so a fleet mid-rollout legally disagrees and nothing else would say so |
| `store` | The node's LOCAL databases — TWO of them, brought up by one `Open`: the NODE estate (the audit event log, learning memory, config revisions, the bootstrap half of the secret store that `fleetsecrets` migrates off at boot) and the REPLICATED estate beside it, which is everything a state log's applier writes. Two files because a snapshot is a copy of ONE of them: taken from a single file it would be a copy of everything followed by a delete, and with no in-place `VACUUM` the audit log's pages ride along in the artifact, the transfer, the checksum and the integrity check. No transaction spans the two and no read joins across them — a static walk holds it. **One file, one process** — exclusively owned, so nothing here has to be safe against a peer, and the migrator's advisory-lock idiom simply disappears. Anything the COMPANY has to agree on belongs in `coord` instead, and migrations 0010–0013 are what that rule cost when it was broken. One driver: `Open` and `Pending` reach a connection through ONE helper, because the second path was the one that skipped the native-library preparation and panicked. And a handle that is NOT OPEN answers `ErrNoEstate` rather than crashing — nil is a legitimate, documented state of the replicated peer (an adoption closes it between its rename and its reopen, and `Close` leaves it nil), so a goroutine already in flight reaches it honestly; the pointer is atomic for the same reason, since the adoption writes it while readers are running |
| `statelog` | The durable-state framework, with THREE domains on it — the tracker and the knowledge base strictly ordered and claiming identity, the vectors compacted and claiming neither. One ordered stream per domain is the write-ahead log, N identical SQL copies are the durable state, and the checkpoint commits in the same transaction as the rows. NOT the store's own WAL — that is Turso's local journal, per file and invisible to any peer; this is an APPLICATION log a fleet derives its SQL state from, and the two share only a word. The write authority is one sentence — take ONE snapshot of your own rows, decide and form the expectation inside it, publish, let the broker arbitrate, never guess — and every clause buys a specific failure: a second read pairs an old decision with a new expectation and the broker accepts it, and an expectation formed from `version` rather than from the ARBITRATION ANCHOR wedges on the one case a gate creates. Three values with three readers (anchor, version, coverage), three write outcomes (`applied` / `pending` / `unknown`, never a bool) — and TWO PENDINGS, told apart by `Waited`: one waited and the node is behind, one never asked, and reported as a single fact a deliberate skip reads to an operator as a node falling behind under load. `NoWait` has exactly ONE correct caller, a sign-in's session record, because the cookie carries the record's own position and every node validates against its own applier, so the row this node would wait for is a row nothing in the answer reads; it does NOT touch the session wait, which is the one that makes a decision read the caller's own previous write, and the FLOOR THEOREM in the package doc with all three of its enforced preconditions, because retry-at-zero on a trimmed anchor is safe only because of them and any consumer using per-subject conditional append inherits the hazard. One suite certifies every domain — `statelogtest`, in the `queuetest`/`coordtest` idiom, with a control expected to come back clean and a meta-test that hands it a domain which lies. Its ALARM TABLE lives here rather than beside any one subsystem, because an alarm is the framework's answer to "is this node doing its job" and everything above it asks the same question. Every alarm fires at a threshold ANOTHER decision already made — the grace that sheds a node, the grace that moves its seats, the budget a caller was promised, the replay window an operator set a log's ceiling to hold — because an alarm with a threshold of its own is a second opinion about one event, and the two drift. One evaluation feeds every surface, so a gauge, a log line and a screen can never disagree about whether something is wrong. Two of its inputs are MEASURED rather than read off a gauge: a strict log's intake over the trailing day, counted out of the log's own records (every record of the day is still in it, `min_age` being floored at a day) and a POINTER in the reading, because a log that took in nothing and a log nobody measured — compacted, younger than the day, a node that has not ticked — are opposite facts; and how long a seat binding has DANGLED, which is an age this node's own evaluations observed from first sighting to latest, because no record carries when a binding began to dangle. THREE LOOPS PER NODE make its recovery path real rather than declared, and each is useless without the other two: a POSITION HEARTBEAT (the trim takes a minimum across these rows, so a node that publishes nothing is one it cannot see and trims past), a SNAPSHOTTER (declining while it is catching up, holds an undecodable record, is short of disk, or is the only member — and retrying soon rather than waiting out the interval, because every one of those clears at boot), and a DONOR (every member serves, because a fleet whose only donor was down would have nothing to give). The recipient half alone is the worst state this has: it reports itself working while asking a fleet that can never answer. A FOURTH loop is the TRIM, and it is the one without which a log only grows: a fleet singleton evaluating six terms per domain, purging what all six permit, and PUBLISHING what it concluded — because which term came lowest is one tick's conclusion and HOW LONG it has been lowest is a property of a series of them, and a duty that moves on a lease carries no memory across the move. A stream's CEILING is the one setting none of that can change while the engine is running: a resize is decided against the usage the log is at and a publisher makes that a moving quantity, and what retires a request the broker already queued is the broker PROCESS restarting. So it is a fleet-wide window across three modes — `normal` publishes, `maintenance` may write configuration, `seal` may NOT, which is precisely what makes a seal-mode acknowledgement evidence rather than a claim — driven by an evidence table (`maintenance.go`) that states every transition once. Two of its inputs have no other source: a backup ANNOUNCES what it covers when its manifest is written (the node holding the duty is rarely the node that took the copy and can never see a directory on another host), and the evictions come from the REPLICATED rows rather than a bucket, because that is where an eviction actually lives. SETTLED IS NOT APPLIED: the checkpoint moves past a record a node RETAINS, so a reader that decides from an ABSENT row reads `PrefixIn` — the prefix its own snapshot has APPLIED — and `DeferredIn` — whether any retained record's scope covers what it reads, asked of the whole deferral index and never the earliest record — inside the transaction that reads the rows |
| `textindex` | The engine's own lexical search: the analyzer that turns a document into terms, and the BM25 arithmetic over the inverted list they are stored in. It exists because Turso has NO fts5 and no `fts` index method — `internal/store/caps.go` probes both on every open, and behind the experimental flag that gates every `USING` clause the engine answers `unknown module name 'fts'`, so the gate is not what is missing, and the alternatives were refusing knowledge search on the only driver this build ships or embedding a search library with its own index format, file and backup story on every node. PURE FUNCTIONS OVER VALUES — the SQL is `search`'s — so the ranking is testable without a database and an index rebuild cannot quietly change it. Every tuning constant is pinned by a test naming what it buys: K1 at 1.2 because these documents repeat their subject constantly, and a higher one ranks by verbosity instead of by coverage |
| `search` | BOTH HALVES OF KNOWLEDGE SEARCH, and the lexical one arrived here when the package that held it stopped projecting: `Indexer` maintains the BM25 inverted list over `textindex`'s analyzer, and it is the one reader in the tree that CROSSES THE ESTATE BOUNDARY — its sources are replicated rows and its index is this node's own, so every comparison is a batch from each side rather than a JOIN, and the walk that replaces the join repairs a drifted row where a version compare only ever noticed a source that moved. Plus the SEMANTIC half's arithmetic and the two-stage retrieval it composes, and the vector DOMAIN that fills it — the framework's second, and its first COMPACTED one. `Quantize`, `Hamming`, `TwoStage` and `Fuse` are PURE FUNCTIONS OVER VALUES for the reason `textindex` gives for the same shape: a ranking that can only be exercised through a database is a ranking nobody re-measures. What the deterministic gate measures is the ARITHMETIC — that an exact rerank over a 1-bit candidate pool recovers the exact ranking at sufficient depth — and never recall on a company's own documents, which is a property of that corpus's distribution and of nothing else (0.29 to 0.98 over a family of embedding-shaped generators); `crewlet search eval` is what answers that, judged against the exact f32 scan so nobody authors a judgement. The floor is a CURVE, per corpus size, because recall from a sign code decreases with N. The SQL is here too and the estate boundary is why: the vectors are replicated and the lexical index is this node's own, so no read joins them and the fusion is over two ranked lists of ids rather than a UNION. Every indexed document also carries a SHARD in both estates — a stable hash of its own identity into 64 buckets, unrelated to every other thing this engine partitions on, so a source that is re-filed keeps its bucket and the division follows the corpus rather than the company's shape. Nothing routes on it: a node holds every bucket and reads every bucket, and what the column buys is that a scan's cost is measurable per BUCKET RANGE rather than per corpus held. NEITHER ESTATE INDEXES IT, which is the same conclusion migration 0004 reached about the vector scopes and measured at 1 min 44 s against 50 ms: a bucket range is seekable, so an index over it is how a planner stops driving on the term and starts walking a sixty-fourth of the corpus instead. The FAN-OUT is here too: a coordinator divides the 64 buckets across the live nodes, each scans its own range, and the merge is SCORE-WITHIN-METHOD FIRST and RRF ACROSS METHODS SECOND — because RRF combines different rankers over one corpus, and run over one ranker across disjoint slices it ranks by PLACEMENT (a rank-1 on a weak slice contributes 1/61 against a rank-2 on a strong one at 1/62, so 0.10 outranks 0.98). Taking FuseN per slice is EXACT rather than sampled, because the slices are disjoint; and BM25 stays comparable across them for the one reason a design that divided STORAGE could not borrow — every node holds the whole corpus, so THE SCAN IS BUCKET-LIMITED AND THE STATISTICS ARE GLOBAL. An assignment that does not answer is NAMED (`buckets_answered` / `buckets_missing` / the absent nodes) rather than silently truncated, because the coordinator holds the whole corpus and could have answered alone — a short result set is indistinguishable from a short corpus |
| `events` | The envelope and the typed-payload registry. Two load-bearing properties: evolution is additive-only, and an event type this build does not know decodes, round-trips and re-publishes losslessly — a rolling upgrade puts unknown types on the wire, and dropping them would make every upgrade an outage |
| `org` | The organization model: the hierarchy, the seats that hold it, and the identity derivation every node shares. A seat's id is DERIVED — a UUIDv5 over (org name, handle) — so every node computes the same id with no database and no running instance, which is what lets the node that wins a delivery route to a seat it is not running. A pool miss means "not on this node", never "does not exist" |
| `iam` | The engine's IDENTITY VOCABULARY: who is acting, what sort of thing they are, how far through enrolment they got, and which capabilities they carry. A LEAF, and that is the point — config, the tool layer and the query registry all have to be able to SAY "principal" without any of them pulling in a password hasher, a store client or a coordination backend, so this package holds VALUES ONLY and a test walks its imports to keep it that way. THREE NAMESPACES THAT CANNOT COLLIDE, by the SHAPE OF THE VALUE rather than by a uniqueness check somebody has to remember to run: a seat handle is ONE SEGMENT, a person's login joins segments with DOTS, a machine's handle joins them with a COLON — so both of the grammars here require a separator, which is the one thing a seat handle can never carry, and they require DIFFERENT ones — and both stop at `MaxLogin` (64, a seat handle's own width, because a login is a SUBJECT TOKEN the broker indexes for the life of the deployment and sits in the author column beside a handle), enforced IN the grammar so every surface that enrols, renames or composes one inherits it; nothing bounded a login before. And EACH GRAMMAR BELONGS TO ONE KIND (`ValidLoginFor`), which disjointness alone does not give: `token:<id>` is the login a Tier A token acts under and the directory row holding it is what binds that token to a seat, so while either kind could take either shape a person redeeming an invitation as `token:ops` made the deployment's own credential act as their seat — and config refuses a Tier A token id unless `token:<id>` is itself a machine handle (`ValidTokenID`), since a token IS a machine. That replaced the `operator:` prefix `opsmcp` used to write, which showed one person as two people three rows apart in the audit feed because a work commit carried the bare id and a page commit the prefixed one; the kind is a COLUMN now. Nothing here decides anything — a gate asks `Can`, a route declares an `Access`, a store writes what `ActorFor` returns — and a caller's OWN RECORD (inbox, pins, priorities, personal views) is written AND read under `RecordOwner`, the name `ActorFor` writes with: the seat for a bound person, the login for everybody else. There were two answers, the tools writing under the login and the query surface reading under the seat, so an unbound caller's record was written where nothing read it — and `NamesSelf` says a bound person's login names the same record their seat does. SOMEBODY ELSE's record is `OwnerOf`'s, the one function every tool, question and route resolves a name through: a LOGIN is its holder's record (a bound person's seat, followed through a rename), read through the `Holders` seam the engine implements over the identity directory and the chart — the one read this leaf is HANDED rather than makes — and only once the caller's `MayLook` gate has let it be, so the ORDER (the authority first, the directory second) is enforced in the leaf rather than remembered at three call sites and a refusal comes back as a `LookRefused` carrying the caller's own answer; a `token:<id>` login is held by the Tier A entry of that id on the answering node and by nobody when none declares it; and a login is never matched against the roster (`NamesLogin`), because `jane.doe` resembled the seat `jane` ("Jane Doe") and `set_priorities` replaced that seat's queue; anything else is handed back UNSETTLED for the chart. `ErrNoHolder` and `ErrHolderUnseated` are the two answers, and every other error is UNKNOWN. What a request actually presented is `From`'s THREE-VALUED job, because the engine knowing you are nobody and the engine being unable to tell are different answers. `ActorFor` answers the CREDENTIAL too (`Actor.OperatorID`): a principal's `Via` — `pat:<credential id>` for a machine token acting as its owner, `session:<lineage>` for a browser session — or its login, because a token is composed AS its owner and without it every write one made was recorded exactly as the owner's own, and a session's person is the author already, so repeating their login said nothing about which sign-in wrote; the `pat` and `session` classes are refused to every machine login so each name only ever means its credential. `NormalizeEmail` is here for the same reason the name grammars are: TWO DOMAINS MATCH ON AN ADDRESS and must never disagree — the chart derives `chart_seats.email_index` from it so a vendor payload resolves to a SEAT, and `iamdomain` derives a person's keyed blind from it so a sign-in resolves to a PERSON — and written twice one address would reach a seat and a different person. ELEVEN GRANTS, and the eleventh is `people:manage` — authority over PERSON ROWS, split from `config:write` because changing the company document rebuilds every seat's tools while changing a person's row decides who may do that tomorrow, and it BOUNDS ITSELF: a caller may not confer a grant they do not hold, on anybody, themselves included. `audit:read` is `transcripts:read` renamed once the identity trail landed on it — ONE GRANT OVER BOTH TRAILS, because an agent's turn names the principal that woke it and a sign-in names the session every later turn carried, so a reader holding one and refused the other could establish neither half. TWO HATS, NEVER ONE LADDER: a `Grant` is authority over the DEPLOYMENT and a `Colleague` level is reach into the COMPANY's own work, and folding them names the same counterexample pair every time — an auditor holding two grants who must not be assignable, and an SRE who files bugs and changes nothing about the deployment. `Colleague`'s zero is the CLOSED end and is a real setting, which is the one place this package's zero value is meaningful rather than refused, and it is meaningful only because it is the closed end — the opposite of the `allow_anonymous_read` bool whose zero was the open one. `proof.go` holds the two VALUES config reads about proof (`SecondFactor`, `MinPasswordChars`) rather than `iam/credential`, for this package's own premise: internal/config has to be able to say "a second factor is required" without linking a password hasher — and `Recency`, the step-up window a rule asks for, spelled as the `api.auth.session` key that sizes it, which `Principal.Proved` judges against the principal's TWO deadlines (`ReauthAt`, `SensitiveReauthAt`): two instants and not one, because a proof forty minutes old is inside one window and outside the other |
| `authz` | MAY this party do this, to this? ONE FUNCTION over ONE TABLE, pure over values apart from the chart. THIRTEEN authority classes — read, self, colleague write, own record, own or lead, SAVED VIEW, container, chart object, destructive, authored, operator, and the two DIRECTORY classes — and a row per verb, because there are dozens of verbs and thirteen ways to decide one — a table keyed on the verb would state each rule dozens of times and drift on whichever copy somebody edited. "NO AGENT MAY TAKE THIS" IS A FIELD ON THE ROW (`humanOnly`) and not a class, because it is a fact about the VERB that composes with whatever class decides it — purging takes the admin grant AND a principal that is not a seat, archiving a project takes the container's lead AND the same — and as a class of its own (`ClassPurge`, now gone) it was already two spellings of one rule; it is checked BEFORE the class, so an agent is told it is a seat rather than that it lacks a grant it plainly holds. A SAVED VIEW is its own class because the table is keyed on the ACTION: two verbs would let the CALLER pick which question is asked, and a caller writing a shared view would ask the personal one — so the PAYLOAD decides, an owner making it that person's record and none making it its container's (a project's lead, a unit's, a person's page, or the workspace, which is the admin path alone). And a save REPLACES a view whole by id, so the payload is only half of it: `save_work_view` asks the same action again on the STORED view's owner and container once it has read them (`tracker.ViewPrior`), and the write refuses a prior that no longer holds — decided on the arguments alone, anybody who may keep a view could overwrite a project's shared tab or a colleague's personal view. A VERB WITH NO ROW IS REFUSED rather than defaulted, which is how a new verb ships ungated and ships looking correct; three walks fail the build on a verb with no rule, a class no verb reaches, and a GRANT NOTHING ASKS FOR — the last of those is what caught `state:read` opening nothing at all. Those three hold the table against ITSELF, which a stale row satisfies perfectly, so a FOURTH walk lives in `internal/agent/builtin` and holds it against the tools this build registers over BOTH surfaces, in both directions — it caught two rows that outlived the tracker's goal verbs and seven tools a seat uses every turn with no rule at all — and a FIFTH beside it goes one level down, holding the ARGUMENT NAMES each tool's object is read from against that tool's own JSON Schema: four rows named arguments their tools do not have, so `my_work`, `mark_inbox`, `set_pins` and `save_work_view` read an empty owner on every call and were gated shut for everybody but an admin. The [Chart] seam is CONSUMER-DEFINED, FIVE relations — who leads a person, a project, a unit and a PAGE CONTAINER, and whether somebody leads ANYBODY — and every one is THREE-VALUED. The fifth is asked of a record NOBODY HAS RESOLVED YET (`Object.Unresolved`): somebody else's LOGIN is resolved by the identity directory, whose answers differ by login, so a person verb decides first what it can without the record — the admin grant admits, a class with no lead path and a caller who leads nobody are refused with EXACTLY the decision a seat they do not lead gets, and a caller who leads somebody is answered `ErrUnresolved`, the one unknown a caller acts on: resolve it, then decide again on the record. Decided after the lookup, a login nobody holds was refused while a held one this node could not place answered 503 naming its seat, so anybody read which logins exist off the difference. The first four are questions never asked with another's key: a unit declares its tracker project in `project:` and its container in `space:`, so the project relation asked with a container key matched only a company that spelled the two alike and refused the lead everywhere else with no error to notice, which is what `LeadsContainer` exists to make impossible. Three-valued because the seam replaces a `func(...) bool` whose body opened `if c == nil || c.Org == nil { return false }`, so a node booting, applying a revision or behind the chart log told every lead in the company that they lead nothing, while reporting itself healthy. Err is UNKNOWN and answers 503, never 403 — and the admin grant is checked BEFORE the chart, so a lagging node never tells an operator it cannot tell. Route policy travels with the REGISTRATION and the wrapper is the only reader of `r.Pattern`: a middleware runs before the mux matches, so anything it decided from would be a second router. A refusal the router renders itself is the ENGINE'S ENVELOPE (`EnvelopeRefusal`) — `403 unauthorized` carrying the rule's `reason` and `Decision.Grants`, the capabilities the deciding rule would have admitted the caller on, which a walk holds against `Decide` over every verb, kind and grant; `503 unavailable` with a `Retry-After` for UNKNOWN — because it was `http.Error`, and `/iam` and `/chart` answered every refusal in `text/plain` no client of the rest of the API could branch on. The one authority concern that IS outside is the canonical-path refusal, because Go's own mux cleans and redirects before it matches — measured, a check inside the route handler never fired. EVERY ROW ALSO STATES HOW RECENTLY ITS VERB NEEDS THE PRINCIPAL TO HAVE PROVED WHO THEY ARE — the STEP-UP, `iam.Recency`: none, `step_up` (config, chart, setup, credential and deployment writes) or `step_up_sensitive` (a reveal, every `/iam` write but ending your own sessions, ending everybody's) — because it was a setting nothing read and a week-old cookie reached every one of those; asked AFTER the class admits, so a caller who could never take the verb is told what they lack, and refused `403 step_up_required` naming the window, the code `/auth` already used. A zero recency fails a walk (read as none it is a sensitive verb shipped open) and so does a TOOL row asking for one, because a seat has no keyboard and `/operator/mcp` is not a step-up surface; a credential with nobody at a keyboard is fresh by construction, stamped by the guard. `Decide` takes `now` as an ARGUMENT for it, which is what keeps it pure over values |
| `config` | The two tiers. Tier A (`Bootstrap`) is ops-owned, on disk, restart to change — including the retention estate (`stream.tracker_retention`, `store.snapshot_dir`, `retention.backup_owner`), because how a deployment backs up and how long its disk holds a replay window are facts about the OPERATOR rather than about the company. Tier B (`Company`) is founder-owned, versioned in the store, edited live. A third channel beside errors: `Warnings()` on each tier is what is VALID and worth knowing before applying — a declined fsync's window, a trim that will never advance, a unit keyed on a name somebody will rename — printed by `crewlet validate`, never affecting its exit code, because a warning that can fail a build is one somebody suppresses. Tier A is the root of trust and therefore resolves with `EnvOnly` — it holds the keys to the secret store, so it can never read a value out of it. Tier B's secrets are `${VAR}` POINTERS, stored verbatim, resolved only where a provider or transport is constructed. FOUR SETTINGS STOP BEING OPTIONAL the moment `api.port` is non-zero, and `api.go` is where the rule lives: `api.external_url` (the cookie's Secure flag and __Host- prefix, the CSRF origin, the OIDC redirect base and every webhook URL — the engine is behind a TLS-terminating proxy and can read none of it off the request), `api.auth.max_grants` (THE CEILING on what a directory row or an IdP group mapping may confer, intersected per node per request so a mixed fleet is a LEGAL state each node publishes a hash of), at least one `api.auth.tokens` entry (a fresh estate is empty, so it is what creates the first person, and the way back in when the provider is down) and `secrets.keys` (it signs every session cookie). A token's value is checked on what the `${VAR}` RESOLVES to, which is the only place the floor bites, and `api.trusted_proxies` is a CIDR list and never a bool, because the question is whether THIS peer is the proxy. `allow_anonymous_read` and `api.auth.disabled` are DELETED into permanent named refusals: the first was a bool whose safe value was its zero and which was `omitempty`, so closing it did not survive an export round trip, and the second authenticated the EMPTY credential into full operator authority |
| `configplane` | The control plane: how a fleet agrees which company configuration it is running, and what a node does when it does not have the current one. An append-only ACTIVATION POINTER names the epoch and every node writes its own apply status; a node reads both and picks a posture. Append-only because re-activating an UNCHANGED revision is the credential-rotation gesture, and a pointer keyed on revision id would rebuild nothing on exactly that operation |
| `seat` | Which seats this node runs. The placement policy is deliberately dumb — greedy claim to `ceil(seats / live nodes)`, converging in BOTH directions, `preferred` orders the attempt and never gates it. `watchdog` is the other half: a wedged event loop cannot be signalled, so the thread's only honest move is to exit |
| `node` | Where the fleet layer becomes runnable — a broker client, a coordination backend and the seat host, composed. Deliberately thin: the one behaviour none of its parts can express alone is that WHEN A SEAT IS ACQUIRED ITS MAILBOX IS ATTACHED, and released in an order that never leaves a seat consuming work it no longer owns |
| `engine` | The ENTANGLEMENT POINT — which concrete thing satisfies which seam. Deliberately many small files rather than one large one -- this package is flat, and the hardest passage of all, the inbox handler, is a package of its own (`agent/inbox`); the config apply is `reconcile.go` and `epoch.go` here. `mcp.go` is where `mcp_servers:` becomes running children: shared servers on the epoch, per-role children on a seat's LEASE, each claimed seat with its own bridge and its own registry — two children of one template publish the same tool names, so anything shared between seats hands one seat's identity to another. It is also where every fleet-singleton DUTY is armed, and the reason is the same: the trim, the maintenance sweep and the EMBEDDING duty each need the company's provider, the log's publisher and the fleet's leases in one place. The alarm table is the exception that proves it — evaluated on EVERY node on its own HEARTBEAT (`statelog.AlarmInterval`, the fifteen seconds every fleet heartbeat beats at) and never behind the trim's duty claim, because a reading describes one NODE and a table evaluated only where the lease sits would report the lease holder's health as the fleet's, and because the trim's quarter-hour silently raised every sixty-second threshold to fifteen minutes. The trim's tick is where the SCANS the table needs are taken — the vector coverage and each log's daily intake, the second withheld through a log's first two days because its first day is the one a company imports into — and the heartbeat and every report read them back rather than repeating them; a gauge whose measurement became unknown is WITHDRAWN rather than left holding the last figure. The directory's dangling bindings are observed on the heartbeat itself, re-read only when the directory's or the chart's applied position moved, and a beat that cannot read either changes nothing — an outage neither clears a firing alarm nor restarts a residue's clock. A binding dangles by ONE rule, `bindings.go`, which is the request path's own seat table applied to a person's row, and both `/iam/check` and `iam_binding_dangling` ask it. The IDENTITY estate's four duties are armed here too (`identityduties.go`) — the per-bucket sweep publisher, the OIDC deactivation probe, the KEY DUTY and the claim report — on FOUR LEASES rather than one duty with four jobs, because their costs land on four different systems; a duty whose interval exceeds an hour CLAIMS hourly and runs every N claims, since a lease may live no longer than `coord.MaxDutyTTL` and the probe's interval is an operator's setting. They are armed only where the identity domain AND the `workers` role run — elsewhere every claim would be refused, so arming them would be loops that never run reported as duties that do — and `/health` serves what a node armed as `identity_duty_seconds`, because a duty never armed looks exactly like one finding nothing. And the COMPANY KEY a blind is derived under (`companykeys.go`) is MINTED here, on first use, with a CREATE the store refuses over a key that landed first, and every minter reads back whichever did: the store is last-write-wins for a put, and two nodes that each found a key absent each read their OWN write back and derived under different keys. The fleet hold and the in-process gate stay only as the courtesy that keeps two nodes from each running the guard — a lease cannot fence a holder paused past it, so a put under the hold split the company exactly as it did with none. A mint takes a REQUIRED guard, and the identity key's refuses both an estate holding a blind (minting over a DELETED key orphans every address) and rows that have not APPLIED the whole identity log — behind it, or holding a record they retained, which moves the checkpoint without writing the rows — whose "no blind here" may be rows that were never applied; the log's END is read first (`IdentityLogEnd`) and the rows prove themselves against it in their own snapshot. The party registry has TWO rebuild triggers — a published company, and the identity applier's standing signal (`directory.go`), with a thirty-second re-read behind it — serialised on one lock, each building a WHOLE registry for its company and swapping it, never a diff; a directory it cannot read carries the last reading forward, and a node that has NEVER read one withholds every human seat until it does, because the reading it would otherwise carry is the chart-only one. A node that runs no identity domain reads the FLEET's directory (`fleetdirectory.go`): every node that runs it SERVES who holds each seat over the queue's ephemeral scatter, naming seats and stages and never a person, each answer SIGNED under the fleet's keyring and echoing the asker's nonce because the broker authenticates nothing, and the satellite takes the most-caught-up answer it can verify on the thirty-second net — never one past the log's head, and never one below the reading it already took, since the best of one scatter is only the best of whoever answered in time — nobody answering over a log that has never carried a record is an empty directory, and over one that has is the unknown arm. The appliers' post-commit hooks are one `applyHooks` value built by `Engine.applyHooks`, so the register's own cases build appliers from what a running node hands them — a nil hook compiles and passes every case in the domain's own package The probe is BUILT in one place (`Engine.newProber`, held by a gate) over the node's audit trail, because one built without it ended sessions as `idp_revoked` in `iam_history` and announced none of them on the feed. |

*The agent runtime:*

| Package | What it is for |
|---|---|
| `agent/turn` | The turn loop — an executor and a reviewer — the guards around them, and the one question the whole engine turns on: did this turn actually reach anybody? WHO IS WAITING is derived at dispatch from the trigger's own type and WHAT RAN is the tool loop's own record, so neither half is something a model can get wrong. It replaced a reconciliation of the planner's PREDICTED tools against reality, which is where both recorded delivery incidents came from |
| `agent/runner` | Drives the phases against real models and real tools — the wiring between the prompt builder, the tool registry, the provider chain and the turn loop's contract, none of which knows the others. Plumbing, plus the one rule plumbing cannot express: what happens when the model never gives a structured answer at all. What a valid answer IS lives here too, in the decoders; the SHAPE is `agent/structured`. The executor has TWO runtimes (`agentrun.go`): the native tool loop, and somebody else's agentic loop behind one seam — same prompt, same surface, same submission, same rescue, and only the rounds happen elsewhere |
| `agent/structured` | How a phase gives a typed answer: it CALLS A TOOL rather than writing prose the engine parses, so the ordinary tool loop carries the answer out and nothing needs a side channel. Three rules travel with the shape — a rejected submission goes back to the model (the one failure a model reliably fixes), the last write wins (a second submission is a correction), and an absent submission is not a value (every rescue path turns on telling "decided this" from "nothing decided anything"). The decoder belongs to the caller, and `Remarshal` — the one mapping from arguments to a typed payload, which `delegate` decodes through too — REFUSES a field the payload does not have, at any depth and by name: decoded the obvious way it was DROPPED, so a reviewer's misspelt `completed_work` never told the next round what had already landed, and a delegated task's `afer` ran at once on input it was told to wait for |
| `agent/phase` | The per-phase vocabulary: which phase is running, which model it runs on, and what a turn concluded. `execute` keeps its wire string although the phase it names now decides as well as acts — the value is a column in the event store and read by every dashboard, so renaming it would buy a better word at the cost of a value migration |
| `agent/toolloop` | The model↔tool round-trip, and the `Suspend` primitive a detached coding run returns through |
| `agent/execstate` | The wire format for a suspended executor loop. A detached sandbox run stops the phase mid-loop with its tool call UNANSWERED and is re-entered later — after a restart, possibly on another node — so the conversation is serialized into the pending-run row rather than parked in a goroutine. The sandbox coordinator carries it back without ever decoding it. Evolution is additive WITHIN a version and a reshape takes a new one plus a PERMANENT reader for the old (`compat_v1.go`): nothing rewrites a parked row, and the layer holding the blob cannot decode it, so the only thing that can ever read an old row is a build that still knows how |
| `agent/extension` | The round-cap extension judge: when a phase runs out of tool rounds, a cheap model says whether it is progressing or thrashing, and the engine grants more or falls through to the rescue path. THE ARITHMETIC IS SEPARATED FROM THE JUDGE — how many rounds a decision is worth is pure policy, impossible to exercise through a live model, so it is a value type tested directly |
| `agent/subagent` | `delegate`: the short-lived WORKERS an executor hands work to. One tool loop with a persona (a `workers:` template or an inline prompt), a slice of the parent's own tools, a declared answer shape — the task's own `output` or its template's, the first held to the second's load-time rule (`config.ValidateWorkerOutput`) before anything runs — and hard caps on rounds, wall-clock and tokens. A worker is a LEAF — it delegates nothing further and contacts no colleague — which is the only thing bounding the depth of the construct, so there is no depth counter anywhere. One call is a GRAPH: `after` makes a task wait for others and receive their submitted fields, run in topological waves under one barrier each, because a wave boundary is checkable where a per-task readiness queue's deadlocks are not. Three rules are enforced in code because a prompt is only a request: the grant is a security boundary (a config template widens nothing — every name still passes the same filter), one budget slice covers a whole call however many tasks it has, and an absent submission is `no_result` with the worker's prose rather than an answer the engine invented |
| `agent/inbox` | What wakes a seat, and what it must not be woken by twice. `ledgered` is the completion side |
| `agent/ledger` | The prior-work ledgers: `iteration` (within a turn), `conversation` (across turns in one thread), `budgets`. Structured, never a transcript replay — the thread has moved by the next turn |
| `agent/prefetch` | The turn-start context assembly: relevant knowledge, memory, the CHAT THREAD the turn was woken in, the blocks a prompt gets. FROZEN before the runner exists, which is what stops a self_iterate loop moving the system prompt underneath a turn. Nothing re-fetches mid-turn any more — the executor asks, with `search_knowledge`, over this same seam. The thread block is what a prompt telling an agent to go and read one cost: three MCP rounds before a word, and an agent that skipped them answering a pointer. It is bounded by WHOLE MESSAGES against an item cap and a byte ceiling anchored at a third of the conversation ledger's, because both are frozen into one system prompt and re-sent every round; it reports what it dropped; and its two failure sentences differ, because "nothing was said earlier" and "this node could not read it" send a seat to opposite places — the second covers a maintenance-mode node, an instance down at boot and a refused seat token, none of which is a chat API error |
| `agent/prompts` | The prompt text, and one builder per phase. Tuned against observed model behaviour; a reworded section is a behaviour change. Two prompts where there were three: the frame that DECIDES is the frame that acts, so the identity scaffold is sent once rather than twice and thrown away in between |
| `agent/skills` | Prompt fragments admitted from the knowledge base and injected per phase, plus the load-before-use guard. The registry is keyed by the PAGE a skill was read from, not by its key: every change names a page, and a page whose key was edited no longer says what it used to hold, so a key-addressed registry could never remove the old key. Pages as the identity are also what make a one-page update end exactly where a walk of the same pages would |
| `agent/skillsync` | The one loop per node that keeps the registry current: a walk when an apply moves the skills source (and the previous source retired at once), a read of the one page a webhook named, a `tool_skill_page_changed` broadcast so every node hears a webhook only one node consumed, a periodic walk behind all of it, and a bounded retry behind a walk that failed. ONE GOROUTINE DOES EVERY READ, which is what lets it need no version comparison: a later read always sees later state. Every change is READ BACK, a deletion included, because the backend and never the report says whether a page is gone. And a walk or a read that finds what the registry was built from changes nothing and logs nothing above debug, or the periodic walk would repeat every registry and audit warning on every node each interval |
| `agent/builtin` | The tools the engine itself ships, and the ONE AUTHORIZER that gates both catalogues — a seat's registry and the operator's — built from `authz.Decide` over the running chart and wrapped round every tool ONCE, at registration: the action IS the tool's name, so there is exactly one place the check could be forgotten. A NIL AUTHORIZER REFUSES, the opposite of a nil skill gate and deliberately so — a missing skill gate is a company that locked nothing, a missing authority decision is a surface that shipped without wiring one. What each tool is ABOUT is a table of ARGUMENT NAMES rather than closures, because a name can be walked against the tool's own schema and a closure can only be called with arguments somebody thought of; a personal verb that takes no handle is about the caller. The few checks that need a stored row first — re-routing a task, a project's lead-only facets, archiving it, setting somebody's priorities — ask the SAME seam from inside the tool under a dotted action, since which project a task is filed under is not in the arguments. A person verb that takes a name (`get_person`, `work_inbox`, `set_priorities`, and the HTTP surface's `MarkInboxFor`/`SetPinsFor`) resolves it to a RECORD first (`WorkDeps.personRecord`, over `iam.OwnerOf` and `WorkDeps.Holders`) and is decided on THAT, so it is `inTool` — but the directory is asked only once the verb has been decided on the record nobody has resolved yet (`WorkDeps.mayLook`, `authz.Object.Unresolved`), so a caller who leads nobody is refused before any lookup, in the very words a seat they do not lead gets, even when this node cannot read the directory; a login nobody holds is then decided on the name as typed before it is refused as naming nobody, and the directory's own words — which name the seat a login is bound to — go to the log and never into a refusal. Together that is what keeps the directory from being a roster. The same gate REFUSES AN ARGUMENT A TOOL DOES NOT DECLARE, naming it and what the tool reads, before any authority is asked (`ErrUndeclaredArgument`): a builtin reads its schema's properties and nothing else, so an undeclared one was DROPPED and the call answered as though it asked for less — the HTTP routes refused it and nothing else did, so a seat, a worker, a sandbox's bridge and the operator's assistant each saved `save_work_view`'s retired `owner` as a shared tab. The gate derives the principal FROM THE TURN when the context carries none, and never over an answer already there; and its wrappers are POINTERS, because a struct holding a func is uncomparable and the engine's epoch-identity `==` over a seat's tools panicked on it |
| `agent/colleague` | Resolving a free-text query to a seat — the lookup before an A2A ask, a chat mention or a page mention. A model types what it remembers, so the answer is either one seat or an honest list of who it might be, NEVER a guess. Four tiers, earlier ones short-circuiting later, so a query that is exactly somebody's handle is never fuzzy-matched against everybody else |
| `agent/turnctx` | What a turn IS, carried as an explicit argument rather than ambient state. A turn has at least five values that want to travel ambiently, and a goroutine SHARES whatever context it captured — forever, including after the turn that created it finished — so what would elsewhere merely obscure a dependency is a live data race here. |
| `tools` | The registry, holding four kinds of tool under one contract and recording WHICH at registration — the only frame that knows, and the last that can say |
| `mcp` | MCP client and child supervision. A stdio server is a process TREE, not a process. |
| `a2a` | Agent-to-agent asking: one ask, one answer, then closed. THE CHANNEL IS AN AUTHORIZATION RECORD, NOT A TRANSPORT — nothing queues here, and both the brief and the reply travel over the durable seat inbox, so a colleague owned by another node is an ordinary target. The in-process bus this replaced woke the target on whichever node owned ITS seat, where the queue was empty |
| `tracker` | The native work tracker as `statelog`'s FIRST DOMAIN — its records, its subjects and the arithmetic that has no database in it. Every change is one record published to the subject of the object it changes, so THE SUBJECT IS THE ARBITRATION UNIT rather than a routing label: two writers racing on one task contend at the broker and exactly one wins, and two writers on different tasks never contend at all. The record decodes in TWO PASSES — an envelope every build reads and a version-gated payload — which is what lets a node file a newer peer's record under its subject, probe for it, drop it through a gate and reprocess it later; a record that failed to decode at all yields no id, no kind and no subject to file it under. Its `Scope` is the complete set of objects an apply may write, stated by the WRITER, and the paths NEST — an object's path is written under its container — because that is the only shape under which a record deferred on a project blocks a write to a task in it while a record deferred on one task does not block its neighbour. `rank.go` is the manual order as a VALUE TYPE: a byte-comparable key with a magnitude head that borrows and carries, so repeated head insertion grows the key by about a character per sixty-two placements rather than exhausting a thirty-symbol alphabet, and the two mint sets — a create's pure integer at or above the origin, and every `rankorder` mint below it or carrying a fraction — are disjoint BY THE SHAPE OF THE VALUE, so the proof mentions no counter, no staleness and no in-flight create. A DEPENDENCY is the one relation with two ends and therefore the one gesture that is a SEQUENCE — `waiting_on` AUTHORED on the dependent, `Dependents` MIRRORED on the blocker — and the asymmetry decides everything: the counterparties are read before the first append so a full, tombstoned or absent blocker refuses the edge; the authored commit lands; the mirror is BEST EFFORT, because the edge is durable without it and refusing the whole gesture over it would lose what somebody asked for. The residue is a ONE-SIDED edge, which the applier DERIVES rather than the record carries — whether a blocker lists this task is a fact about the other end's rows, so a writer stating it would be stating what it read in another transaction on another subject, which is why the flag had no producer and the partial index the schema ships for the repair duty was over a column that was zero on every row. `one_sided_final` is the opposite kind of fact and stays on the record: the duty's DECISION that this edge will never be mirrored. Every collection a record carries whole takes a GESTURE resolved inside the decide — `Watch`, `Relate`, `Depend` — for one reason each time: a caller cannot form a set it read in another transaction, and a cap governs the set the write LANDS on. And the routing precedence is ORDER IN ONE FUNCTION rather than a list beside it: what THIS COMMIT did to you outranks the role you hold, which is what `mention`, `asked`, `answered` and `blocking` sit above `assignee` for — behind it `blocking` could name nobody at all, since the only handle it ever adds is the blocker's own assignee. `coerce.go` is the CUSTOM-FIELD table and it is PURE over values, for the reason `textindex`'s arithmetic is: a rule exercised only through a database is a rule nobody re-reads. It runs at the WRITE because that is the only place a value can be REFUSED — the applier deliberately salvages, since refusing a malformed value there would stop that task's every later change on every node — and it NORMALISES what it accepts, so the record carries one canonical form and every node writes identical rows without re-deciding. Nothing is rounded to fit: a field exact to two places refuses `3.14159` rather than storing a number nobody typed. The one seam it declares is the ROSTER, because a people field's value belongs to the EPOCH rather than to a row; a relationship's task is deliberately not a seam, being a row this package reads inside the decide's own transaction. Its AUTHORITY VALUES — `ProjectAuthority`, `TagAuthority`, `PersonAuthority` — state the TABLE'S DECISION and never its inputs: they used to carry a lead flag and an "is an operator" flag that every caller combined for itself, which was a second copy of the rule that consulted no grant, so a caller holding `fleet:operate` was refused here while every HTTP route allowed. The caller asks `internal/authz` and passes what came back; this package keeps only the half a chart cannot know — which FACET each answer unlocks — and the one rule of its own, which is about the gesture rather than anybody's authority: an AGENT never re-orders a colleague's queue, even one it leads, because that is a hand-off in disguise that bypasses the guarded take and the reassignment budget. Its applier also says WHOSE INBOX a committed batch moved: one `InboxMovement` per recipient per batch — identifiers and a count, never content — announced POST-COMMIT, because the transaction body can re-run and a movement announced inside it could describe a write that rolled back, and noted only when the notice row was actually written, so a redelivery the history guard already absorbed announces nothing. The count is a HINT, keyed on (record, recipient) |
| `changefeed` | How a committed record becomes a WAKE: a durable, fleet-wide pull consumer over one ESTATE — a bucket family's create-only change class, or a state machine's log. The estate is a SEAM rather than a document family (a coordination type that left with the last projector), because a log delivery has no family, no key class and no change record, and manufacturing a family for one would have started a projector over a bucket that does not exist: a `Source` names it as a string, an `Opener` opens one consumer over it, and a `Record` is one delivery from either. The principle is that a wake is derived by something that OUTLIVES the writer, never published by the writer's goroutine as a courtesy — the chat path already swallows a failed publish and the webhook path is safe only because the vendor retries, so the durable record is the retry. A group rather than a duty, because a lease flap on a duty holder would stall the whole company's notifications for stateless work. TWO dedupe layers, each covering the other's gap: a claim collapses a redelivered message and FAILS OPEN (a coordination store that cannot be reached must not silently stop notifications), and a deterministic per-recipient wake id lets the inbox and the completion ledger collapse what the open claim let through |
| `pages` | The engine's own KNOWLEDGE BASE — containers, pages, revisions and the change record — and `statelog`'s THIRD domain, its second STRICT one, which is what tells a per-domain checkpoint from a per-node one. There is no projection and no coordination family behind it any more: a write is one record on `CREWLET_PAGES_LOG`, an applier writes every node's rows, and a read reports the POSITION it answered at rather than a hydration boolean. Adopting the log removed all three of the two-key sequences the bucket forced: a create is ONE record whose apply writes the title, the head, the first revision and the history entry together, so there is no orphan claim and no grace rule for stepping over one. THE SUBJECT IS THE ADDRESS — a create arbitrates on the TITLE rather than on the new page's uuid, because two writers must contend for a name and two uuids never would — and the title travels as a bounded TOKEN, since a subject is a broker path and a title is prose carrying spaces, dots and wildcards; the applier recomputes it from the payload and REFUSES a record that took one address and claimed another. Plus the three things a wiki has that a tracker does not. A TITLE IS AN ADDRESS, so it is unique per container and it is what a create ARBITRATES ON. A BODY HAS A HISTORY, so revision N is the body at version N, including the newest — the other reading, that a revision holds what was replaced, collides with itself on the first save and leaves the current body reachable only through the head; a save must state the version it edited, unlike a work item's optional If-Match, because there is no per-field merge that makes overwriting prose safe. And a page is SEARCHED rather than filtered, so `Searcher` fulfils the `knowledge` seam and the native backend is a drop-in for Confluence. A RENAME IS ITS OWN OPERATION rather than a field of a save, because one record has one subject and a record carrying both an address change and a content change could arbitrate only one of them; it stamps the head's `scoped_through` and never its `version`, so the page's own expectation is never poisoned. A COMMENT DOES NOT SUBSCRIBE ITS COMMENTER here, which is the opposite of the tracker's participants rule and deliberate: a page a hundred people have remarked on would otherwise wake a hundred seats when somebody fixes a heading. The applier is also what NOTICES a tool-skill page moving — natively there is no page webhook and the change feed deliberately drops those changes, so the apply, which already derives the skill flag, is the only thing that sees both an arrival and a departure. THE RESERVED CONTAINERS ARE THE STORE'S RULE, not a tool's: it was one check on `write_page`, so a seat that could not create a page in the skills container could still save over one, rename one and comment on one, and every surface added later would have inherited the hole silently — now every write path asks, reading the keys live because they are Tier B. The two containers are reserved for DIFFERENT REASONS and refuse different gestures, which is `Reserved`'s shape: the SKILLS container is machinery excluded from search and routing, so an AGENT is refused every write there — create, save, rename and every comment path; the ORG ROOT is the company's canon and is searched and routed like any other, so an agent is refused only ADDING a page. Folded into one list, every agent write into the root was refused with a sentence claiming its pages were unreadable. It is a rule about the ACTOR and not about capability: a seat cannot see that a page there is machinery, and a person publishing there on purpose is what those containers are for. The CAPABILITY half is the tool layer's, because the store exempts every person and holds no grants: a person's create, save, rename, trash, restore or purge of a page in the SKILLS container is asked `pages.skill.write` (`config:write`, never a seat) once the container is known — a tool skill is injected into every seat's turn, so `knowledge:write` alone had been a way to rewrite the prompt the whole company runs under. A REMARK IS ITS AUTHOR'S: an edit is refused to anybody else in the decide's own snapshot, and a removal — which had no check at all — takes the author or a caller the table admits as a moderator, passed as `CommentAuthority` because this package holds no chart and no grants. And `ContainerKey` is the one canonical form of a container key, because eleven copies had drifted and six of them did not trim |
| `chart` | The company's own ORG CHART — its units, the seats that hold it, and the structure the engine executes — as `statelog`'s FOURTH domain and its third STRICT one. It left the Tier B company document because a nested object settles one question badly and three not at all: a write is the WHOLE document, so two people editing two different teams collide on a revision neither touched and the loser's change is overwritten rather than refused; there is no per-object arbitration, so the unit of contention is the company rather than the object; and the revisions say what the chart BECAME and never who moved, out of which unit, into which, at whose hand — which is precisely the audit a reorganisation needs. A party's PROVENANCE — the credential it acted through, the turn behind it — travels with it through `Writer.As` and is REPLACED there exactly as its grants are: As took none, so every `/chart` write recorded no `operator_id` and one made through somebody's machine token read as theirs. What is particular to an org chart is that STRUCTURE IS AN OBJECT IN ITS OWN RIGHT: a task belongs to a project and a page to a container by a FIELD on the thing contained, so two writers moving two tasks agree about nothing, while a chart's containment IS the thing — the tree every derived answer is read out of — and two reparents through one common ancestor can each be locally valid and jointly produce a CYCLE no node can see from the subject it arbitrated on. So there are TWO kinds of subject: the STRUCTURE on one subject for the whole chart, which deliberately serialises the rarest write a company makes to put a cycle out of reach rather than merely detect it, and the CONTENT per object, so two leads editing two seats never contend. The two are ordered by the LOG and never against each other per object, so a content record for a seat a reorganisation is about to remove still applies and is then removed: STRUCTURE WINS, because the structure is what every derived answer is read out of. A KEY IS NOT AN IDENTITY — a unit's key and a seat's handle are ADDRESSES people type and both can be reassigned, and a former key goes on resolving until something else claims it; the IDENTITY is the address the object was CREATED under, which resolves to it for ever and is NEVER ISSUED TWICE — no rename and no creation (a batch, a content write or an import) may take it, and a removal tombstones it beside the address it held — because a second seat created under it shares the first one's mailbox, lease and diary — and THE CHART IS NOT THE ORGANISATION: `org` builds the runtime tree a turn reads, with lead inheritance and manages-expansion applied, and none of that derivation is stored beside these rows, because a derived value written down is a second answer that drifts. HALF OF EVERY OBJECT IS OPAQUE HERE, on the row's `document` and never in a column: this domain can say what a unit key, a parent and a lead mean and cannot say what an `mcp_env` key is for, so a seat's model chain, credentials, sandbox cell, worker grants and schedules travel as bytes `org` owns both ends of — and NOTHING IS IN BOTH HALVES, which `org`'s partition gate holds by walking the two types and failing on a field that is in neither or in both, because the copy inside a blob is the one nothing validates, nothing indexes and nothing can arbitrate. A SEAT'S REMOVAL CONSULTS THE IDENTITY DIRECTORY inside its own snapshot and is ADVISORY, which is the whole of what a cross-domain read can be: two logs, two appliers, two anchors, so a bind and a removal each pass their decide and both land, and the residue is a NAMED legal state a duty reports. What it buys is that the ordinary mistake — removing a seat a colleague still uses — carries that person's name. A node that CANNOT read the directory REFUSES naming itself rather than deciding on an empty table, which is the one place the seam's absence is the second value rather than the third: a report may skip a finding it cannot compute, and a WRITE may not proceed on evidence it does not have |
| `iamdomain` | The company's own IDENTITY ESTATE — people, the addresses and logins they are known by, the credentials they hold, the invitations that enrolled them and the sessions they are signed in with — as `statelog`'s FIFTH domain and its fourth STRICT one. `iam` stays the VALUES LEAF; this is the durable half and it imports that leaf rather than restating it. What is particular to it is that ARBITRATION IS THE CONSTRAINT: the replicated estate forbids a UNIQUE index outside a primary key — a violation inside an apply transaction aborts it deterministically on EVERY node, which here is every login in the company — so there is no uniqueness check anywhere in the schema and nothing to add one to. What keeps two people off one address is that each claim arbitrates on ITSELF, create-only at an expectation of zero: `iam.email.<blind>`, `iam.login.<login>`, `iam.seat.<identity>`, `iam.link.<blind>`, `iam.session.<lineage>`, and NO TWO CLAIMS SHARE A SUBJECT, because a shared one never contends and the duplicate identity nobody refused is found by somebody signing in as somebody else. A person's id is minted first and every claim names it, so AN ENROLMENT IS A SEQUENCE for the tracker dependency's reason — one record has one subject — and the residue of a half-finished one is a claimed address with no person, a LEGAL named state a duty reports rather than corruption. The SCOPE IS FLAT AND BUCKETED (64, by FNV-1a over the id nothing renames, this domain's own and unrelated to `search`'s) and that is forced rather than chosen: a claim's subject is a BLIND and the person it is about is inside a payload the deferring node could not decode, so a per-person path would be computable by every node except the one that needs it — and a person's id in a deferral row is personal data in the one place this estate most carefully keeps clear of it. THE ROOT SCOPE IS REFUSED to every kind but the FOUR that can never be deferred, because a deferral at the root freezes every suspension, revocation and login at once — three of the four install a gate and the fourth is the framework's own barrier. TWO COUNTERS END SESSIONS and they are deliberately not one word: a person's REVOCATION EPOCH ends every session THEY hold, and the fleet-wide SESSION GENERATION (`iam.invalidation`, one subject for the whole company) ends everybody's — which is what a restore needs, because a revocation performed after an artefact was taken is rolled back with it and the generation is the only number that can be pushed forward without knowing who was affected. It is also the FIRST strict domain whose ReadinessInput is FALSE: no turn reads it, a seat's authority is the chart's, so shedding a company's seats because a human cannot sign in would be an outage caused by the wrong subsystem — what a stall gates is the REQUEST path, per request, with a 503 and never a 401. NAME AND EMAIL ARE SEALED BY THE WRITER before publication, which is what keeps the identity claim a byte comparison, and destroying the key is what a removal is; a LOGIN is deliberately in the clear, because blinding a value the dashboard prints on every row costs an operator their own audit trail for nothing. The APPLIER never decrypts: every sealed value is written through as bytes, because opening one would mean a fleet-secret read inside the apply transaction and an applier that can fail on a coordination read is one that stalls the log when coordination is down. A REMOVAL IS A KEY DELETION — the rows commit first and the key goes after, post-commit, because destroying it inside the transaction destroys it for a removal that rolls back; the residue of a failed shred is a row that says removed and a key that lives, which a duty retries. A key is also minted BEFORE anything owns it — an enrolment seals the address it is about to claim, an invitation its own — so a refusal leaves a key NOBODY owns, and the same duty destroys one only past an hour AND only on rows that have APPLIED the whole log (`ShredKeys`, `KeyCensus.Judge`, `CoversLog`), because a row not applied yet reads exactly like no row — and APPLIED is not CONSUMED: the applier's checkpoint moves past a record it RETAINS (a newer build's, one signed under a keyring key the node was not restarted with), so a census that asked the checkpoint destroyed live people's keys mid-rotation; the duty and `/iam/check` share the one judgement, which answers each key nobody's, waiting or UNPROVEN, and the blind-key mint guard proves its "no blind here" the same way; the refusal never shreds inline, since a mint that finds a key keeps it and the id may be a live person's. A MINT NEVER REPLACES A KEY AND ALWAYS RE-DATES ONE: it CREATES where there is none (the store refuses a create over an existing key, so two minters of one derived id seal under one key) and TOUCHES the one it finds, because a retried redemption re-uses its first attempt's key and the duty ages a key by its last write — and the duty destroys only the VERSION its census judged (`Sealer.Collect`), so a key re-dated between the census and the delete is spared, while a touch that finds a key destroyed writes nothing back and the mint makes a fresh one. The CLAIMS-FIRST enrolment order is what makes a claim's apply an UPSERT: the claims are what can be refused so they go first, and their apply leaves a RESERVATION row with no kind and no stage — not `active`, so it may do nothing — which the content record then fills in; every READER reports that row as a reservation rather than decoding its empty document, because decoded it was the unknown answer, and a Tier A token bound through a half-enrolled login answered 503 on every route until a sweep collected it. And the participation NARROWS (ingress and workers, never a seats-only satellite), which is the first entry in the register to do so and the case `participationIn` was written for. THE ADMINISTRATIVE GRANT IS `people:manage`, checked at the RECORD rather than only at a route because a record can be published by a CLI, a duty, a migration and a test, none of which passes through one — it was `config:write`, which was wrong in both directions and wrong loudly in one: the NODE's own writer holds `fleet:operate`, so every bootstrap enrolment and every invitation redemption was refused on a real deployment while the surface's own suite, built on a stub writer, stayed green. A MACHINE TOKEN IS MINTED ON ITS OWNER'S SUBJECT, reading the owner in the snapshot the credential is formed in (`MintToken`), because what it may carry is a fact about the owner NOW — a subset of their grants, never `secrets:read` or `people:manage`, nothing the minting party does not hold, a reach no wider than theirs — and a caller handing in a finished credential would be stating a read from another transaction. A PERSON'S TOKEN IS MINTED BY THAT PERSON ALONE, decided in the same snapshot on the minting party's ID (`TokenMint.Minter`, never a login, which a rename moves between people): whoever mints a token is shown its value and it acts as its owner, so `people:manage` minting on a person's account let any administrator hold a credential acting as anybody — the grant now mints for SERVICE ACCOUNTS only, the one kind with nobody behind it to mint for itself; `Reader.MachineToken` answers the row, the owner, their epoch and the fleet generation in ONE snapshot for the guard. AN ENROLMENT AND AN INVITATION CONFER ONLY WHAT THEIR WRITER HOLDS, exactly as an edit does — an enrolment is a grant change from nothing, and it was held to nothing, so `people:manage` alone could create a colleague holding every grant — and the two enrolments the NODE's own writer performs (holding `fleet:operate` and `people:manage` only) NAME their authority instead, checked in the person record's own snapshot: a redemption the INVITATION (no grant, reach or address it did not carry, and no link already spent or aged out) and the first person the one-time CODE — the one stated exemption, and the one enrolment ARBITRATED ON ONE SUBJECT (`founding.go`): person subjects never contend, so two founders redeeming two live codes at once each passed their own person record, and a founding now TAKES the exemption on `iam.bootstrap` before it claims anything, refused while another code's take is current (`FoundingInProgress`) — and ENDS every earlier unfinished attempt first, a removal on that attempt's own person subject that contends with its person record so exactly one of the two lands, found through the codes they took and, once a code row is swept, by the FOUNDER SHAPE only a founding's derived id carries (`FounderAttempt`): the founder is derived from the code, so the fresh code the documented remedy hands out derived a different person whose claims the stopped attempt's reservation refused for ever. The person record lands only on this founding's own current take with nobody else enrolled by `anybodyEnrolled` — the ONE predicate the route's open flag, the boot mint, the re-issue and every founding decide ask, in which a reservation is nobody and which is UNKNOWN rather than no while this node retains a record. A RE-ISSUE (`ReissueBootstrap`) is one gesture decided where its MINT lands, withdrawing every live code and ending a current take until its snapshot holds nothing else, because withdrawals read once and published one by one left a code minted between them live beside the new one. An invitation's basis is ALSO read before the first claim, read-only, with the record's snapshot staying the authority, and every bound a founding meets that is a property of the values — a grant this build can name — is checked before the take: met only at the last step, a code a day old was refused after the founder's address and login were claimed, and that reservation held their own address against the fresh code. AN ENROLMENT NEEDS TO BE FINDABLE rather than addressed: a PERSON needs an email (it is the interactive login key) and a MACHINE needs a login (it has no login page, so requiring an address would mean inventing one) — the flat address requirement meant a company could not declare a service account at all. A login or a seat is MOVED new-first (`Rename`, `Rebind`) and a login is never released on its own: releasing first left a refused rename with no login, which recorded a person as nobody and silently unbound a Tier A token from its seat. And EVERY PRINCIPAL NEEDS A LOGIN, a person too although their address finds them: it is the name `iam.ActorFor` records an unbound person under, and a person enrolled by address alone was written as `anonymous` beside every change and failed `Principal.Validate` on every request — so an administrator types one, the first operator types one, and an invitation's view PROPOSES one from the address (`iam.LoginFromAddress`) for the person to keep or change. THE BLINDER IS A SOURCE (`Blinds`), resolved at the write and never held from construction, because its key is minted by whichever node first needs it — held from boot it was nil on every fresh deployment and every address write was refused for a key nothing minted; and it is resolved BEFORE a person's key is minted, so a refusal leaves no key behind. The retention SWEEP'S PUBLISHER is here beside its applier (`sweeppublish.go`): the only clock read on the whole retention path, turned into POSITIONS once per tick, and a bucket is swept only when something is a day past its horizon, which bounds it at 64 records a day with no memory across a lease move. A sweep is the one record written at RECORD VERSION 2 — it also collects what version 1 kept for ever, redeemed invitations and codes and revoked or expired credentials, the last from the person's DOCUMENT as well as the rows, since the next credential change would republish them — while every other record stays at version 1: a record above a peer's decode ceiling is deferred there, so the ceiling is what a build READS and never what it writes, and a version-1 sweep on the log is still applied as version 1. A provider sign-in's REFRESH TOKEN is kept in the company's secret store (`refresh.go`) for the probe, and a session is OVER for it by every lever — ended, expired, a revocation epoch, the invalidation generation — while one this node has not applied — not arrived, or arrived in a record it RETAINED, which its checkpoint moved past without writing — is UNSEEN and kept; and whether a retained record is about somebody is asked of the WHOLE deferral index for their bucket in the rows' own snapshot (`deferredFor`), because the runner's earliest deferral carried no scope and was about one bucket, so the session table's and a token binding's "this node cannot vouch" answer never fired. AN IDENTITY PROVIDER SUBJECT IS A CLAIM TOO (`link.go`): a LINK is pinned by an invitation redeemed through the provider (`Enrolment.Link`, claimed after the login) or by an administrator (`Writer.Link`/`Unlink`), never by an address match, and what its apply writes is the person's oidc CREDENTIAL ROW — the claim is the ONLY writer of that row (a person's document is refused carrying one, and a content record's apply never deletes one), so the arbitration and what a sign-in resolves through are one fact; a person holds one link, the removal payload deliberately does not name it (its version is pinned for ever) and its apply deletes it with every other credential, and `iam_identity_linked`/`iam_identity_unlinked` carry the issuer and never the subject, because an event outlives the crypto-shred. AN INVITATION arbitrates on the ADDRESS, since the obvious subject — its own fresh id — would never contend and two administrators would each get a link that creates the same person; the row read covers `iam_invites` as well as `iam_people`, because the broker's create-at-zero settles only the CONCURRENT case and the second invitation an hour later publishes above the anchor the first left. EVERY WRITE ANSWERS ITS WHOLE `statelog.Result` and never a bare position, because a position cannot tell `applied` and `pending` from `unknown`: a zero one beside a nil error read as success, so a second factor whose spend may not have landed opened a session. REUSE DETECTION'S REVOCATION IS CONDITIONAL (`RevokePast`): it moves the epoch only while it is still at the replayed bearer's, and publishes nothing past it, because every ingress node the replay reaches asks and a per-node dedupe is bounded — an unconditional bump per asking ended the sessions the person had opened since the last. A SEQUENCE STOPS AT A STEP NOBODY CAN CONFIRM — an enrolment's claims, a rename's claim before its release — and answers `unknown` under the GESTURE's op id, which every step's id is derived from. The claim report (`claims.go`) reports a duplicate and an orphaned reservation and repairs neither. THE STAGE HAS ONE WRITER and every reader takes it from the row's COLUMN: the status apply moves the column and the document together, and a read-modify-write decide forms the next document from the column, because the document's copy went stale on the first suspension — sessions kept validating, and the next grant edit re-published `active` and reinstated the person. The applier's post-commit hook SIGNALS when a batch moved a seat's standing (a bind, an unbind, a stage, a removal) and `SeatHolders` answers who holds each seat at what stage in one snapshot, a removal read off its tombstone, which speaks for the BINDING it released and no other: the first bind of the seat after the removal stamps `seat_rebound_by`, so a successor's later unbind hands the seat to the chart rather than back to the leaver — which is what lets a suspension withdraw a seat's contact identities with no chart record. A SEAT IS BOUND BY ITS IDENTITY and never by an address (ADR-0020): the claim subject, `seat_id`, a removal's tombstone and the stamp on it name the handle the seat was CREATED under, which no rename moves and the chart never issues twice; a bind resolves whatever handle it was given through the chart inside its own snapshot, and every reader finds the seat by that identity — keyed on the handle typed at the time, a renamed seat was claimable twice, a removal under the old handle withheld the next holder for ever, and a stranger who took the freed handle inherited somebody else's suspension |
| `iam/oidc` | SIGNING IN THROUGH SOMEBODY ELSE'S IDENTITY PROVIDER. THE ROUND TRIP IS STATELESS and that is its whole shape: a login is two requests, and the obvious place to keep the state between them — a map on the node that started it — is why a fleet cannot serve logins, because a load balancer puts the two halves on whichever nodes it likes and the failure is intermittent, proportional to node count, and absent on the single-node deployment anybody tests on. So the state, the nonce and the PKCE verifier are SEALED into a cookie the browser carries, under the fleet keyring every node holds. SEALED AND NOT SIGNED, because one of the three is a secret: the verifier is what proves the party redeeming the code asked for it, so a readable one is PKCE defeated. FIVE VALIDATIONS on the ID token and each is invisible while it works — drop the issuer and any provider's token is accepted, drop the audience and every other application at that provider is a way in, drop the nonce and a token from another login replays, drop the expiry and one captured replays for ever, drop the algorithm pin and a key source returning BYTES turns a published symmetric key into a signing secret (the classic confusion case is refused by the key TYPE rather than the pin, which is why the pin has a case of its own). LINKING IS EXPLICIT and an email match is never a link: at most providers a user sets their own address, so matching on one binds whoever is most worth becoming — and there is no `auto_provision`, which is the same decision written as a field, which is how it ends up on by accident. The exchange is ONE FORM POST rather than a module: `client_secret_post` and not Basic, because RFC 6749 requires form-urlencoding the credentials in a Basic header and half the ecosystem gets that wrong in each direction. THE DEACTIVATION PROBE is the only way a central off-boarding reaches this engine — a provider tells nobody — and the only thing to ask with is the refresh token, so an `invalid_grant` ends the session as `idp_revoked` while EVERY OTHER ERROR IS UNKNOWN, because treating an unreachable provider as a deactivation signs the whole company out during somebody else's outage. A rotated refresh token is RECORDED, or the next probe presents one the provider retired and reads the refusal as an off-boarding. THE GROUPS CLAIM IS NAMED by `api.auth.oidc.groups_claim` — ONE name, never a list of aliases, because trying several is a guess about which claim carries authority — and an empty name reads none; a claim of any other shape than a list of names or one name refuses the sign-in naming it. What `group_grants` makes of them RIDES THE SESSION the sign-in opens and never the person, so the guard's effective set is (declared ∪ the session's carried) ∩ its ceiling at decision time and group authority lapses with the session that presented it: the callback used to merge the mapping into its sighting and drop it, so no mapping ever conferred anything. The mapping rule is `config.APIOIDC.GrantsFor` and nowhere else — this package carried a second copy nothing in production called. The FLIGHT CARRIES A REDEMPTION (`Flight.Invite`, `Flight.Login`) sealed beside the verifier, and `Start` takes a flight TEMPLATE and mints the state, nonce and verifier over whatever it holds, so a caller can never choose one. Which person a subject is pinned to is the estate's claim and NOT this package's — the `ErrSubjectClaimed` sentinel nothing returned is gone. A flight with a `MaxAge` is a STEP-UP: the request carries `prompt=login` and `max_age` (which makes `auth_time` REQUIRED in the answer), and `Flight.ProvedAt` refuses one the provider did not confirm inside the window rather than dating it |
| `iam/credential` | WHAT SOMEBODY PROVES THEMSELVES WITH: argon2id passwords, RFC 6238 second factors, recovery codes, machine tokens, and the throttle that keeps a sign-in endpoint from being a ROSTER. Everything stored is a VERIFIER and never a secret, because the identity estate is replicated, snapshotted, backed up and donated to joining peers. THE RULE IS NOT "hash secrets with argon2id", it is SPEND COST WHERE AN ATTACKER HAS A SHORTCUT — a password came from a space small enough to grind, so it gets 64 MiB and t=3; a recovery code and a machine token are `crypto/rand` values this engine minted, so there is no dictionary and SHA-256 is the whole of it, and a token is presented on every request a pipeline makes. `p=1` is the parameter most often set wrong: parallelism makes ONE verification finish sooner and does nothing to an attacker already running every core they own, while costing the ability to serve concurrent sign-ins at all. `VerifyCap` is DERIVED as max(1, NumCPU/p) because each verification holds 64 MiB — uncapped, an unauthenticated caller turns the endpoint into an out-of-memory kill. The PHC string is what makes a cost raise possible: the plaintext is not stored, so the only instant a stronger digest can be computed is the one where somebody presents their password. TWELVE CHARACTERS AND NO COMPOSITION RULES, with a blocklist of HUNDREDS rather than a published ten-thousand corpus — those are ranked by frequency and cluster at six to ten characters, so almost none is reachable behind the floor, and what is reachable is a few families caught by FOLDING a candidate (`password1234`, `P@ssw0rd1234` and `p4$$w0rd1234` are one entry). THE ENUMERATION ORACLE is what the file is shaped by and it takes three mechanisms: admission keyed on the SOURCE before the subject resolves (keyed on the subject, the 429 IS the roster), a fixed-cost HMAC decoy for a subject that does not exist (an argon2id decoy would be a memory DoS a stranger can trigger), and both arms padded to ONE deadline measured from ARRIVAL — the only one that equalises timing, and the residue under saturation is stated rather than hidden. TOTP is hand-written over `crypto/hmac`, `crypto/sha1` and `encoding/base32` and certified against the RFC's own vectors, because forty lines of arithmetic against a module with its own idea of tolerable drift is the dependency rule working; the LAST ACCEPTED STEP is stored, or the drift tolerance is a ninety-second window in which one shoulder-surfed code works repeatedly. Every secret comparison is constant-time and every candidate loop runs to the end, asserted BY AST because no behavioural test can see the difference. A MACHINE TOKEN is `cwl_pat_<id>_<position>_<secret>` and whether one is good is a TABLE over one snapshot's values (`CheckToken`), THREE-VALUED on the position it carries in the clear: a node below the mint answers UNKNOWN (503) and one that covers it and holds no row answers REFUSED (401) — the two are otherwise one observation. The verifier covers the prefix, the id and the secret and deliberately NOT the position, which is a hint about when to look and can only ever buy a forger a 503. What a valid token carries is its mint's grants cut, PER REQUEST, to what its owner STILL holds, so a demotion reaches every token they made; and it is minted at its owner's revocation epoch AND the company's session generation and refused once either moves, because a restore brings a revoked token back exactly as it brings a revoked session back |
| `iam/session` | THE SIGNED COOKIE a browser holds, and the two THREE-VALUED TABLES that resolve it to a person and a seat. SIGNATURE-STATELESS AND ROW-STATEFUL, because neither half works alone: a stateless cookie cannot be revoked before it expires, so off-boarding would be impossible, and an opaque id into a table costs a read for every forged cookie and has nothing to say until the row it names has been applied on the node the request reached. THERE IS NO VALIDATION CACHE ANYWHERE, stated in the package doc — the lookup is a local read of a replicated row and a map lookup on a pinned chart view, so a cache would be a second idea of who is signed in, with its own staleness, in front of a read that is already local. The rule the whole package is shaped by is 503 AND NEVER 401 ON A NODE THAT IS BEHIND: a browser reads 401 as "sign in again" and discards the cookie, so one stalled applier answering 401 logs everybody on that node out and stampedes the IdP — and the grace exists only for the arm a lagging node can honestly serve (reads of a session it has not yet seen), ending at `statelog.StallGrace`, so a node serving stale identity is by definition one already alarmed. What makes those three answers REACHABLE is the START POSITION in the bearer: an absent row on a node that covers it is a session that ended, and on a node below it is one that has not arrived, and the two are otherwise one observation with two opposite answers. THE KEYRING IS REQUIRED — `runtoken`'s per-process random fallback is REFUSED by name, because every ingress node would derive a different key and a browser would be signed in on whichever node its request reached — and the key TAG is `runtoken`'s own, so a rotation is add, restart, flip, restart, drop, and dropping a key early is what ends live sessions. ROTATION IS DERIVED (the session's age in `rotate_after` units, the age read out of the lineage's own uuid7, and the index travels as an integer because the lineage beside it already discloses the millisecond the session began) so an hour of use writes nothing, and `rotate.go` argues at length what an index CAN and CANNOT prove: a LAGGING index is an idle session and a captured cookie in identical bytes, so it is served and re-issued rather than called theft — the rule that called it theft signed an honest person out of everything after lunch and made the twelve-hour idle deadline unreachable. What survives as positive evidence is an index AHEAD of the clock past the overlap. A WRITE on a node below the bearer's start position WAITS for it rather than answering 503 — at most `auth.SessionCatchUp`, the publisher's own resolve budget, over the identity reader's `AwaitApplied` — because a sign-in answers before any node applies the session it opened (`NoWait`), so the request that reads the bearer next owes its position the wait the sign-in skipped: refused outright, every client that signs in and then acts met a 503 on every node, and `crewlet iam token -login` failed on every real run. A DEADLINE IS DECIDED BEFORE ANY ROW IS READ, so a cookie a record ended a day earlier lands on the same `ended`-by-deadline row as one that was live until then; `Standing` is the read the refusal skipped — the rows half of the table, shared with `Validate` — which the guard asks before it announces a deadline ending, so an ending is said once, by what caused it. `session.Idle` is a CONSTANT and not config, because it rides in every bearer and two nodes disagreeing about it would hand one browser two expiries |
| `iam/authevents` | WHERE THIS NODE'S AUTHENTICATION FACTS BECOME EVENTS — the one trail the sign-in surface, the request guard, the identity directory, the identity writer and every state-log applier hand what they saw to, built ONCE PER PROCESS by the engine because its dedupe is the node's: a token's first use in an hour is one row whichever surface saw it, and a replayed cookie ends its sessions once however many requests carry it. TWO DOORS and they are the whole design: a fact a VERIFIED credential or the engine authored is published as it happens (`Emit`, or `EmitOnce` for a fact worth one row per window, which REPORTS whether it published so an action that must happen once can hang on the same decision), while a FAILED ATTEMPT publishes NOTHING — it bumps `crewlet.auth.attempts.failed` and folds into a tally keyed on (client, minute), and the engine's own loop publishes one `iam_login_failures` per client once the minute has CLOSED, because internal/events admits no type whose rate an unauthenticated caller authors and a row per attempt was 6.9 million a day from one host. A tally NEVER HOLDS WHAT WAS PRESENTED: the distinct-name count is taken over 8-byte HMAC digests under a key the process generated and never wrote, discarded with the minute — the presented value is where a password typed into the login box lands, and an unsalted hash of it reverses against the company's own roster in one pass. Every size an attacker would otherwise choose is a constant with its reason (clients per minute, subjects counted, people named, folded clients, and the once-keys of EACH CLASS), and a count at its cap SATURATES so a row reads "at least". The once-per-window dedupe keeps ONE BOUNDED SET PER CLASS of fact (`OnceClass`) and evicts only within it — expired first, then the oldest claim — because one shared set evicting whatever expired soonest let the deadline endings a process remembers for ever push out a replay's key, and the replayed cookie then published and revoked on every presentation; `Claim` is the same decision for a caller that must read before it knows the fact, handing the key back when the read cannot say. Publishes are DETACHED from the request and bounded, because the gesture already happened and a client hanging up must not erase its row; the loop's final flush on stop is what keeps the minute a restart interrupted |
| `knowledge` | The backend-neutral seam the turn-start knowledge prefetch and the `search_knowledge` builtin both read through. Exactly ONE backend per company, because two would make "what do we already know about this" depend on which searcher was asked. A caller passes a seat, plain text and ancestor exclusions — never a CQL fragment, a space key or a project list — and search is BEST EFFORT: every failure path is an empty result, so a turn never dies because a wiki was slow |
| `providers/llm` | The model contract. It does NOT retry (rotation belongs to the credential pool, fallback to the chain) and does NOT decide what a failure means beyond a coarse kind. A `cli-agent` entry has TWO modes: `text` drives the CLI as a model behind the engine's own tool loop, `agent` hands the executor to the CLI's own loop as a detached coding run with the seat's tools bridged in — and only the executor branches, so every other phase stays a text call on the same entry |
| `providers/credential` | The key pool a provider rotates through: each call leases a key, a rate-limit or auth failure benches it for a TTL, and an exhausted pool hands the seat's fallback chain on to another model. Cooldowns are measured on a MONOTONIC clock, so an NTP correction or a VM migration neither revives a benched key early nor strands a live one; selection is least-in-flight, declaration order breaking the tie |
| `providers/embeddings` | The vector backend behind diary and episode recall |
| `sandbox` | Code work as a suspended Execute phase. A coding run is DETACHED: the tool starts it, the loop suspends, and the engine resumes that same loop — minutes later, possibly after a restart, possibly on another node. Nothing parks a goroutine on a running job. Two real backends behind one contract — a remote VM per run (E2B, cloud or self-hosted) and the engine host itself — held as a CATALOGUE keyed on `Placement`, because `providers.sandbox` configures which boxes exist and each seat picks its cell with `role.sandbox.run_in`. A run's placement is on its ROW: the turn that collects it may be another process on another node, and reconnecting to a remote box through the local backend reports a box that vanished. The closed set `run_in` accepts is asserted against the switch that builds them |
| `hostbox` | The primitives for running somebody else's process in a directory on the engine host. Two subsystems must do it IDENTICALLY — the subscription CLI backend, which drives a coding CLI per seat with its own HOME, and the local sandbox provider — and each primitive is a guard, so a second implementation is not drift but a hole |
| `procgroup` | Addressing a child process TREE with one signal. Every long-running child starts behind something that forks — `uvx`, `npx`, a Node runtime, a shell — so signalling the process Go started reaches the launcher and nothing beneath it. Its own process group makes the tree one negative pid, and it lives here rather than beside either caller because two copies of a platform-conditional signal helper is how one stops matching the other |
| `learning` | What a seat remembers. Everything here is BEST EFFORT by design: a failed write is logged, a failed read answers empty. `memsync` is the exception that makes the rest true across a fleet: a seat's memory is written to the NODE's store, and placement moves seats — so every memory row rides a COMPACTED CHANGELOG (one subject per row, one message retained per subject) and a node hydrates a seat's rows before its mailbox attaches. Deletes deliberately do not travel: the lifecycle re-converges, and a tombstone protocol would be a second thing to keep correct forever. It DECLARES the two `statelog` contracts it rests on rather than implementing their shapes privately: `ReplayCompacted`, which every rule in the hydration follows from, and STREAM IDENTITY — because a recreated changelog is EMPTY, so hydrating from one reports success and gives every seat on the node a blank diary, and a compacted stream has no checkpoint and no generation to answer a recreation with, so its only honest move is to refuse the seat |
| `schedule` | Role- and unit-scoped cron work, with at-most-once delivery, missed-tick catchup and a wall-clock cap |

*The edges:*

| Package | What it is for |
|---|---|
| `api` | REST + the dashboard. Served INSIDE the engine's process and nowhere else: `crewlet run` builds it over the engine's own store, broker and coordination plane, and `New` refuses a missing one by name, because a narrower answer built around a nil (an omitted health field, a 503, an absent route) reads as deliberate and hides the wiring mistake. `NodeRuntime` is the seam it asks the engine through. EVERY REQUEST LEAVES THE GUARD CARRYING A PRINCIPAL — resolved, anonymous, or unknown — and the EXEMPTION LIST is the whole of what is not guarded (probes, webhooks on their own HMAC, the per-run token paths, the dashboard shell). There is no list of what IS: guarded is what a route is, so the always-guarded prefix list is gone and each surface states its authority as a GRANT where it is enforced — which is why every surface `New` mounts, `/config`, `/secrets` and `/setup` included, is a `guardedMounter` whose routes go through `authz.Router`, and why the deployment's own controls (`/budgets/reset`, `/backup`, every `/work/retention*` route) are mounted there on `fleet:operate`. None of them decided a grant before: the guard answers only whether somebody resolved, and a surface that relied on that opened itself to every signed-in reader the day sessions arrived. The query registry is where both transports meet that: one question, one declared grant, and a REST route and a socket frame decided by the same check, so no question is reachable by choosing a channel — and a registration with no grant PANICS at wiring time, because a question answering any credential at all is an ungated surface that ships looking deliberate. Its refusal is three-valued for `iam.From`'s reason: an identity estate this node cannot read is `ErrUnavailable` with a Retry-After, a context no resolver touched is a plain FAULT (a route nobody wired through the guard never clears by waiting), and only a credential positively checked and refused is unauthorized. THE VIEWER IS THE CALLER: the principal travels in the context on both transports and no operator id rides beside it, and `viewer=` is gone from `work_items` and `work_views` — it had exactly one honest value, and any other was a free choice of whose pins, personal views and queue to read. A question that names somebody else's record resolves the name first — `iam.OwnerOf` through `Sources.Holders`, which `New` REQUIRES, so a login is its holder's record and never read literally, and only after the question's own verb has been decided on the unresolved login (`Sources.mayLook`), so a caller who leads nobody is refused before the directory is asked — and is decided by the authority table on what it resolved to, under the QUESTION'S OWN VERB — a person's queue by the owner-or-lead class through `Sources.Chart`, whose UNKNOWN answers 503 rather than a refusal, and a seat's trail (`conversations`, `agent_memory`) by `audit.seat.read`, the audit grant whoever's seat it is — and a refusal carries the rule's `reason` and the `grants` that would have admitted the caller rather than a sentence written beside the rule — and which `New` REQUIRES, because the production wiring once left it out and every lead's read of a report's record was a 503 no retry could clear; it used to be "any operator credential", which made every Tier A token a reader of every seat's inbox. A token acts AS A SEAT only when the identity directory holds an ACTIVE MACHINE row under its login bound to one, and that seat is resolved through the SAME chart seam and `session.ResolveSeat` a signed-in person's is (`auth.SeatBindings`) — followed through a rename, `403 seat_unavailable` once removed or an agent's, `503` where this node cannot say — because the lookup it replaced returned the row's raw handle, so a token went on acting as a removed seat, a renamed one or whoever the chart later gave that handle to; an UNBOUND answer is served even by a stalled node, which is what keeps break-glass reachable — `contact.crewlet_operator_id` is gone with the transport it named, because a contact block says how to reach a person and not which credential they hold. A CROSS-SITE WRITE is refused on its `Origin` before the handler runs, landing BEFORE any cookie-minting route exists rather than after, because the other order leaves a window in which every write on this surface is forgeable. THREE CREDENTIAL SHAPES meet at that one frame — a Tier A bearer composed from the configuration alone, a SESSION COOKIE resolved through `internal/iam/session`'s two tables, and a MACHINE TOKEN (`auth.Tokens`, `Options.Tokens`) resolved from one read of the directory to its OWNER, carrying what it was minted with that the owner still holds, and naming itself on the principal's `Via` so every record, single-column trail (`created_by`, `set_by`) and audit event it causes carries `pat:<id>` beside the owner — which is also where `auth.PresentedToken` reads it, one carrier rather than a context value beside the principal — the two bearers told apart by the value's SHAPE, which picks the check and never admits anything; `crewlet run` builds the token arm on EVERY node, a node running no identity domain included, so a token minted elsewhere is a 503 there rather than a 401 teaching a pipeline its credential is broken — and the HEADER WINS when both arrive, because a browser sends its cookie whether or not the caller meant to and an `Authorization` header is only ever there because somebody put it there, so a request presenting a WRONG one stays anonymous rather than being upgraded by whatever was in the jar. The ceiling clamps a person exactly as it clamps a token. And a person — or a bound token — whose SEAT the chart no longer holds is the one answer that is neither a resolution nor a refusal the routes make: they are RESOLVED (this node knows exactly who they are) and the guard itself writes `403 seat_unavailable` naming the seat — 401 would loop a browser through the sign-in page for ever — everywhere but `/auth/`, whose subject is a person's own credential rather than the seat, or an offboarded person would hold a live cookie with no way to sign out. `Options.Inbox` is REQUIRED: it is the engine's `SetOnInboxMoved`, which the app turns into `inbox_changed` frames, and every node that serves the API applies the tracker's log, so a node that heard nothing would be a wiring mistake rather than a narrower node. `/health` says whether the company has its FIRST PERSON (`identity`: `ready`, `unclaimed`, or `unknown` where the identity estate cannot be read — never folded into `unclaimed`) and, while unclaimed, THIS node's founder code path, through the optional `Founding` seam — because it is the one surface an install with nobody in it can reach; absent on a node that serves no sign-in surface, whose empty copy of the estate would read as "nobody", and it never moves `status` |
| `api/chartapi` | `/chart` and `/company/export`: the company's ORG CHART over HTTP, which had nowhere to live while the chart was part of the stored revision — `/config` refuses a body carrying one BY NAME and, until this surface, pointed at nothing a person could use. What it adds to the domain's own rules is the READER's posture and the WRITER's question, and both come from one line the domain already draws: half of every object is OPAQUE to internal/chart and travels on the row's `document`, which is a seat's model chain, its credentials, its sandbox cell and its `mcp_env` — exec.Command on every engine host. So a read is STRIPPED by default and opt-in (`?runtime=true`) behind the grant that reads the company document, stripping rather than refusing because the rows were readable either way, and SAYING SO in the answer, since a company with no runtime renders exactly like a caller silently stripped. And a write's authority is picked by its PAYLOAD rather than by its route: the public half is the object's own lead, anything under `runtime` is the company's grant, and the route asks the second question only once the body is in hand — the pattern cannot see which half arrived. Structure is neither, and goes through ONE BATCH that the domain serialises against every other structural write. The three write outcomes stay three: `applied` is 200 because the next read HERE sees it, `pending` is 202 with the position to read at, and `unknown` is 503 carrying the op id, because the only safe retry is the SAME one. Its CONTINUOUS REPORT reads the IDENTITY DIRECTORY for one of its findings, through a nil-able seam whose absence is the THIRD VALUE: a node running no identity domain has a legitimately empty copy of that estate, so the unheld arm is SKIPPED rather than answered — a report that cannot ask does not guess, and one that read the emptiness would report every human seat in the company. `seat_unheld` (nobody in the directory is bound to it) and `seat_unreachable` (no contact identity) are separate kinds, because they are independent facts with different remedies and folded together whichever remedy somebody tried first would appear not to work — and `seat_unreachable` is a WARNING and the only place that state is said, because validation ADMITS a human seat with no contact: a person who works only through the dashboard has no chat account to declare, and refusing the seat refused them. It also holds the CONTINUOUS REPORT — one evaluation over the chart this node has and the settings epoch it applied, served whole at `/chart/check` and as counts on `/health` — for the findings the split made reachable and nothing can refuse at a write: the two halves are written by different people at different times, so a revision that strands every seat on a removed provider is a valid revision |
| `api/webhooks` | The seven inbound vendor sources — GitHub, GitLab, Jira, Confluence, Slack, Datadog and Atlassian's Forge relay — plus Slack's OAuth landing and the GitHub App return. Six verify a signature over the body; Datadog and Confluence Cloud cannot, because their providers sign nothing, so a shared token IS the authentication there and it has a length floor for that reason (`secrets.CheckSharedToken`). A route whose secret is unset has nothing to verify with and answers 503 — it does not accept the delivery |
| `runtoken` | The signed, self-describing credential a PER-RUN endpoint carries in its own URL path: the OTLP receiver's and the MCP bridge's. Minting and verifying happen on DIFFERENT NODES of a fleet, so a token has to carry its own subject and expiry rather than be a key into a map. One implementation because it was about to be two, and the two would have had to agree about five decisions nothing compares; the DOMAIN in the key derivation is what stops one endpoint's token validating at the other |
| `api/mcpbridge` | One running seat's tool surface, served to a coding agent over MCP. The dispatch is [tools.Surface] itself — never a second idea of what is granted, what a guard refuses, or what gets recorded. What it adds is the four things that ARE new: a credential a box may hold, a durable call log (a bridged run outlives its turn and its process, so the reviewer's evidence cannot live in memory), a session lifetime, so a box that outlived its run keeps no working key, and an IDENTITY — the per-run token IS the credential, so every call acts as the run's SEAT, set by the bridge per call OVER whatever the request carried: `/mcp/` is exempt from the guard, which therefore resolves every box as anonymous (or, on a laptop, as the `-dev-principal`), the SDK keeps the opening request's context for the whole session, and the turn's own principal defers to any answer already there — so every gated builtin refused the seat's own call as needing a credential. It is overridden HERE and nowhere else, and a session whose surface is bound to no seat is refused at `Open` |
| `whsec` | The Standard Webhooks signing-secret format, and the one place that decides what a valid one is: `whsec_` plus padded base64 over a 32-byte key, with the DECODED bytes as the HMAC key. Three places need the rule and none may disagree — written twice it drifted, and a `${VAR}` holding a 16-byte key was refused as a literal and silently accepted as a reference |
| `api/authapi` | `/auth`: how a person BECOMES a principal. Twelve routes over machinery that already existed — argon2id, TOTP, recovery codes, the throttle, the OIDC round trip, the session signer, the domain's writer — and what it owns is the HTTP shape of the sequence and the REFUSALS. A SIGN-IN SURFACE MUST NOT BE A ROSTER: every failed sign-in answers one code at one wall-clock deadline measured from ARRIVAL, whatever went wrong, and a miss pays the same argon2 verify a hit does through the decoy, because otherwise the ABSENCE of that cost is the answer. Neither half works alone — a distinguishing code makes the pad pointless and a distinguishing delay makes the code pointless. The named exceptions each disclose nothing the caller did not have: the throttle is keyed on the SOURCE, a second-factor prompt is reached only past the first, an invitation's refusal is read by somebody holding the link, and a provider subject two people hold is `409 subject_conflict` answered only past the provider, naming neither holder and carrying no Retry-After, because waiting never clears it. FIVE ROUTES ARE EXEMPT FROM THE GUARD and the list is EXACT rather than a `/auth/` prefix, because the same surface ends sessions and enrols second factors — the exemption and the registration are one set of constants and a gate walks the mux in both directions. A GET on an invitation RENDERS and never spends (a mail client prefetching, a scanner, a preview card are all GETs). A REDEMPTION CAN BE RETRIED until it lands: the person it creates is DERIVED from the invitation (`iamdomain.InvitedPersonID`) and the founder from the bootstrap code and its mint instant, because an enrolment is a sequence and a person minted per request left the address its first attempt claimed held for an id no retry named — a redeemer told their login was taken could never choose another; what keeps a link single-use is the link (redeemed, expired, or an address somebody is ENROLLED under answer 410), and a bootstrap code counts only while its mint record is LIVE on the log, or TAKEN by its own founding that has not finished — checked by its digest ON THE LOG, so any node redeems any node's code (the route compared the serving node's own file and refused every other node's code as a wrong one), and one that aged out after its 24 hours, was withdrawn, or never got its hash published answers `410 bootstrap_code_stale` naming `crewlet iam bootstrap-code` rather than `bootstrap_closed`, which told a founder whose Friday code had run out that their company had started. A CODE SPELLS ITS OWN EXPIRY (`cwl_boot_<unix seconds>_<64 hex>`, the digest over the whole value), because its row is swept a week after it stops working and a swept code was answered as a WRONG one on every node but the one still holding its file; one past its own expiry is `410` everywhere, which says nothing a stranger could not read off the string they sent, while one whose expiry is to come is decided by the log alone. EXACTLY ONE FOUNDER LANDS because the domain takes the exemption on one subject, so a live code presented while another founding is part-way through is `409 bootstrap_in_progress` carrying `until`, and like `bootstrap_closed` it is not a failed attempt. At boot a node KEEPS a file whose code still works (live, or taken by a founding it finishes) and REPLACES a dead one (it used to advertise any file it found), withdraws nobody else's code, and removes its file once the company has started; the re-issue hands the whole gesture to the domain rather than reading the outstanding codes and withdrawing them one by one, which left a code minted in between live beside the new one; the route, `/auth/config`'s flag, the boot offer and the re-issue ask ONE gate. `Service.Founding` is what `/health` reports of the company's first person — `AnyPerson`'s answer, whose unreadable arm is an error and never "nobody", and while nobody is in, THIS node's code file only while the log honours its code — read WITHOUT the code lock, because a probe must not wait behind a mint that waits on the log. Ending a NAMED session compares the owner this node holds against the caller the guard resolved, since a lineage is not a secret; the logout's own lineage comes from a bearer whose SIGNATURE verified, and ONLY A SESSION THE ROWS STILL HOLD IS ENDED: a sign-out, named or not, of a session already over (a deadline, a revocation, a record that ended it) clears the cookie and writes and announces nothing, because read under the signature alone every cookie ever signed wrote a close and another `iam_session_ended` per post. A provider sign-in hands its refresh token to CUSTODY, required wherever a provider is, and one whose token cannot be kept is REFUSED with 503 and its session closed: admitted, it is the one session the deactivation probe can never ask about. AN INVITATION CAN BE REDEEMED THROUGH THE PROVIDER (`invite_oidc.go`), which is the only way a subject is linked without an administrator: `/auth/oidc/start?invite=` refuses a spent link and a login outside the grammar BEFORE the round trip and seals the invitation into the flight, and the callback redeems THAT invitation — never one the way back names — as the password redemption does (the derived person, the same op id, the invitation as the authority) with the provider's subject as an `Enrolment.Link` and no password. A PROVIDER'S PROOF IS DATED BY THE PROVIDER: a provider answers from its own session, so a provider sign-in's session is proved at the ID token's `auth_time` (`oidc.Flight.ProvedAt`) and never at the instant the token arrived — stamping it "now" handed every step-up window to a week-old provider session — and one asserting none proved nothing datable. Somebody who signs in only through the provider holds no password for `POST /auth/step-up`, so `/auth/oidc/start?step_up=true` is THEIR step-up: `prompt=login` and `max_age` asked, accepted only on an `auth_time` inside the window, replacing the session it was made from exactly as the password step-up does. An OIDC `return_to` is refused unless it is a path on this deployment, because an open redirect is the ordinary way a sign-in flow leaks a credential. A provider account linked to somebody else, an invitation begun with another account, and a subject a restore left on two people are `409 subject_conflict`, counted as a failed attempt and never a 503, because no retry clears them. The subject resolves through the LINK table and only a live link, never the person row's address blind — the two are different classes inside one MAC, so the address lookup matched nobody and every provider sign-in was refused. A SECOND FACTOR IS SPENT BY THE SIGN-IN IT COMPLETES, decided in the snapshot that checked it: an app code records its step and a recovery code is removed, either accepted wherever the person holds it, and a spend this node cannot record is a 503 rather than a session on a code that would still work — both were missing, so a recovery code could never be used by anybody holding an app and neither was ever spent. Every refusal reaches `iam/authevents` as a FAILURE rather than an event, and every success as the event it is. A STEP-UP REPLACES the session it was made from rather than adding one: the old session is ENDED FIRST, so a close that does not land fails the gesture instead of leaving two live sessions, and the new one records its own proof instant (what `reauth_at` is composed from) while keeping the old one's absolute deadline and provider-carried grants, because it confirms a sign-in rather than repeating one. AN UNKNOWN OUTCOME IS NEVER BUILT ON OR ANNOUNCED: every write here reads the writer's whole result, and one nobody can confirm — a second factor's spend, a session's start, a close, a revocation, a factor stored — answers `503` with its `op_id` (`httpjson.UnavailableWith`) and opens no session, mints no cookie, shows no codes and emits nothing. EVERY 503 HERE CARRIES A RETRY-AFTER and nothing in the package spells the status itself — `httpjson.Unavailable` with `auth.RetryIdentitySeconds`, the one number, is the only writer, and a gate walks the source for `http.StatusServiceUnavailable` — while a fault waiting cannot clear (a provider block that does not validate) is a 500 rather than a 503 telling a client to retry for ever. A MACHINE TOKEN MANAGES NO PROOF: it acts as its owner, a PERSON, stepped up by construction, so the kind check and the step-up clock both passed it straight into TOTP enrolment and recovery-code regeneration — a leaked token could have read back new recovery codes. The step-up, the enrolment and the regeneration refuse on what the request PRESENTED (`auth.PresentedToken`), the one fact the principal cannot carry. `POST /auth/token` EXCHANGES A PRESENTED TIER A BEARER for a one-hour session whose subject is the token's LOGIN rather than a person, and the guard re-composes it from the ENTRY this node holds on every request through the same function the bearer takes — grants cut to the ceiling, the directory's seat binding, stepped up by construction — so removing the entry ends it wherever the node stands on the log; it was minted for the token's derived id, which no row holds, so it answered 401 the moment its start record applied and a bound token could not exchange at all. |
| `api/iamapi` | `/iam`: the company's IDENTITY DIRECTORY over HTTP — sixteen routes over the fifth state-log domain. ALWAYS GUARDED INCLUDING READS, for the reason `/secrets` guards its listing: a map of who can reach a company and how is worth as much to an attacker as the grants themselves. THREE PARTIES ASK and `internal/authz` holds that as two CLASSES rather than a condition here, so a REST route and a socket frame cannot answer it differently — the person a row is ABOUT, whoever holds `people:manage`, and, for a read only, whoever holds `audit:read`, because "who can reach this company" is the audit question and an auditor who could change what they audit would make the read grant a write grant. THE OBJECT A ROUTE NAMES IS A PERSON ID, never a login, and this is the one place in the authority table where that is true: the estate keys on an id precisely because a person changes their login, so a self check against a mutable name opens somebody else's row the day they swap. EDITING YOUR OWN ROW IS NOT A SELF GESTURE — the directory WRITE has no self path at all, closed twice over (the mount names no object AND the verb's class ignores one), because changing your own grants is the escalation this estate exists to close; the record layer refuses it a third time, since a caller may not confer a grant they do not hold on anybody. A WRITE WAITS for this node's own apply, so `200` means the administrator's next read HERE sees it, `202` carries the position when this node is behind, and `503` carries the op id because the only safe retry is the SAME one. It used to promise that and answer 200 for all three, because the writer answered a bare position: now a sequence answers its WEAKEST step (an unknown one ends it there), nothing is announced, linked or minted on an unknown, and EVERY 503 carries the one `auth.RetryIdentitySeconds` hint — held by the same AST gate authapi has, since three sites spelled a bare 503 and a fourth private copy of the number sat beside them. NOTHING OPENS A SEALED VALUE IT WAS NOT ASKED TO: a name and an address cost a fleet-secret read EACH — deliberately uncached, since a cache would go on opening values for somebody whose key a removal just destroyed — so the page size is what bounds that fan-out, and a REMOVED person (key destroyed, permanent) and a SEALED one (a keyring this deployment does not have) are different states rather than one decrypt failure. The invitation link and the minted token are shown ONCE and nothing reads either back. A MINT'S OWNER IS THE ROUTE'S — `?person=`, or the caller — and never a body field: the handler used to mint for a body `person` the authority table never saw, so anybody could mint a token on anybody's account by asking about themselves. And the MINTER is the resolved principal's id, which the domain holds against a person owner: a person mints their own from their own session with no `?person=` (`crewlet iam token -login` signs in for exactly that; the dashboard has no mint screen), and an administrator's `?person=` names a service account. A mint ignores `Idempotency-Key`, because a replay would answer the first attempt's record beside this attempt's secret, a token that verifies against nothing. A request that PRESENTED a machine token may not mint (one minted from a token renews for ever with nobody present) and revokes only machine tokens — never its owner's password or second factor; the bootstrap code answers a FILE PATH and the NODE it is on, and a re-issue is ONE domain gesture decided where its mint lands (`iamdomain.Writer.ReissueBootstrap`) — every live code withdrawn and a founding in progress ended first, so where it lands it is the one code that works, and a re-issue that kept finding new codes is `409 stale` having minted nothing, never `bad_params`, which tells a client it can never succeed — and it is refused by the redemption's own gate rather than by an administrator check of its own, which minted a code for a company whose only person was suspended that the redemption then refused — `409 bootstrap_closed` once the company has started, while a deployment whose `api.auth.bootstrap` is closed hands in NO seam and the route is ABSENT (`404`), because never bootstrapping this way is a different fact from having started and the reference promised the 409 for both. `oidc_subject` on `PATCH /iam/people/{id}` is the ADMINISTRATOR'S LINK: blinded under the issuer the engine's own provider carries (`Options.Issuer`) so it is the blind every sign-in resolves under, refused before the first record where there is no provider or the value is not a subject, a MOVE naming the link the person holds now, `""` to unlink; a link's credential id revoked through `DELETE /iam/credentials/{id}` UNLINKS, because a link is its claim's row and a credential-set rewrite would have answered "nothing changed" while it went on working. `GET /iam/check` names a DUPLICATED claim with every holder, an ORPHANED reservation, a removed person whose KEY STILL LIVES and a key NOBODY owns past the hour — the last only on a node that has applied the whole log, counting in `keys_unchecked` what it could not judge; an unreadable claim report is 503 rather than a clean bill, because it is the only place a duplicate identity is ever named |
| `api/workapi` | `/work/items`, `/work/projects`, `/work/views`, `/work/catalogue`, `/work/people` and `/pages`: the HUMAN WRITE SURFACE over the tracker and the knowledge base. ADAPTERS OVER THE TOOLS, NOT HANDLERS OVER THE WRITER — every route that has a tool behind it builds the same catalogue a seat and the operator's assistant are served from, per request, and calls the tool with its own arguments; the obvious shape, a handler per route calling `tracker.Writer`, would have been the THIRD copy of "file an item" and the first one a person uses, drifting on which field is trimmed, which default applies and what a refusal says. A FEW VERBS HAVE NO TOOL because no seat is given them — a rank move, rewriting one's own remark, a work item's purge, the knowledge base's rename and its trash, restore, purge and comment take-down — and those call the writer themselves after deciding through the SAME table. AUTHORITY IS DECIDED ONCE: where the object is in the PATH the route decides the verb, and where it needs a STORED ROW (an item's project, a page's container, a comment's author) the route admits a reader and the verb is decided once the row is read. A refusal is worded by `builtin.Refusal` over `builtin.DecisionError` — the tools' own two functions — so "you may not" is the same bytes in a turn, in the assistant and here, which a test holds across all three. The status is read from the tool result's CAUSE and never from its sentence (403 refused, 409 stale, 422 the domain's own refusal, 503 undecidable or unknown), because reading a status out of prose written for a model would make every refusal's wording an API. An `Idempotency-Key` becomes the operation SEED of every record the request writes, so a retry after `unknown` is the same operation. A body argument the tool's own SCHEMA does not declare is refused `400` by name on every tool-backed route, because the tool drops what it does not read: a client still sending `save_work_view`'s removed `owner` had a personal view saved as a shared tab under a 200 — and the refusal is the TOOLS' GATE's (`builtin.ErrUndeclaredArgument`), never a check of this surface's own, which is what it was while a seat's turn and the operator's assistant dropped the same argument. Somebody's inbox, pins or priorities by path go through `builtin.MarkInboxFor` / `SetPinsFor` / `set_priorities`, which resolve the path's name to its RECORD through `iam.OwnerOf` — a login to its holder's, a seat EXACTLY — and decide the verb on that; the route admits only on the verb for the caller's OWN record, because the path's spelling is not the record (decided on a bound person's login it admitted an administrator to a record nothing of theirs reads, and refused a lead their own report) — and the tools are deliberately NOT widened to take a handle: a model that could name whose inbox to mark could mark anybody's |
| `api/setupapi` | `/setup`: connecting an integration from the dashboard rather than a shell. It performs no write of its own — a credential goes through the store `/secrets` serves and a pointer through the merge, validation and compare-and-set `PATCH /config` performs — so there is one write path onto the company document and one onto the sealed store. Every route takes a grant, reads included (`config:read`), because the list of which credentials a company has NOT configured is a map of what to attack; CONNECTING IS `config:write` AND `secrets:write`, the second asked through the same guard wherever a credential is sealed — a submission carrying one (which only the body can say), a provisioning pass, a GitHub App — because a caller who could not write a credential through `/secrets` must not be able to write one here |
| `api/opsmcp` | The company's own tracker and knowledge base, served to the OPERATOR's own AI assistant over MCP — because the premise of running this engine is that an AI manages your company, and the person doing that is usually working through one too. The tools are `builtin`'s, not a second implementation: ONE field on the deps decides who a write is attributed to, so a schema, a default and the wording of a refusal are written once. Every tool is decided by the SAME authorizer a seat's registry is built with, so the relations a caller reaches come from the seat the identity directory binds them to. A write carries the name `builtin.PrincipalActor` makes of the request's principal — a person bound to a seat writes AS the seat, kind `human`, which is what lets the tracker leave them out of the wake their own change sends; a credential nobody is bound through writes under its WHOLE login, colon and all, kind `operator` — and there is deliberately no way for the caller to name a seat to act as, because a tracker whose author field is chosen by the writer is not an audit trail. Its own always-guarded prefix rather than `/mcp/`, which is exempt WHOLESALE so a sandbox box holding no API token can reach its seat's tools; a writable company surface there would have sat behind no credential at all. SERVED STATELESS, so every call is decided by the credential of the request CARRYING it: the SDK's stateful session serves each later request through the context of the request that OPENED it, so a withdrawn grant kept working for as long as the assistant stayed connected and any other credential echoing the `Mcp-Session-Id` acted as the opener — a session id is a handle, not a proof. Nothing here needs the state: the catalogue is fixed at construction and no tool asks the client anything back |
| `api/secretsapi` | `/secrets`: the company's credentials, written through a running node because the coordination broker is inside its process and listens on no socket. EVERY ROUTE TAKES A GRANT through `authz.Router` — the listing `config:read`, every write `secrets:write` — and the ONE route that returns a value needs an explicit `?reveal=true`, asks `secrets:read` on top BEFORE the store is read, and logs the access. The routes used to decide nothing: the guard answers only whether somebody resolved, which meant "the operator" while a Tier A token was the only credential and meant "anybody" once a person could sign in holding `state:read`. And THE ENGINE'S OWN KEYS ARE UNREACHABLE HERE: every route that takes a name answers a reserved one `403 reserved_name` before anything else, whatever the caller holds — and every refusal here is the engine's envelope, its codes in `httpjson`'s vocabulary with a sentence each, where the surface wrote bare `{"error": …}` maps no screen had words for — and the listing COUNTS them per keyring key (`engine_keys`) without naming any — a reveal of a person's key was a copy taken ahead of the removal that shreds it, and a DELETE a removal nobody recorded |
| `api/stream` | The `/ws/stream` socket: pushes, plus a request/response query channel that is a thin adapter over the SAME function each REST route calls. WHO IS ASKING IS THE GUARD'S ANSWER, read from the context: the handler used to re-read the credential through the Tier A arm alone, so a browser signed in with a session cookie was refused here while every REST route beside it served them — and no identity travels beside the query as an argument, because two identities on one call are two chances to answer about different people. The in-flight query budget is PER PRINCIPAL rather than per socket — keyed on the principal's ID, never its login, because a login is a name somebody renames — and while a login was optional every person enrolled by address alone shared one budget under the empty one — held by the service because it outlives any one connection: per socket, nothing tied a person's second tab to their first, so three tabs offered twelve concurrent scans against a reader floor `internal/store` had derived as "two full dashboards at four queries each" — the sizing was right and the unit made it describe a deployment that could not exist. Refcounted by socket, because the key is a principal and an entry kept for the process's life is one channel per person who ever opened a tab; its release is `sync.OnceFunc`, since a duplicate would decrement a count another tab has since taken over and evict a LIVE budget, which is the per-socket allowance back by a longer route. An OPEN socket RE-RESOLVES the credential it was opened with every `statelog.StallGrace`, because a handshake decision alone let a revoked session's tab go on receiving the company's state for as long as it stayed open: a credential that names nobody closes 4401, a person whose seat is gone closes 4403, and an identity estate this node cannot read DEGRADES the socket on an `identity` frame rather than closing it — a close there would put every tab on a stalled node into a reconnect loop of full snapshots against the same node. WHAT a socket receives is decided per KIND: every push kind declares the grant its question takes beside its route (`event` is an `events` row, so `audit:read`; every other company kind `state:read`), a client carries the AUDIENCE its principal's grants describe, and the snapshot — and its four REST mirrors, `/agents`, `/org`, `/tools` and `/stream/snapshot` — is built for that audience. It used to decide only that the caller was somebody, which was harmless while every credential was an operator token and a firehose the day a session cookie resolved: a person holding `work:write` alone received every phase's prompt and response. `inbox_changed` is the one SEAT-ROUTED kind, pushed by `InboxChanged` to the sockets whose `watch` of that seat was ALLOWED — the watch's name RESOLVED first through `iam.OwnerOf` and the same directory `work_inbox` reads, so a login is installed on the record its holder's notices are pushed to rather than on the login as sent (where no frame ever arrives, and on which a lead was refused as leading nobody), and the directory asked only once the watch is decided as far as it can be without it; then decided with `authz.ActionPersonRead`, the rule `work_inbox` takes about the same seat (its holder, whoever leads it, the admin grant), and three-valued: a refusal is an error frame on a socket that stays open, never a 4403, which the dashboard reads as the whole channel withdrawn; an undecidable answer installs nothing; and every credential re-check re-decides the watch, withdrawing it only on a refusal. Two walks hold the kinds: every seat-routed kind needs a `PushSeat` producer, and the dashboard's `PushKind` union equals the declared kinds in both directions. A refusal on AUTHORITY — a query's or a watch's — carries the rule's `reason` and the `grants` that would have admitted the caller on its `unauthorized` error frame, under the REST envelope's own keys: the app used to reduce it to the question's name on the way here, so the dashboard's only data channel was the one transport that could not say what would change the answer |
| `observe` | The observability edge, and the two routes are deliberately different: the STORE is written by a publish listener inline on the publishing node (no consumer group, so no two nodes can write one row); the PROJECTION is fed by an ephemeral per-caller broadcast |
| `tokens` | Rolls per-phase LLM spend into the breakdown a dashboard renders — by phase, model, worker, agent and turn. ONE IMPLEMENTATION is the whole point: this aggregation had three copies once, and a refresh routinely disagreed with the page it replaced. A leaf package, because both the live projection and the event store need it and either importing the other would be a cycle |
| `tracing` | The OpenTelemetry wiring: ONE TracerProvider, built from the standard `OTEL_*` environment (never a Tier A block), and the conversion between OTel's `context.Context` carrier and the `trace_id`/`span_id`/`parent_span_id` the event envelope has always had. The provider is installed UNCONDITIONALLY and only the exporter is optional, so ids reach the event store whether or not a collector exists — and its own `init` installs a working one, because OTel's built-in default is a no-op that passes the parent's span context through and would silently reinstate the bug this package removed |
| `notify` | The backend-neutral notification spine — conversation keys, digest coalescing, party resolution, the rate valve, and BOTH DIRECTIONS across a chat surface: the working indicator writes, and `ThreadReader`/`ThreadReaders` read a thread back for the turn-start block. Each direction is a per-backend seam with a fan-out that routes on the trigger's own `transport`, because a company runs two chat surfaces and a caller holding one backend's reader silently reads none. `ThreadOf` is deliberately NOT `ConversationOf`: one answers where a reply lands (a top-level message anchors on its own id), the other what was already said (a message with no thread has no answer). Built before any vendor sat on it, because a spine built after its first vendor has that vendor welded into it. A human seat's contact identities are registered only while the person the IDENTITY DIRECTORY binds to it may be reached: a `Standing` is ONE reading of the directory, applied whole to the registry being built, and its zero value is CHART-ONLY, which no running node builds from: a node running no identity domain reads the FLEET's directory instead, because its empty copy of the estate read as "nobody holds any seat" is right only by accident and routing by the chart there attributed a suspended person's word to their seat for every delivery it won. A binding names its seat by the handle the seat was CREATED under (ADR-0020), so a reading finds the seat by that identity, exactly as the request path finds the same binding's seat, and never follows a handle a rename retired to whichever seat took it next. What AGENTS are shown follows the same reading: a turn pins the live registry's withholding beside its org (`turnctx.Turn.Withheld`), so a lead's roster and `lookup_colleague` leave out exactly the people an inbound message would not be attributed to, and neither says why. `active`, `invited` and `enrolling` route; `suspended`, `retired`, a removal and a stage this build cannot name withhold, and an unreadable directory is an error rather than a reading, so a caller never hands every suspended person's seat back to the chart over an outage |
| `backup` | A restorable copy of everything a node holds, taken from INSIDE the engine because that is the only place both estates are reachable: the store file is locked to this process and the driver refuses a second opener, and the embedded broker binds no socket for any outside tool to dial. The store copy is `VACUUM INTO` (the backup API is stubbed and plain `VACUUM` is behind an experimental flag) and is VERIFIED before it counts; the streams — mailboxes and every coordination bucket alike, since a bucket IS a stream — go over the JetStream wire API, because the vendored client has no snapshot call at all. The MANIFEST IS THE CLAIM: written last, and a directory without one is the debris of a run that did not finish rather than a partial backup |
| `maintenance` | The retention sweep for the tables that answer "recently" rather than "ever" — fire claims, channel membership, counterparty profiles, the conversation ledger, and each state-log domain's OPERATION LEDGER. Every one of their migrations says they are swept and ships the index a range delete needs — a table with a documented retention and no sweep grows for the life of the deployment, which is what the `<domain>_ops` tables did until `StatelogJobs` existed. A ledger horizon is the DOMAIN's, and a domain may declare a SHORTER one per subject KIND whose writers never re-ask late (`OpsHorizon.Kinds`, a job of its own) — the identity estate's sessions go in an hour where everything else keeps the month, a range over the stored subject on an index only that domain ships. A fleet singleton, because N nodes scanning the same rows is waste rather than corruption — except where the rows are a table each node owns its own copy of, which is what `Job.PerNode` is for: swept under the singleton, such a table is tidied on one node and grows for ever on every other, and that looks exactly like a sweep that works to whoever checks the node it ran on |
| `mattermost`, `slack`, `gitlab`, `github`, `jira`, `confluence`, `datadog`, `atlassian` | The vendors. Each contributes only what is genuinely its own: a client, a parser, a transport, a prompt, a provisioning reconcile — and no more, which is why Jira has no transport (an agent writes through its own MCP tools) and why its reconcile and GitHub's report rather than mint (neither vendor issues a credential on a provisioner's behalf). Datadog is the ALERT one and the shape shows it: a monitor is addressed to nobody, so it routes on the monitor's own TAGS with a company-wide fallback seat, and its deliveries cannot be signed at all. Slack is the one with no reconciler — its apps are created from the command line — so it is registered teardown-only and carries no reconcile status |
| `integration` | The app-neutral reconcile spine, and the reason there are eight vendor packages rather than eight loops. It owns the vocabulary a pass reports in — `Finding`, its severity ordering, and the `Phase`/`Actor` a `Report` classifies to — the schedule that follows WHO HAS TO ACT rather than what failed, the fleet-singleton `Worker` that runs every registered surface, and the disconnect intent a teardown is driven by. `integrationtest` is its certification suite: one set of cases every reconciler passes, in the `queuetest`/`coordtest` tradition, because a promise about what a pass does to somebody's Slack workspace written down and checked by nobody is the worst shape a rule can be in |
| `fleetsecrets` | The company's credential store: this package owns the KEY, coordination owns the BYTES. Also the one-way migration off `store`'s own table, which copies before it deletes and never overwrites a name the fleet already holds. TWO VIEWS OVER ONE BUCKET: `Store` is the OPERATOR's, and it neither lists, snapshots, reads, writes nor deletes a name in the engine's own namespace (`secrets.Reserved` — `iam/…`: a person's data key, a provider session's refresh token, the identity directory's blind-index key), answering `secrets.ErrReservedName`; `Estate` is the ENGINE's, and it addresses those names and nothing else. They shared the environment-variable grammar once, which made every person's key listable, revealable, deletable and decrypted into each node's `${VAR}` snapshot on every apply. A REKEY is the one gesture that crosses — the keyring is one keyring — and it COUNTS the engine's rows rather than naming them. `Estate.Keys` lists by prefix with NO keyring — names and WRITE TIMES, never an envelope — because finding a removed person's key and deleting it must both work on a node that can decrypt nothing, so an off-boarding is never stuck behind a missing keyring; the write time is what a key NOBODY owns is aged by, and the VERSION beside it is what the duty destroys one at. The engine's view writes its keys CONDITIONALLY — `Create` only where none is, `Touch` re-dating a row only at the version it read (so a removal's shred is never written back), `UnsetAt` only at the version judged — and a REKEY re-seals every row at the version it read, because a plain put of what it read reverted an operator's rotation and resurrected a key a removal destroyed in between |
| `textcut` | Shortening a string without breaking it. It replaced FOUR helpers — three named `truncate`, in `agent/extension`, `learning` and `providers/llm/cliagent`, plus `learning`'s own `preview` — which were four copies of one rule already disagreeing on the easy half: two appended `…` and two appended `...`. The hard half is that a plain `s[:n]` is invalid UTF-8 whenever a multi-byte rune straddles the cut, which a JSON encoder substitutes, a model reads as a replacement character and a vendor rejects. BYTES ONLY, because cutting is the last resort rather than the first: content a turn reasons over is passed whole, and a value with a vendor limit is REFUSED naming the field rather than silently cut to fit. It is NOT the tree's only shared cut — `agent/ledger.Elide` is the other, counting RUNES and reading a limit of 0 as unbounded where this one reads it as empty, and the two package docs point at each other rather than folding two contracts into one name |
| `jsoncarry` | How a record ROUND-TRIPS through a build that only half understands it: a decode keeps the fields the struct has no home for, and the next encode folds them back in. ONE CLAUSE is the whole package and it is the one a second implementation gets backwards — A CARRIED FIELD LOSES TO A KNOWN ONE, because with the precedence the other way round a stale carried copy silently reverts the write that set the field, on exactly the path an upgrade is in progress on. It was written TWICE, in `pages` and `chart`, and the copies had already drifted — one decoder carried a fourth parameter the other did not have and neither read — which is `textcut`'s, `whsec`'s and `jsprovision`'s story arriving for a fourth time; a fifth state-log domain would have made three copies of a precedence rule nothing compares. THE KNOWN NAMES ARE READ OFF THE STRUCT'S TAGS (`Names`), by encoding/json's own rules, and never off a marshalled zero value: a zero value does not marshal an omitempty field, so every domain had to list those names by hand, four had been missed (`colleague` on a person and an invitation, a chart unit's `origin_key`, a seat's `origin_handle`), and each missed field was decoded AND carried, so clearing it to its zero value re-encoded the stale carried copy straight back. A LEAF over `encoding/json` and `reflect` |
| `httpx` | The ONE outbound HTTP transport. Leaving `http.Client.Transport` nil is not "the defaults" — it is `http.DefaultTransport`, whose `MaxIdleConnsPerHost` is **2, process-wide**, which seven vendor clients were silently sharing. One transport rather than one per client: a per-client clone is strictly worse than the default, since each keeps a private pool and reuses nothing between them. EVERY outbound client is built here, the operator CLI's included — a bare `&http.Client{}` outside this package is the bug, and a round tripper that wraps another takes `httpx.Transport()` as its base, which is the shape that slips through because it names no client at all |
| `api/httpjson` | How every JSON surface answers. Four byte-identical `writeJSON` copies had already drifted into three spellings of one 413, and five sites handed `http.Error` a JSON literal — which sets `text/plain` **and** `nosniff`, the one combination that stops a strict client parsing it. EVERY CODE IS ON ITS TABLE WITH A SENTENCE, and every 503 goes through `Unavailable` / `UnavailableWith`, which write the `Retry-After` and the envelope together — ZERO meaning not retryable, because a node missing its keyring has none after any wait and a header saying "come back" teaches a client to hammer it. A state-log refusal gets its seconds from `statelog.RetryAfter` through `RetrySeconds` (rounded UP) on every surface — zero for one waiting cannot clear (an evicted node, a record this node cannot decode, a full log), the refusal's own derived hint where it has one — because `/chart` and `/work` each turned those into `Retry-After: 2` and a client polled a node that would answer nothing until somebody readmitted or upgraded it. `/config`, `/secrets` and `/setup` had built their own `{"error": code}` bodies with no sentence, `/setup` had minted a dozen codes of its own (two of them second spellings of codes the table held), and a dozen 503s said "come back" without saying when; a source walk in `internal/api` now holds those surfaces, `/chart` and `/work` to all three rules |
| `setup` | What an integration NEEDS before it works, in one vocabulary every vendor and the dashboard share. A vendor declares a list of requirements — where each value lives in the config, what it is called at the vendor, whether it is a credential, whether the engine can mint it, and which reconcile FINDING satisfying it clears — and the screen renders a form from that without knowing any vendor. It also owns the write ORDER, which is the whole of it: the credential is sealed first, then the config gets a `${VAR}` pointing at it, then the epoch advances. Pointer-first leaves a route refusing every delivery; value-only reaches no running seat at all, because a `${VAR}` resolves from a snapshot taken at apply time. It also runs a vendor's PASS — the same Reconcile the loop runs, with the same sink and the same public base, because CONNECTING IS THE PERMISSION and a timer running what a person already asked for is not a second decision. What it adds is `Hold`: the ONE guard every writer at a surface takes, an in-process claim AND the surface's fleet lease, because either alone leaves the collision reachable — a lease claim by an owner that already holds it doubles as a renew, so two goroutines in one process are both told yes |
| `jsprovision` | The ONE place that decides how long a replicated JetStream create gets, how often a forming cluster is re-asked, and what "still forming" looks like on the wire. Both subsystems that provision replicated objects at boot are the same call underneath — `queue/jetstream` makes the streams and consumers, `coord/kv` makes the buckets, and a bucket IS a stream — and written twice the rule drifted exactly as `textcut`'s and `whsec`'s did: five decisions spelled separately, each doc comment asserting it matched the other with nothing enforcing it. The budget BRANCHES on clustered, which is what the flat thirty seconds could not: a solo create is local file-store setup and a clustered one is a raft round trip against peers that are themselves booting, and the flat number was measured failing a fleet's boot on a different object every time — which is what says the object was never the problem. It also bounds the SEQUENCE, because a bring-up is many creates in a row and a per-create budget bounds only the term: without a ceiling the real limit was the product, which nobody had declared and raising the term would have multiplied. And it is where a stalled create gets its one BREADCRUMB — the step was otherwise silent, so a node that hung emitted nothing between the line before it and the failure a budget later, and the log could not say which of fifteen objects it was on. Its `Settle` owns the READ-BACK window rather than taking one, which is the only arrangement where both halves are bounded — a window the caller built separately was still running while a lookup blocked past it — and it is handed the context that bounds the BOOT, never the per-create one: two of these read-backs exist precisely BECAUSE a create's deadline expired, so a loop gated on that deadline was dead code on the paths it was written for |
| `jwks` | THE ONE CACHED READER of somebody else's published key set, and the one place that decides what this engine trusts one to say. Two edges verify a third party's signed token — the Forge relay a webhook arrives over, and the identity provider a person signs in through — and written twice the copies must agree about SIX decisions none of which is locally obvious: the staleness budget, the unknown-kid refetch floor, the document ceiling, whether one unusable entry discards the rest, whether a stale key still verifies when the source is down, and whether the fetch happens UNDER THE LOCK. It is `textcut`'s, `whsec`'s and `jsprovision`'s story again, and this one had already drifted on the last of those: the webhook's copy held its mutex across a ten-second network fetch, so a CDN having a bad minute serialised every delivery in the process behind one hung request. THE FETCH IS OFF THE LOCK and the window that opens — every caller deciding to fetch at once — is closed by a SINGLEFLIGHT, so one key rotation is one request however many tokens arrive during it, and a follower waits on ITS OWN context rather than being held to the leader's deadline. A failed flight is CLEARED rather than latched, because one bad minute at a provider must not refuse every token until something restarts. An unknown `kid` refetches at most once a minute, because a kid nobody published is what a forged token carries and treating it as staleness makes verification an amplifier an unauthenticated caller aims at whoever hosts the keys. And a key the issuer marked `use: enc` is skipped while one with no `use` at all is kept: the field is optional, so requiring it would discard every set that omits it, and honouring it is refusing to check a signature against a key its own publisher says cannot have made one |
| `logging`, `secrets`, `provision`, `envref`, `envfile`, `redact`, `workkey`, `backoff` | The small shared grammars. Each imports nothing from the rest of the engine, which is what lets `config` itself depend on them |
| `skipgate` | THE ENFORCEMENT BEHIND "a skip is not a pass", which was a convention held by memory and had already decayed twice. It reads `go test -json` on both test pipelines, renders it back so a CI log is unchanged, and fails on a skip nothing declared — plus it carries the run's own verdict, because in a pipeline `go test`'s exit status is gone by the time make looks. The allowlist is keyed on the test's own NAME with a reason per entry rather than being a count, because a count goes green when one skip is fixed and another appears. Two-sided where it can be: an `Always` entry that stopped firing is stale and fails, scoped to packages the run actually covered — without that scoping each half of the partition fails on the other half's entries |
| `solo` | WHICH TEST PACKAGES NEED THE RUNNER TO THEMSELVES, as a declaration rather than a list. A package imports it from `TestMain` and `internal/solo/partition` derives both halves of the suite from that, so `make test` / `make test-solo` / ci.yml share ONE computed answer instead of three hand-maintained copies of a `grep -v`. Its roster guard is what makes the declaration self-maintaining, and it fails in BOTH directions — a package standing up a multi-member broker without declaring (the flake) and a package declaring without needing to (the flake-suppressant). The predicate is exact rather than approximate: the cluster harness's ENTIRE exported surface is four multi-member constructors, so importing it IS forming a quorum. The package doc is also where every alternative's measurement lives — a build tag hides files from `go vet` and golangci-lint and can drop a package from `go list` entirely, `-short` skips nothing on its own, a custom flag kills every test binary that does not define it, `-skip` leaves `[no tests to run]` and exit 0 — and every one of them fails SILENTLY, which is why this is an import and not a tag |
| `clientsource` | Finds a constant the DASHBOARD declares, so a gate can hold it against the engine's own. Several lists exist twice by necessity — the dashboard is a separate build in a separate language and cannot import a Go identifier — and each copy has drifted at least once, silently: a chip for a category no event carries filters a log down to nothing and reads as a quiet engine. The check belongs on the engine side because the engine owns the value, and the walk is keyed on the DECLARATION rather than a path, so a screen moving is invisible and the only two failures it reports are the ones that matter — nothing declares it, or two files do. A normal package rather than a test file because three packages' gates read it |
| `sourcetree` | Walks THIS MODULE'S OWN SOURCE and stops at anything nested inside its directory that is somebody else's — a directory holding its own `.git` (a clone, or the `.git` FILE a worktree carries) or its own `go.mod`, which is the boundary git and the go tool already draw. A dozen source gates walked the root with `filepath.WalkDir` and a private list of names to skip, and none of those lists could say that: `.gitignore` expects `.claude/worktrees/`, and with a parallel task's checkout there the gates read the SIBLING's files as this tree's — an ADR template cited twice, a decode site declared once and found three times, every withdrawn name a parallel task had not yet removed failing a branch that had. A gate judging a different set of files from the one the build compiles certifies something other than what ships, so every walk over a source tree goes through `sourcetree.Walk` |
| `version` | What the binary calls itself: the tag goreleaser stamps at link time, falling back to the module's own build info and then to `dev`, so a binary built outside the release path names itself honestly rather than claiming to be a release it is not. `IsDevelopment` is the one PREDICATE over that, and it is an ALLOWLIST of the two unversioned spellings (`dev`, `(devel)`) rather than a test for a release shape — its caller is the development principal, which authenticates every request with no credential, so a spelling this build does not recognise must refuse the bypass rather than enable it. Split as `IsDevelopmentFor` for `resolve`'s reason: the process's own version is a `sync.OnceValue` over link-time state, so no test can put a release in front of the predicate that decides whether a bypass exists. Its tests cover the package and nothing else now: four cases over the stamp/build-info/`dev` fallback. They used to be ten times its code and to assert the RELEASE SURFACE instead — the ldflags stamp, the pure-Go build, the image's `${TARGETPLATFORM}` copy, the catch-all notes category, the pre-release flags, the single tag trigger and every Dependabot entry — because each of those fails silently and has no other symptom |

## Pre-commit Checks
Before committing, ALWAYS run and fix any issues from **`make check`**, which is every gate CI runs on a pull request, in one target:
- `gofmt -l .` — prints the files that need formatting; the output must be empty
- `go mod tidy -diff` — go.mod / go.sum must already be what `go mod tidy` would write. It prints the patch and exits non-zero rather than writing it; `make tidy` applies it. This catches the half the build cannot: an UNDER-tidy module already fails `go build ./...`, but a leftover `require`, a stale `go.sum` line or a wrong `// indirect` marker compiles green and lands as churn in someone else's pull request
- `scripts/check-signoff.sh` — every commit the branch adds carries a `Signed-off-by` trailer, and `scripts/check-signoff_test.sh` is that gate's own suite. The only gate whose subject is git history rather than the working tree, and therefore the only one a later edit cannot fix: the repair rewrites commits. It runs here and in CI's pull-request `sign-off` job, and nowhere else — the push-side `sign-off (main)` job that judged what each merge added to `main` is gone
- `go vet ./...`
- `golangci-lint run` — what CI's lint job runs
- `go build ./...`
- `make test` — every package that shares a runner, under the detector, as CI runs it
- `make test-solo` — the packages that need a runner to themselves, at `-p 1`, which is its own CI job
- a cross-compile of every release target (linux and darwin × amd64 and arm64)
- the three dashboard gates, because ci.yml's `dashboard` job runs all three and a local pass that skipped one is a pass CI does not give: `npm run format:check` (`dashboard-lint`), the rebuild-and-diff of the committed bundle (`dashboard-check`), and `npm run typecheck && npm test` (`dashboard-test`). `dashboard-lint` was the one `check` did not depend on, and five unformatted files reached a pull request through the gap

The race detector is not a special case for concurrency work: every package runs under it, because the engine's concurrency model is real parallelism and every "atomic because it is single-threaded" assumption is a data race until proven otherwise. `-count=1` is the other half — without it a cached PASS from before the change answers for the change. `make test-norace` is the faster loop and is not a gate. **The packages that stand up a multi-member broker are excluded from `make test` and run in `make test-solo`**, because each stands up N engines each embedding its own NATS server in ONE process: sharing a runner with the other packages, their cluster cases could not form a quorum inside the 30s provisioning budget and failed every cluster-start attempt, while alone they pass. `check` depends on both, so nothing is uncovered — but a run of `make test` by itself has NOT exercised the fleet. **WHICH packages those are is COMPUTED, never listed** — a package declares it by importing `internal/solo` from its `TestMain`, `internal/solo/partition` derives both halves from that, and `internal/solo`'s roster guard fails the build in BOTH directions (a cluster-forming package that did not declare, and a declaring package that forms no cluster). `go doc ./internal/solo` records what every alternative measured; do not reach for a build tag, `-short`, `-skip` or a flag without reading it, because each of them fails SILENTLY. The filter this replaced was a hand-maintained `go list | grep -v '/internal/e2e…'` in three places — it named ONE package while `internal/node`, `internal/statelog` and the cluster harness's own suite each stood up a THREE-member cluster inside the shared job, surviving on a port-race retry rather than on having room.

**The Makefile is the same command CI runs, or it is a lie — and nothing checks that any more.** Every target mirrors `.github/workflows/ci.yml` flag for flag. `internal/version/makefile_test.go` used to assert it; it was dropped with the rest of the file-content assertions, so a target that quietly drops `-race`, stops cross-compiling a release target, stops setting a `CREWLET_*` variable a suite selects on, or starts a compose profile `docker-compose.yml` does not define would report a pass CI never gave and NOTHING would notice. Change a target and its `ci.yml` step in the same commit, and read both. A new target still ships with its `## ` help line — `make help` is to this file what `usage()` is to the CLI — but only `usage()` has a test behind it now.

**A skip is not a pass — and `internal/skipgate` is what enforces it.** Both test targets pipe `go test -json` through it: the stream is rendered back to ordinary output and then the run FAILS on any skip not declared in `internal/skipgate/allowed.go`, where every entry carries a reason and a `When` (`Always` for a structural skip, which is ALSO reported when it stops firing, since a structural skip that stopped means the world changed; `Environment` for one that depends on the machine, checked in the undeclared direction only). It had to exist because `go test` says nothing about a skipped SUBTEST without `-v` and the suite job never passed it — so every skip this repository ever took was invisible in CI, and under that an API gate reading a deleted dashboard path certified nothing for a whole rewrite while reporting a pass, and a queue conformance case skipped on BOTH backends and ran nowhere. Adding an entry is a decision that lands in a reviewed diff; a COUNT would have been worse than nothing, since it goes green when one skip is fixed and another appears. Two suites need something the machine may not have and skip silently without it: the dashboard's suites need `node`, and `internal/e2e`'s client-half replay needs `node` too. A green local run has not necessarily exercised either. CI fails rather than skips where it can, and so do the make targets: they refuse to run without node. No queue backend is in that category any more — both certify themselves in-process, the JetStream suite starting an embedded broker per test, so there is no environment variable whose absence silently drops a backend. What `make check` does NOT cover, it prints when it passes — `make snapshot`. `internal/store`'s capability cases skip DELIBERATELY, and that is the mechanism rather than a gap: a feature Turso announces but does not reach Go yet turns into a passing test the day it lands.

## Dependency Updates
Dependabot watches every dependency surface in the repo — `.github/workflows/*.yml` (`github-actions`), `go.mod` (`gomod`), `dashboard/package.json` (`npm`, rooted at `/dashboard`), `Dockerfile` (`docker`), and `docker-compose.yml` (`docker-compose`). The config is `.github/dependabot.yml`: one entry per surface on a weekly schedule, plus the commit prefix that surface's bumps carry, and — on the npm entry only — the two `groups:` rules below; keep it that way unless a knob earns its place. `docker` and `docker-compose` are separate ecosystems reading separate manifests — a repository with both needs both entries.

- **The React family is grouped.** `react`, `react-dom`, `@types/react` and `@types/react-dom` move in ONE pull request, because **react-dom asserts at RUNTIME that `react` and `react-dom` carry the exact same version** — not a compatible one — which is a constraint no version range can express, so nothing in npm's resolver enforces it and no caret makes it safe. React 19.3.0 proved it by arriving as two pull requests that were BOTH red, in different phases: the `react-dom` half could not install (`ERESOLVE` in about a second, from `@types/react-dom@19.3.0` peering `@types/react: ^19.3.0` against 19.2.18 — and `react-dom@19.3.0` peering `react: ^19.3.0` against 19.2.8, a second unsatisfied edge `npm ci` never even printed), while the `react` half installed cleanly and then lost 19 test suites of 36 to that exact-version check. There was no merge order that made either one green. `@testing-library/react` stays OUT of the group — its react peers are `^18.0.0 || ^19.0.0`, which genuinely ride a React minor — and `@vitejs/plugin-react` is out because it declares **no react peer at all**, only `vite: ^8.0.0`: its name is the only React-shaped thing about it.
- **The vite family is grouped for the same reason, one major out.** `vite`, `vitest` and `@vitejs/plugin-react` move together because `vite` is a REQUIRED peer of the other two — `^8.0.0` from the plugin and `^6.4.0 || ^7.0.0 || ^8.0.0` from vitest — so a lone `vite` 9 bump violates both ceilings and `ERESOLVE`s exactly as the react-dom half did. Minors were never the problem (8.2.2 → 8.3.0 landed alone), so it buys nothing until the major and then it buys all of it. State the cost too: a `vitest`-only bump now arrives titled for the group, with the packages it moved in the body rather than the subject.
- **Nothing else is grouped, and that is the rule, not the current state.** The only other REQUIRED edges a bump there could split are `@testing-library/react`'s — on react and react-dom at `^18.0.0 || ^19.0.0`, which ride a minor, and on `@testing-library/dom`, a transitive it brings itself and Dependabot therefore moves with it. Every remaining vite peer (`@vitest/mocker`'s, vite's own on `@types/node` and the bundlers) is OPTIONAL, which npm does not fail on. A group raises whichever members have an update together, under a title naming the group rather than any package in it — so it costs a vaguer subject on every bump, and it puts one member's bad release in front of the rest. Pay that only where a split bump is genuinely broken, never for tidiness and never to batch unrelated noise.
- **A dashboard runtime bump does not auto-merge, by construction.** `static/dashboard` is committed and ci.yml's `dashboard` job rebuilds and diffs it, but Dependabot only ever edits `package.json` and the lockfile — it cannot run a bundler. So a bump that changes the emitted bytes (React and `react-dom` do; `@types/*`, `prettier` and a build tool that happens not to alter output do not) lands red on that gate until someone commits `make dashboard` alongside it. That is the gate working, not a fault to route around: the alternative is a binary embedding a bundle nobody built.

- **Pick the newest release, then pin it exactly.** Every time a version gets chosen — a Go module, a tool you reach for, a base image, an action, a compose service, a toolchain — take the latest released one, and establish what that IS from the registry or index rather than from memory. A version that was current when something was written is a version that is now behind, and quietly picking it forfeits whatever was fixed since, with nothing to say so. "Latest" here means the newest release written as a LITERAL pin (`4.2.4`), never a floating tag: `latest` and `@main` give Dependabot no version to move, so the surface is watched by nothing while looking exactly like a surface with nothing to update — and a floating tag makes a green run a claim about a build nobody can name afterwards, which is the whole reason every compose service is pinned to the one CI runs against.
- **A version held back on purpose carries its reason AT the pin.** That is the one exception, and it is a comment on the line, not a memory: `mattermost-db` holds its Postgres major so an existing volume stays readable. Without the comment the next reader — or the next agent — cannot tell a considered hold from a bump nobody got to, and "upgrade everything" silently breaks the thing the hold was protecting.
- **A new dependency surface ships with its `updates:` entry.** Adding the first `Dockerfile`, a `package.json`, a `go.mod` — the Dependabot entry is part of that change, not a follow-up. Nothing fails when it is missing; the surface is just never watched, which looks exactly like a surface with nothing to update.
- **Actions pinned to a non-version ref are invisible to Dependabot.** A branch pointer (`@release/v1`) or `@main` yields no update PRs at all, so pin actions to a version tag or a full SHA. Maintainer-facing detail is in `CONTRIBUTING.md`.
- **A bump merges itself.** `.github/workflows/dependabot-merge.yml` approves a Dependabot PR and queues it with `gh pr merge --auto --squash`, so it lands on green CI with no maintainer in the loop. Two things outside the tree hold that up, and neither is visible from a checkout: `main`'s protection rule must REQUIRE the `ci` checks — `--auto` waits for the checks a rule names and for nothing else, so with no rule the bump merges the instant it is mergeable — and "Allow auto-merge" must be on in the repository settings, or the step fails and paints the PR red. The job runs only when Dependabot is BOTH the PR's author and the actor that triggered the run; the actor half is what stops a commit a PERSON pushed onto a Dependabot branch from collecting the repository's own approval.
- **Nobody retitles a bump before it lands**, so each entry pins its `commit-message.prefix`, and every surface carries the same one: `build(deps)`. A bump moves a pinned version, which is a change to what the project builds against whichever file records it, and it is what the bumps already in `main` carry. Unset, Dependabot INFERS the prefix from recent history, and an inference that changes its mind writes a bare `Bump x from 1 to 2` straight into `main` and into the release notes. Dependabot capitalises the "Bump" and offers no way not to, so a bump is the one subject here that does not start lowercase.
- **The auto-merge guard is remembered, not asserted — so read it on every edit.** `internal/version` used to check both halves of the author/actor condition and the `--auto --squash` flags; that test was dropped. Nothing checks them now, and each fails silently: weaken the `if:` and the workflow keeps running, it just starts approving and merging pull requests Dependabot did not write; drop `--auto` and the bump merges before a check has reported. This file has `contents: write` and `pull-requests: write` and runs `gh pr review --approve`, so its `if:` is the only thing between that and an unreviewed push to `main`. Treat any diff to `.github/workflows/dependabot-merge.yml` as security review.


## Releases
The engine ships as signed binaries and a container image, built by goreleaser from a `v*` tag. See `RELEASING.md`.

- **The tag is the version.** Nothing in the tree records one, so there is nothing for a tag to disagree with and no version bump to make. goreleaser stamps the tag into `internal/version.value` at link time; a binary built any other way reports its module build info instead, so it names itself honestly rather than claiming to be a release. **Never add a literal version constant back.**
- **Version numbers follow semver** — the minor number moves for features, the patch number for fixes.
- **Pushing the tag is the whole release** — the workflow builds four targets, signs the checksums with keyless Sigstore, pushes the image to GHCR and creates the GitHub Release with notes GitHub generates from the merged pull requests. Those generated notes are the only description a release gets, so pull request titles carry it. A pre-release takes neither GitHub's "Latest" nor the `latest` image tag.
- **The release surface is NOT asserted — read it yourself when you touch it.** `internal/version` used to check the ldflags stamp, the pure-Go build, the `${TARGETPLATFORM}` copy, the catch-all notes category, the pre-release flags and the single tag trigger against the files that carry them; those tests were dropped. What is left is what the pipeline does on its own: `goreleaser check` validates `.goreleaser.yaml`, the snapshot job builds all four targets and the image on any pull request touching the release paths, and both release jobs run the built binary and compare its version against `dist/metadata.json` by equality. That covers the stamp, cgo and the image COPY loudly. It covers **nothing** in `.github/release.yml`, `.github/dependabot.yml`, `docs-publish.yml`'s `workflow_run` name or the `v*` tag trigger — each of those fails silently in production, with no symptom until someone reads a release body, notices a bump that never arrived, or pushes the tag that races.

## Breaking Changes Are Free Until a Tag Ships Them
A `v*` tag is the only compatibility boundary this project has — it is the one thing an operator can pin, pull and run. A surface no tag has ever shipped therefore has nobody behind it: nothing pinned it, no deployment holds its old shape, and there is nobody to migrate. **Change it outright** — rename the config field, reshape the struct, drop the CLI flag, move the route, redraw the interface — rather than carrying a compatibility path for a version that never existed. That path is a band-aid with no wound (see "No Band-Aids" above), and it is permanent: the alias, the fallback branch and the adapter all outlive the reason nobody can reconstruct for them.

Rules:
- **Establish what has shipped; never remember it.** `git tag --merged` and the repository's Releases page say which tags exist, and `git log <newest tag>..HEAD -- <path>` says whether what you are about to change is inside one. Today that answer is *nothing*: the only release is `v0.1.0`, and it sits on the INITIAL COMMIT, which does not contain one `.go` file. Not a package, config field, CLI command, event type, API route, schema file or dashboard module in this tree has ever been in a release. Re-run the check rather than trusting this paragraph; the next tag changes the answer for everything it carries.
- **An unreleased surface gets no compatibility path.** No deprecated alias beside the new name, no "accept both spellings", no `if the old field is set` fallback, no adapter, no `V2` type because the old name has to keep meaning what it meant, no vestigial flag or subcommand kept so that nothing breaks. Every one of them is code with no caller, indistinguishable to the next reader from code whose caller nobody found.
- **And no breaking-change ceremony.** The `!` in `feat(config)!:` and a `BREAKING CHANGE:` footer exist to tell an operator what they have to change. With no release behind the surface there is no operator and no migration, and the generated notes are the ONLY record a release gets — so the ceremony spends the one place a real break has to be visible on a break that never happened.
- **A free break is still a complete break.** Freedom from compatibility is not freedom from the sweep: the rename lands everywhere in the same change — `docs/`, `examples/`, `skills/`, the dashboard, the tests, the package docs — and `schema/` is REGENERATED, never hand-edited. Grep the old name and finish at zero hits.
- **What a missing tag does not free you from is state and peers.** Two constraints hold at any tag count, because neither one is about releases:
  - **An applied migration is history, not source.** `schema_migrations` keys on the FILENAME, so editing a file that already ran silently never re-runs it: every database that applied it keeps the old shape while the code assumes the new one. Reshape with a new numbered migration under `internal/store/schema/`, released or not.
  - **A rolling upgrade puts two builds on one stream and one coordination store.** The event envelope evolves additive-only (`internal/events`) because an unknown type must decode, round-trip and re-publish losslessly in both directions — a contract between PEERS, which no tag count makes optional. The same holds for any key, lease or payload two builds share.
- **Once a tag ships it, the bar changes.** Then a break carries the `!` and a `BREAKING CHANGE:` footer naming what an operator must change, the pull request title says so plainly because that title *is* the release note, and while the major is 0 the minor number moves for it. `CONTRIBUTING.md` and `RELEASING.md` carry the same rule — change all three together.

When in doubt, ask: "Could anyone be running the shape I am about to change?" If no tag ever shipped it, nobody is — so change it cleanly and completely rather than carrying its ghost forward.

## Testing
- Run tests: **`make test`** and **`make test-solo`**, never a bare `go test ./...`. That command bypasses BOTH halves of what the suite now depends on: it runs the cluster-forming packages alongside everything else, which is the contention `internal/solo` exists to remove, and it skips `internal/skipgate`, so a case that quietly stopped asserting reports a pass. A single package under `go test ./internal/foo/` for a fast loop is fine — it is `./...` that is the trap
- Test files sit beside what they cover, as Go expects: `internal/queue/queue.go` → `internal/queue/queue_test.go`
- **A contract with more than one implementation gets ONE suite.** `queuetest`, `coordtest`, `storetest`, `scheduletest` and `sandboxtest` export the suite; each backend runs it. A twin that agrees only with itself proves nothing, which is why the memory twins are certified by the same cases as the real backends
- Every subsystem needs tests, and a test names the invariant it protects rather than the function it calls
- Use the fakes in `internal/providers` — tests never call real LLM APIs
- **A test that cannot fail is worse than no test.** Mutate what you wrote: break the thing on purpose and confirm the suite goes red. A guard nothing catches is a claim, not coverage

## Documentation — Keep Docs Updated
Every change to code, config, or behavior MUST include corresponding documentation updates. Docs are NOT optional — they are part of the deliverable.

Rules:
- **Any code change that affects public APIs, config formats, CLI commands, data models, or system behavior** must update the relevant docs in `docs/`
- **New features** require a new or updated doc page explaining the feature, its usage, and any config options
- **Renamed or removed APIs/config fields** must be updated everywhere in docs — grep for old names and fix all references
- **New modules or packages** must be added to the Package Layout section above and to `docs/index.md`
- **Architecture changes** must update the relevant `docs/concepts/` page
- **Quickstart-affecting changes** must update `docs/getting-started/quickstart.md`
- **Always check** `docs/index.md` to see if new pages need to be added or existing entries need updating — it is also the docs site's navigation source, so an unlinked page fails the site build
- **`docs/` is the product documentation, published to docs.crewlet.ai** — it is written for people running Crewlet: how the engine works, how to configure, deploy, and integrate it. Project process aimed at people with commit rights on *this* repository is NOT product documentation and does not go there; it lives as a root-level markdown file beside `CONTRIBUTING.md` / `SECURITY.md` / `RELEASING.md`. Ask who the reader is: a founder running an agent company, or a Crewlet maintainer?
- **Diagrams are Mermaid** — architecture, flow, sequence, and state diagrams go in ` ```mermaid ` fences, never ASCII box art. GitHub renders them inline and the docs site themes them with the Crewlet design system. Directory trees and literal command output stay plain code fences.
- If in doubt about whether docs need updating, update them — stale docs are worse than no docs

This applies to every commit, not just "big" changes. A one-line config rename still needs a docs update.


## Architecture Reference
The implementation must follow the architecture docs in `docs/concepts/`. Key subsystems:
1. **Event Queue** — durable pub/sub behind one contract: an embedded NATS JetStream by default, clustered embedded members or an external NATS cluster for a fleet, an in-memory twin for tests
2. **Organization Model** — hierarchy is the execution graph
3. **Agent Runtime** — queue-driven seats, a two-stage turn, an LLM tool loop that can suspend
4. **Work Tracker** — the engine's own, and it is `statelog`'s FIRST DOMAIN: every change is one record on an ordered stream, arbitrated at the broker on the subject of the object it changes, and applied into N identical SQL copies with the checkpoint in the same transaction as the rows. Not a mirror — a FIRST COPY, which is what the old "there is no task engine" rule was actually against: a company on an external PM tool sets `tracker.backend` to it and the engine still mirrors nothing. Hand-offs are bounded on the ITEM (`reassignments`, reset by any human or operator touch) rather than by the delegation depth cap, because an assignment is an ownership transfer down a chart of known height and not a nested ask
5. **Decision Framework** — DACI behavioral guidance (via chat channels, no dedicated engine)
6. **Knowledge System** — the company's own pages as `statelog`'s third DOMAIN, and query-time search over them and the tasks, in three modes behind one seam: keyword (BM25 over the engine's own inverted list, since Turso has no fts5), semantic (two-stage 1-bit retrieval with an exact rerank, no ANN index and no new dependency), and `hybrid` by default — the two ranked lists fused by reciprocal rank fusion at k = 60. Embeddings are a state-log DOMAIN, computed once by a fleet-singleton duty and applied on every node, so the company pays the provider bill once. Confluence remains a backend behind the same seam — single-homed, one per company. Plus the per-agent diary
7. **Communication** — external chat (Mattermost, Slack) + ephemeral A2A channels. The eight surfaces this build serves are Mattermost, Slack, GitLab, GitHub, Jira, Confluence, Datadog and Atlassian (`integration.Kinds`); every one routes end to end, and no integration block is refused any more
8. **Notification Service** — queue-based spine, vendors on top
9. **Provider Layer** — pluggable LLM and embeddings, with a credential pool and a fallback chain around them
10. **Store** — the node's own embedded databases (Turso, the only driver): TWO files brought up by one `Open` — the NODE estate and the REPLICATED estate beside it — because a snapshot is a copy of ONE of them and a single file would put the audit log's pages in every artefact. Coordination lives in the KV layer instead, never here
11. **Coordination** — TTL leases with a fencing epoch, plus the fleet's shared counters, ledgers and the company's sealed credentials
12. **Seat Ownership** — which node runs which seat, and how a fleet converges without a coordinator
13. **Tool Registry** — builtins + MCP tools + A2A tools, each recording its origin
14. **Tool Skills** — knowledge-base-sourced prompt fragments injected per phase
15. **Code Sandbox** — a per-role coding-agent Execute backend; a run is detached and resumes a suspended loop. E2B for a remote VM, the engine host for a local one
16. **API + Dashboard** — one wiring, always beside the engine in one process; the websocket is the dashboard's only data channel
17. **Scheduler** — role/unit-scoped cron work with at-most-once delivery, catchup and a wall-clock cap
18. **Control Plane** — the config activation pointer and per-node apply status; lag alone never sheds
19. **State Log** — the durable-state framework: one ordered stream per domain is the write-ahead log, N identical SQL copies are the state, and the checkpoint commits in the same transaction as the rows. One write authority (take ONE snapshot, decide and form the expectation inside it, publish, let the broker arbitrate, never guess), three write outcomes, four read levels, and a read index that is a quorum-committed barrier append rather than a leader check
20. **Identity Estate** — people, credentials, invitations and sessions as `statelog`'s FIFTH domain. The replicated estate forbids a UNIQUE index outside a primary key, so uniqueness is ARBITRATED rather than indexed: one subject per claim, create-only at an expectation of zero, and the three partial indexes over the address blind, the login and the seat id are NON-UNIQUE and exist to REPORT a duplicate a restore produced. Two retention horizons on the authentication trail and one on the operation ledger; a per-bucket sweep that is a RECORD naming a position range, because rows this estate claims identity over cannot be deleted on each node's own clock
