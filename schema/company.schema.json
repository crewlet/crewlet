{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://docs.crewlet.ai/schema/company.schema.json",
  "$defs": {
    "ConfluenceConfig": {
      "additionalProperties": false,
      "description": "Organisation-level Confluence configuration.\n\nDefines the admin / service account used for org-wide Confluence\nAPI access.  Individual agents authenticate via their own\n``mcp_env`` credentials.\n\nCloud webhooks are handled by the Forge app.  Data Center\nwebhooks use ``webhook_secret`` for HMAC-SHA256 verification.\n\nProvide **either** ``url`` (direct instance URL) **or**\n``cloud_id`` (Atlassian Cloud ID).  Providing both or neither\nis an error.\n\nThis section is **non-tool config only** \u2014 admin credentials for\norg-level REST calls and the inbound webhook secret.  It lives under\nthe top-level ``integrations`` block.  Org-wide knowledge spaces are\nconfigured in ``knowledge.confluence_spaces``, and the Confluence MCP\n*tool* server is declared in ``mcp_servers`` (name it ``atlassian``,\nshared with Jira); see ``docs/concepts/tool-capabilities.md``.\n\nExample YAML::\n\n    integrations:\n      confluence:\n        url: \"https://mycompany.atlassian.net/wiki\"\n        token: \"${CONFLUENCE_ADMIN_TOKEN}\"\n        email: \"${CONFLUENCE_ADMIN_EMAIL}\"\n\n    knowledge:\n      confluence_spaces: [\"HANDBOOK\"]\n\n    mcp_servers:\n      - name: atlassian\n        shared: false\n        command: uvx\n        args: [\"mcp-atlassian\"]\n        env: { CONFLUENCE_URL: \"https://mycompany.atlassian.net/wiki\" }",
      "properties": {
        "url": {
          "default": "",
          "title": "Url",
          "type": "string"
        },
        "cloud_id": {
          "default": "",
          "title": "Cloud Id",
          "type": "string"
        },
        "site_url": {
          "default": "",
          "title": "Site Url",
          "type": "string"
        },
        "token": {
          "title": "Token",
          "type": "string"
        },
        "email": {
          "default": "",
          "title": "Email",
          "type": "string"
        },
        "webhook_secret": {
          "default": "",
          "title": "Webhook Secret",
          "type": "string"
        }
      },
      "required": [
        "token"
      ],
      "title": "ConfluenceConfig",
      "type": "object"
    },
    "ConfluenceIdentity": {
      "additionalProperties": false,
      "description": "The Confluence space a unit / root-role owns.\n\nUsed for inbound webhook routing \u2014 page activity routes to the unit\nlead (:func:`build_space_key_lead_map`) \u2014 and as the team's write /\nskill-promotion home.  Not a tool credential, and **does not** scope\nknowledge reads: read scope is the org-wide\n:attr:`KnowledgeConfig.confluence_spaces` only.",
      "properties": {
        "space": {
          "default": "",
          "title": "Space",
          "type": "string"
        }
      },
      "title": "ConfluenceIdentity",
      "type": "object"
    },
    "CounterpartyConfig": {
      "additionalProperties": false,
      "description": "CounterpartyProfiler settings.\n\nRuns alongside the PersistDecider in the ReflectEngine.  For\neach turn triggered by an identifiable sender, updates a per-\n``(observer, subject)`` profile on the auxiliary model.  Writes\ngo to the ``counterparty_profiles`` table.",
      "properties": {
        "enabled": {
          "default": true,
          "title": "Enabled",
          "type": "boolean"
        },
        "budget_tokens": {
          "default": 3000,
          "title": "Budget Tokens",
          "type": "integer"
        }
      },
      "title": "CounterpartyConfig",
      "type": "object"
    },
    "CredentialCooldownConfig": {
      "additionalProperties": false,
      "description": "TTL policy for the LLM credential pool.\n\nEach provider's pool keys can be marked cooled-down by error class\n-- a 429 typically clears in an hour, while a 401 / 403 often\nclears in minutes after a token refresh.",
      "properties": {
        "rate_limit_seconds": {
          "default": 3600,
          "title": "Rate Limit Seconds",
          "type": "integer"
        },
        "auth_seconds": {
          "default": 300,
          "title": "Auth Seconds",
          "type": "integer"
        }
      },
      "title": "CredentialCooldownConfig",
      "type": "object"
    },
    "EmbeddingProviderConfig": {
      "additionalProperties": false,
      "description": "Configuration for an embedding provider.",
      "properties": {
        "type": {
          "default": "openai",
          "enum": [
            "openai",
            "openai-compatible"
          ],
          "title": "Type",
          "type": "string"
        },
        "model": {
          "default": "text-embedding-3-small",
          "title": "Model",
          "type": "string"
        },
        "api_key": {
          "default": "",
          "title": "Api Key",
          "type": "string"
        },
        "base_url": {
          "default": "",
          "title": "Base Url",
          "type": "string"
        },
        "dimensions": {
          "default": 1536,
          "title": "Dimensions",
          "type": "integer"
        }
      },
      "title": "EmbeddingProviderConfig",
      "type": "object"
    },
    "EpisodeLifecycleConfig": {
      "additionalProperties": false,
      "description": "Episode-lifecycle settings.\n\nThe ``EpisodeLifecycleWorker`` drains the ``episodes`` table by\ndropping low-value rows and LLM-compacting clusters of similar\nroutine turns into single ``kind='compacted'`` summaries.  Trigger\nis write-side (``EpisodeStore.write`` publishes\n``CompactionRequested`` once an agent's raw count exceeds\n``max_raw_episodes_per_agent``); no daily cron, no caller-path\nlatency on reads.\n\nSee ``docs/concepts/agent-learning.md`` -- episode lifecycle\nsection.",
      "properties": {
        "max_raw_episodes_per_agent": {
          "default": 500,
          "title": "Max Raw Episodes Per Agent",
          "type": "integer"
        },
        "write_check_every_n": {
          "default": 10,
          "title": "Write Check Every N",
          "type": "integer"
        },
        "non_terminal_max_age_days": {
          "default": 14,
          "title": "Non Terminal Max Age Days",
          "type": "integer"
        },
        "consolidated_grace_days": {
          "default": 30,
          "title": "Consolidated Grace Days",
          "type": "integer"
        },
        "compaction_min_age_days": {
          "default": 30,
          "title": "Compaction Min Age Days",
          "type": "integer"
        },
        "compaction_min_cluster_size": {
          "default": 3,
          "title": "Compaction Min Cluster Size",
          "type": "integer"
        },
        "compaction_jaccard_threshold": {
          "default": 0.6,
          "title": "Compaction Jaccard Threshold",
          "type": "number"
        },
        "compaction_batch_size": {
          "default": 200,
          "title": "Compaction Batch Size",
          "type": "integer"
        },
        "compaction_budget_tokens": {
          "default": 4000,
          "title": "Compaction Budget Tokens",
          "type": "integer"
        },
        "exemplar_count": {
          "default": 2,
          "title": "Exemplar Count",
          "type": "integer"
        },
        "compacted_max_age_days": {
          "default": 0,
          "title": "Compacted Max Age Days",
          "type": "integer"
        }
      },
      "title": "EpisodeLifecycleConfig",
      "type": "object"
    },
    "EpisodicConfig": {
      "additionalProperties": false,
      "description": "Episodic-memory settings for the agent-learning subsystem.",
      "properties": {
        "retrieval_limit": {
          "default": 5,
          "title": "Retrieval Limit",
          "type": "integer"
        }
      },
      "title": "EpisodicConfig",
      "type": "object"
    },
    "GitHubConfig": {
      "additionalProperties": false,
      "description": "Organisation-level GitHub integration configuration \u2014 webhook side.\n\nThis section is **non-tool config only**: it enables inbound GitHub\nwebhook handling (``webhook_secret``) and the per-agent GitHub\nidentity registration used to route those webhooks.\n\nThe GitHub *tool* server (the remote Copilot MCP at\n``https://api.githubcopilot.com/mcp/``) is declared in\n``mcp_servers`` as a ``shared: false`` ``http`` server.  Each agent\nsupplies its token in ``role.mcp_env.github`` as an ``Authorization:\nBearer <token>`` header (the per-role ``http`` overrides become\nrequest headers).  See ``docs/concepts/tool-capabilities.md``.\n\nExample YAML::\n\n    integrations:\n      github:\n        enabled: true\n        webhook_secret: \"${GITHUB_WEBHOOK_SECRET}\"\n\n    mcp_servers:\n      - name: github\n        transport: http\n        shared: false\n        url: \"https://api.githubcopilot.com/mcp/\"\n\n    roles:\n      - name: Senior Engineer\n        mcp_env:\n          github:\n            Authorization: \"Bearer ${GITHUB_TOKEN_ENGINEER}\"",
      "properties": {
        "enabled": {
          "default": false,
          "title": "Enabled",
          "type": "boolean"
        },
        "webhook_secret": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Webhook Secret"
        }
      },
      "title": "GitHubConfig",
      "type": "object"
    },
    "GitLabConfig": {
      "additionalProperties": false,
      "description": "Organisation-level GitLab integration configuration \u2014 webhook side.\n\nSymmetric with :class:`GitHubConfig`, with two differences: ``url``\nis **required** (webhook links, boot-time identity resolution via\n``GET {url}/api/v4/user``, and provisioning all need the instance\naddress), and inbound webhooks are verified by the GitLab **signing\ntoken** \u2014 a 19.1+ Standard-Webhooks HMAC over\n``{webhook-id}.{webhook-timestamp}.{body}`` \u2014 so ``signing_secret``\nis **required** when enabled. (The weaker plain ``X-Gitlab-Token``\nscheme is intentionally unsupported; gitlab.com always runs \u2265 19.1\nand the docker-compose test instance runs ``gitlab-ee:latest``, so\nthe signing token is always available. Self-managed GitLab older\nthan 19.1 is not supported.)\n\nThe GitLab *tool* server is declared in ``mcp_servers`` as a\n``shared: false`` server; each agent supplies its service-account PAT\nin ``role.mcp_env.gitlab``. The default is the official ``glab`` CLI's\nstdio MCP server (``glab mcp serve``), with the token in the\n``GITLAB_TOKEN`` env var \u2014 the engine's bridge spawns it per role, so\nthere is no separate server to run. See ``docs/integrations/gitlab.md``.\n\nExample YAML::\n\n    integrations:\n      gitlab:\n        enabled: true\n        url: \"https://gitlab.com\"\n        signing_secret: \"${GITLAB_SIGNING_SECRET}\"\n        provisioning:\n          group: nimbus-hq\n          access_level: developer\n\n    mcp_servers:\n      - name: gitlab                       # official glab CLI MCP server (stdio)\n        shared: false\n        command: glab\n        args: [\"mcp\", \"serve\"]\n\n    roles:\n      - name: Agent SWE\n        mcp_env:\n          gitlab:\n            GITLAB_TOKEN: \"${GITLAB_TOKEN_SWE}\"\n            GITLAB_HOST: gitlab.com",
      "properties": {
        "enabled": {
          "default": false,
          "title": "Enabled",
          "type": "boolean"
        },
        "url": {
          "default": "",
          "title": "Url",
          "type": "string"
        },
        "signing_secret": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Signing Secret"
        },
        "token": {
          "default": "",
          "title": "Token",
          "type": "string"
        },
        "provisioning": {
          "anyOf": [
            {
              "$ref": "#/$defs/GitLabProvisioningConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        }
      },
      "title": "GitLabConfig",
      "type": "object"
    },
    "GitLabProvisioningConfig": {
      "additionalProperties": false,
      "description": "Inputs for ``crewlet gitlab provision`` \u2014 consumed by the CLI only.\n\nThe engine never reads this block: it drives the reconcile that\ncreates one service account per agent seat, adds group/project\nmembership, mints per-agent PATs from the config's own ``${VAR}``\nreferences, and registers project webhooks. See\n``docs/integrations/gitlab.md``.",
      "properties": {
        "group": {
          "default": "",
          "title": "Group",
          "type": "string"
        },
        "access_level": {
          "default": "developer",
          "title": "Access Level",
          "type": "string"
        },
        "access_levels": {
          "additionalProperties": {
            "type": "string"
          },
          "title": "Access Levels",
          "type": "object"
        },
        "username_prefix": {
          "default": "",
          "title": "Username Prefix",
          "type": "string"
        },
        "projects": {
          "items": {
            "type": "string"
          },
          "title": "Projects",
          "type": "array"
        },
        "group_webhook": {
          "default": "auto",
          "title": "Group Webhook",
          "type": "string"
        },
        "token_scopes": {
          "items": {
            "type": "string"
          },
          "title": "Token Scopes",
          "type": "array"
        }
      },
      "title": "GitLabProvisioningConfig",
      "type": "object"
    },
    "HumanContact": {
      "additionalProperties": false,
      "description": "External identities for reaching a human seat.\n\nAgents reach humans through the same surfaces they use with each\nother (Slack mentions, Jira comments, Confluence mentions, GitHub\nreviews); these identifiers let prompts and the colleague-surface\ndispatcher address the right account.\n\nEvery field accepts either a literal ID or exactly one whole-value\n``${VAR}`` environment reference (e.g.\n``plane_user_id: \"${PLANE_FOUNDER_USER_ID}\"`` \u2014 instance-specific\nIDs cannot ship in a config file).  Values are whitespace-stripped\nat validation; references are stored verbatim and resolved from the\nprocess environment at consumption time via\n:meth:`resolved_identities`.  A value that merely *embeds* a\n``${VAR}`` inside a longer string (``\"acme-${SUFFIX}\"``) is rejected\nat validation \u2014 half-substituting it would silently register a wrong\nidentity.",
      "properties": {
        "slack_user_id": {
          "default": "",
          "title": "Slack User Id",
          "type": "string"
        },
        "atlassian_account_id": {
          "default": "",
          "title": "Atlassian Account Id",
          "type": "string"
        },
        "github_login": {
          "default": "",
          "title": "Github Login",
          "type": "string"
        },
        "gitlab_username": {
          "default": "",
          "title": "Gitlab Username",
          "type": "string"
        },
        "plane_user_id": {
          "default": "",
          "title": "Plane User Id",
          "type": "string"
        }
      },
      "title": "HumanContact",
      "type": "object"
    },
    "IntegrationsConfig": {
      "additionalProperties": false,
      "description": "External-system integrations \u2014 the inbound / notification side.\n\nGroups the **non-tool** integration config: the Atlassian (Jira +\nConfluence) admin accounts used for webhook routing and watcher\nlookups, the Slack transport marker, the GitHub / GitLab / Plane\nwebhook configs, the Forge app id, and the outbound notification\n``transports`` list.\n\nAgent *tools* are configured separately under\n:attr:`CompanyConfig.mcp_servers`; this block is purely how external\nevents reach agents and how notifications are delivered.  It is the\ninbound counterpart to ``mcp_servers`` (outbound tool actions).",
      "properties": {
        "jira": {
          "anyOf": [
            {
              "$ref": "#/$defs/JiraConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "confluence": {
          "anyOf": [
            {
              "$ref": "#/$defs/ConfluenceConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "slack": {
          "anyOf": [
            {
              "$ref": "#/$defs/SlackConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "github": {
          "anyOf": [
            {
              "$ref": "#/$defs/GitHubConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "gitlab": {
          "anyOf": [
            {
              "$ref": "#/$defs/GitLabConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "plane": {
          "anyOf": [
            {
              "$ref": "#/$defs/PlaneConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "forge_app_id": {
          "default": "",
          "title": "Forge App Id",
          "type": "string"
        }
      },
      "title": "IntegrationsConfig",
      "type": "object"
    },
    "JiraConfig": {
      "additionalProperties": false,
      "description": "Organisation-level Jira configuration.\n\nDefines the admin / service account used for org-wide Jira\nAPI access.  This is **not** a per-agent identity \u2014 individual\nagents authenticate via their own ``mcp_env`` credentials.\n\nThe admin account is used for:\n- Fetching ticket watchers for routing decisions\n- (Future) Provisioning per-role Jira service accounts\n\nProvide **either** ``url`` (direct instance URL) **or**\n``cloud_id`` (Atlassian Cloud ID \u2014 the gateway URL is built\nautomatically).  Providing both or neither is an error.\n\nThis section is **non-tool config only** \u2014 admin credentials for\nthe org-level REST calls above plus the inbound webhook secret.  It\nlives under the top-level ``integrations`` block.  The Jira/Confluence\nMCP *tool* server is declared separately in ``mcp_servers`` (name it\n``atlassian``); see ``docs/concepts/tool-capabilities.md``.\n\nExample YAML::\n\n    integrations:\n      jira:\n        url: \"https://mycompany.atlassian.net\"\n        token: \"${JIRA_ADMIN_TOKEN}\"\n        email: \"${JIRA_ADMIN_EMAIL}\"\n        webhook_secret: \"${JIRA_WEBHOOK_SECRET}\"\n\n    mcp_servers:\n      - name: atlassian\n        shared: false\n        command: uvx\n        args: [\"mcp-atlassian\"]\n        env: { JIRA_URL: \"https://mycompany.atlassian.net\" }",
      "properties": {
        "url": {
          "default": "",
          "title": "Url",
          "type": "string"
        },
        "cloud_id": {
          "default": "",
          "title": "Cloud Id",
          "type": "string"
        },
        "token": {
          "title": "Token",
          "type": "string"
        },
        "email": {
          "default": "",
          "title": "Email",
          "type": "string"
        },
        "webhook_secret": {
          "default": "",
          "title": "Webhook Secret",
          "type": "string"
        }
      },
      "required": [
        "token"
      ],
      "title": "JiraConfig",
      "type": "object"
    },
    "JiraIdentity": {
      "additionalProperties": false,
      "description": "The Jira project a unit / root-role owns.\n\nUsed for inbound webhook routing \u2014 project activity with no better\nrecipient routes to the unit lead (:func:`build_project_key_lead_map`)\n\u2014 and as the project agents file work under.  Not a tool credential\n(the Jira MCP server authenticates from ``mcp_env.atlassian`` and\nscopes via its own ``JIRA_PROJECTS_FILTER``) and irrelevant to\nknowledge reads.",
      "properties": {
        "project": {
          "default": "",
          "title": "Project",
          "type": "string"
        }
      },
      "title": "JiraIdentity",
      "type": "object"
    },
    "KnowledgeConfig": {
      "additionalProperties": false,
      "description": "Shared-knowledge config (Tier B).\n\nCurrently the org-wide Confluence spaces every agent can search (the\nPlan-phase ``## Relevant knowledge`` prefetch scope).  Distinct from\nthe Tier A :class:`KnowledgeStoreConfig` (the vector-store backend\nbound to the database).",
      "properties": {
        "confluence_spaces": {
          "items": {
            "type": "string"
          },
          "title": "Confluence Spaces",
          "type": "array"
        },
        "plane_projects": {
          "items": {
            "type": "string"
          },
          "title": "Plane Projects",
          "type": "array"
        }
      },
      "title": "KnowledgeConfig",
      "type": "object"
    },
    "LLMProviderConfig": {
      "additionalProperties": false,
      "description": "Configuration for an LLM provider.",
      "properties": {
        "type": {
          "default": "openai",
          "enum": [
            "openai",
            "anthropic",
            "openai-compatible"
          ],
          "title": "Type",
          "type": "string"
        },
        "model": {
          "default": "gpt-4o",
          "title": "Model",
          "type": "string"
        },
        "api_keys": {
          "items": {
            "type": "string"
          },
          "title": "Api Keys",
          "type": "array"
        },
        "cooldowns": {
          "$ref": "#/$defs/CredentialCooldownConfig"
        },
        "base_url": {
          "default": "",
          "title": "Base Url",
          "type": "string"
        },
        "reasoning": {
          "default": false,
          "title": "Reasoning",
          "type": "boolean"
        },
        "reasoning_effort": {
          "default": "medium",
          "enum": [
            "low",
            "medium",
            "high",
            "max"
          ],
          "title": "Reasoning Effort",
          "type": "string"
        },
        "reasoning_budget_tokens": {
          "default": 10000,
          "title": "Reasoning Budget Tokens",
          "type": "integer"
        },
        "timeout_seconds": {
          "default": 120.0,
          "title": "Timeout Seconds",
          "type": "number"
        }
      },
      "title": "LLMProviderConfig",
      "type": "object"
    },
    "LearningConfig": {
      "additionalProperties": false,
      "description": "Configuration for the agent-learning subsystem.\n\nSee ``docs/concepts/agent-learning.md`` for the full design.\nRequires a configured database + embedding provider; when either\nis missing the engine logs a disabled-notice and skips registration\nof learning tools.",
      "properties": {
        "enabled": {
          "default": true,
          "title": "Enabled",
          "type": "boolean"
        },
        "episodic": {
          "$ref": "#/$defs/EpisodicConfig",
          "default": {
            "retrieval_limit": 5
          }
        },
        "reflect": {
          "$ref": "#/$defs/ReflectConfig",
          "default": {
            "enabled": true,
            "persist_decider": true,
            "budget_tokens": 5000,
            "summarize_episodes": true,
            "summarize_max_tokens": 400
          }
        },
        "counterparty": {
          "$ref": "#/$defs/CounterpartyConfig",
          "default": {
            "enabled": true,
            "budget_tokens": 3000
          }
        },
        "skill_synthesis": {
          "$ref": "#/$defs/SkillSynthesisConfig",
          "default": {
            "enabled": true,
            "min_tool_calls": 5,
            "budget_tokens": 4000,
            "max_skills_per_agent": 50,
            "duplicate_jaccard_threshold": 0.7,
            "scheduler_enabled": false,
            "scheduler_interval_seconds": 3600,
            "cluster_window_hours": 168,
            "cluster_min_size": 3,
            "cluster_jaccard_threshold": 0.6,
            "episode_fetch_limit": 200
          }
        },
        "skill_refinement": {
          "$ref": "#/$defs/SkillRefinementConfig",
          "default": {
            "enabled": true,
            "auto_refine_on_success": true,
            "auto_refine_on_failure": true,
            "budget_tokens": 3000,
            "max_body_chars": 20000,
            "max_versions_kept": 10
          }
        },
        "skill_promotion": {
          "$ref": "#/$defs/SkillPromotionConfig",
          "default": {
            "enabled": true,
            "min_sibling_count": 3,
            "jaccard_threshold": 0.6,
            "budget_tokens": 4000
          }
        },
        "skill_curator": {
          "$ref": "#/$defs/SkillCuratorConfig",
          "default": {
            "enabled": true,
            "interval_hours": 24,
            "stale_after_days": 30,
            "archive_after_days": 90
          }
        },
        "personal_memory": {
          "$ref": "#/$defs/PersonalMemoryConfig",
          "default": {
            "max_refreshes_per_turn": 3
          }
        },
        "episode_lifecycle": {
          "$ref": "#/$defs/EpisodeLifecycleConfig",
          "default": {
            "max_raw_episodes_per_agent": 500,
            "write_check_every_n": 10,
            "non_terminal_max_age_days": 14,
            "consolidated_grace_days": 30,
            "compaction_min_age_days": 30,
            "compaction_min_cluster_size": 3,
            "compaction_jaccard_threshold": 0.6,
            "compaction_batch_size": 200,
            "compaction_budget_tokens": 4000,
            "exemplar_count": 2,
            "compacted_max_age_days": 0
          }
        }
      },
      "title": "LearningConfig",
      "type": "object"
    },
    "MCPServerConfig": {
      "additionalProperties": false,
      "description": "Configuration for an MCP server integration.\n\nSupports two transports:\n- ``stdio`` (default): launches a local subprocess\n- ``http``: connects to a remote Streamable HTTP server\n\nWhen ``shared`` is True (default), a single global server instance\nis launched and its tools are available to all agents.  When False,\nthe config is used only as a **template** for per-role instances\n(spawned from ``role.mcp_env`` overrides).  No global instance is\nlaunched.  Use ``shared: false`` for identity-bound servers where\neach agent needs its own credentials.\n\nPer-role instances work for **both** transports.  For each role that\ndeclares ``role.mcp_env[name]``, the engine launches a dedicated\ninstance and applies the role's overrides as **environment\nvariables** (``stdio``) or **HTTP headers** (``http``).  An ``http``\nserver with ``shared: false`` is therefore how an identity-bound\nremote server (e.g. the GitHub Copilot MCP) gets a per-agent token.\n\n**All tool servers are configured here** \u2014 including Jira/Confluence\n(name them ``atlassian``), Slack, and GitHub.  The\n``integrations.jira`` / ``.confluence`` / ``.slack`` / ``.github``\nsections carry only non-tool config (admin credentials, webhook\nsecrets); the engine never derives MCP servers from them.  See\n``docs/concepts/tool-capabilities.md``.\n\nExample YAML::\n\n    mcp_servers:\n      # stdio, shared by all agents\n      - name: calculator\n        command: uvx\n        args: [\"mcp-calculator\"]\n      # stdio, per-role (token from role.mcp_env.atlassian)\n      - name: atlassian\n        shared: false\n        command: uvx\n        args: [\"mcp-atlassian\"]\n        env: { JIRA_URL: \"https://co.atlassian.net\" }\n      # http, per-role (Authorization header from role.mcp_env.github)\n      - name: github\n        transport: http\n        shared: false\n        url: \"https://api.githubcopilot.com/mcp/\"",
      "properties": {
        "name": {
          "minLength": 1,
          "title": "Name",
          "type": "string"
        },
        "transport": {
          "default": "stdio",
          "title": "Transport",
          "type": "string"
        },
        "shared": {
          "default": true,
          "title": "Shared",
          "type": "boolean"
        },
        "command": {
          "default": "",
          "title": "Command",
          "type": "string"
        },
        "args": {
          "items": {
            "type": "string"
          },
          "title": "Args",
          "type": "array"
        },
        "env": {
          "additionalProperties": {
            "type": "string"
          },
          "title": "Env",
          "type": "object"
        },
        "tool_prefix": {
          "default": "",
          "title": "Tool Prefix",
          "type": "string"
        },
        "url": {
          "default": "",
          "title": "Url",
          "type": "string"
        },
        "headers": {
          "additionalProperties": {
            "type": "string"
          },
          "title": "Headers",
          "type": "object"
        },
        "tool_annotations": {
          "additionalProperties": {
            "additionalProperties": true,
            "type": "object"
          },
          "title": "Tool Annotations",
          "type": "object"
        }
      },
      "required": [
        "name"
      ],
      "title": "MCPServerConfig",
      "type": "object"
    },
    "OrgUnitConfig": {
      "additionalProperties": false,
      "description": "The authored YAML shape of one ``units:`` entry (recursive).\n\nTransformed into an :class:`~crewlet.org.models.OrgUnit` by\n:func:`_parse_unit`.  Like :class:`RoleConfig` this forbids extra\nkeys so a mistyped field fails validation rather than vanishing.",
      "properties": {
        "name": {
          "title": "Name",
          "type": "string"
        },
        "type": {
          "default": "team",
          "title": "Type",
          "type": "string"
        },
        "purpose": {
          "default": "",
          "title": "Purpose",
          "type": "string"
        },
        "lead": {
          "default": "",
          "title": "Lead",
          "type": "string"
        },
        "goals": {
          "items": {
            "type": "string"
          },
          "title": "Goals",
          "type": "array"
        },
        "slack_channel": {
          "default": "",
          "title": "Slack Channel",
          "type": "string"
        },
        "knowledge": {
          "items": {
            "type": "string"
          },
          "title": "Knowledge",
          "type": "array"
        },
        "mcp_env": {
          "additionalProperties": {
            "additionalProperties": {
              "type": "string"
            },
            "type": "object"
          },
          "title": "Mcp Env",
          "type": "object"
        },
        "integrations": {
          "$ref": "#/$defs/UnitIntegrationsConfig"
        },
        "roles": {
          "items": {
            "$ref": "#/$defs/RoleConfig"
          },
          "title": "Roles",
          "type": "array"
        },
        "children": {
          "items": {
            "$ref": "#/$defs/OrgUnitConfig"
          },
          "title": "Children",
          "type": "array"
        },
        "schedules": {
          "items": {
            "$ref": "#/$defs/Schedule"
          },
          "title": "Schedules",
          "type": "array"
        }
      },
      "required": [
        "name"
      ],
      "title": "OrgUnitConfig",
      "type": "object"
    },
    "PersonalMemoryConfig": {
      "additionalProperties": false,
      "description": "Personal-memory read-side settings.\n\nControls the Plan-phase ``## Personal memory`` digest and the\nLLM-callable ``refresh_memory`` tool.  Memory writes are governed\nby the ``reflect`` block above (PersistDecider) and the\n``reflect_and_persist`` builtin -- they are not affected by these\nfields.",
      "properties": {
        "max_refreshes_per_turn": {
          "default": 3,
          "title": "Max Refreshes Per Turn",
          "type": "integer"
        }
      },
      "title": "PersonalMemoryConfig",
      "type": "object"
    },
    "PlaneConfig": {
      "additionalProperties": false,
      "description": "Organisation-level Plane integration \u2014 webhook + engine-read side.\n\nSymmetric with :class:`GitLabConfig`: ``enabled`` plus a required\n``url`` when enabled, joined by ``workspace`` (every resource path the\nintegration operates on is workspace-scoped \u2014 only ``GET /users/me/``\nand workspace creation aren't) and ``webhook_secret`` (the\n``X-Plane-Signature`` HMAC-SHA256 key \u2014 the only verification mode\nPlane offers; generated *by* Plane at webhook creation and captured by\nthe provisioner).\n\nThe Plane *tool* server is declared in ``mcp_servers`` as a\n``shared: false`` server; each agent supplies its service-account\ntoken in ``role.mcp_env.plane``.  See\n``docs/integrations/plane.md``.\n\nExample YAML::\n\n    integrations:\n      plane:\n        enabled: true\n        url: \"https://plane.nimbus.example\"      # fork instance base URL (required)\n        workspace: nimbus                        # workspace slug (required)\n        webhook_secret: \"${PLANE_WEBHOOK_SECRET}\"  # X-Plane-Signature HMAC key \u2014\n                                                 #   generated BY Plane at webhook\n                                                 #   creation; provisioner captures\n                                                 #   it into this ${VAR}\n        token: \"${PLANE_ENGINE_TOKEN}\"           # engine read credential\n                                                 #   (subscribers, members, project\n                                                 #   map, page fetch) \u2014 provisioned\n                                                 #   crewlet-engine service account\n        provisioning:            # consumed ONLY by the CLI, ignored by\n                                 #   the engine\n          role: member           # workspace role for agent accounts\n                                 #   (admin|member|guest)\n          roles: { tech-lead: admin }      # per-handle overrides\n          username_prefix: \"\"    # e.g. \"agent-\" if the workspace is shared\n          projects: [LEAD, ENG, PROD, TS]  # project identifiers: membership\n                                           #   targets (+ created with\n                                           #   --create-projects)\n          token_expiry_days: 364 # omit expiry with 0 (Plane tokens:\n                                 #   optional expiry)\n\n    mcp_servers:\n      - name: plane                       # official plane-mcp-server, stdio\n        shared: false\n        command: uvx\n        args: [\"plane-mcp-server\", \"stdio\"]\n        env:\n          PLANE_BASE_URL: \"https://plane.nimbus.example\"\n          PLANE_WORKSPACE_SLUG: \"nimbus\"\n\n    roles:\n      - name: Agent SWE\n        mcp_env:\n          plane:\n            PLANE_API_KEY: \"${PLANE_TOKEN_SWE}\"   # per-agent service-account token",
      "properties": {
        "enabled": {
          "default": false,
          "title": "Enabled",
          "type": "boolean"
        },
        "url": {
          "default": "",
          "title": "Url",
          "type": "string"
        },
        "workspace": {
          "default": "",
          "title": "Workspace",
          "type": "string"
        },
        "webhook_secret": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Webhook Secret"
        },
        "token": {
          "default": "",
          "title": "Token",
          "type": "string"
        },
        "provisioning": {
          "anyOf": [
            {
              "$ref": "#/$defs/PlaneProvisioningConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        }
      },
      "title": "PlaneConfig",
      "type": "object"
    },
    "PlaneIdentity": {
      "additionalProperties": false,
      "description": "The Plane project a unit / root-role owns.\n\nIntegration identity only \u2014 the inbound webhook fallback-routing\ntarget (:func:`build_plane_project_lead_map`) and the project agents\nfile work under.  Not a tool credential (the Plane MCP server\nauthenticates from ``mcp_env.plane``), and it does **not** scope\nknowledge reads (read scope is the org-wide\n:attr:`KnowledgeConfig.plane_projects` only).",
      "properties": {
        "project": {
          "default": "",
          "title": "Project",
          "type": "string"
        }
      },
      "title": "PlaneIdentity",
      "type": "object"
    },
    "PlaneProvisioningConfig": {
      "additionalProperties": false,
      "description": "Inputs for ``crewlet plane provision`` \u2014 consumed by the CLI only\n(slice 4).  The engine never reads this block; it must parse so a\nprovisioning-ready config validates today.",
      "properties": {
        "role": {
          "default": "member",
          "title": "Role",
          "type": "string"
        },
        "roles": {
          "additionalProperties": {
            "type": "string"
          },
          "title": "Roles",
          "type": "object"
        },
        "username_prefix": {
          "default": "",
          "title": "Username Prefix",
          "type": "string"
        },
        "projects": {
          "items": {
            "type": "string"
          },
          "title": "Projects",
          "type": "array"
        },
        "token_expiry_days": {
          "default": 364,
          "minimum": 0,
          "title": "Token Expiry Days",
          "type": "integer"
        }
      },
      "title": "PlaneProvisioningConfig",
      "type": "object"
    },
    "ProvidersConfig": {
      "additionalProperties": false,
      "description": "Tier B provider settings \u2014 LLM + embeddings.\n\nTier A providers (queue, database, knowledge) live on\n:class:`BootstrapProvidersConfig` and load from ``config.yaml`` at\nengine startup.",
      "properties": {
        "llm": {
          "additionalProperties": {
            "$ref": "#/$defs/LLMProviderConfig"
          },
          "title": "Llm",
          "type": "object"
        },
        "embeddings": {
          "anyOf": [
            {
              "$ref": "#/$defs/EmbeddingProviderConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "sandbox": {
          "anyOf": [
            {
              "$ref": "#/$defs/SandboxProviderConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        }
      },
      "title": "ProvidersConfig",
      "type": "object"
    },
    "ReflectConfig": {
      "additionalProperties": false,
      "description": "Reflection/PersistDecider settings.\n\nRuns after every turn to decide whether any durable fact from the\nturn should be persisted to the knowledge store.  Enabled by\ndefault; the on-event dispatcher subscribes only when\n``enabled=True``.",
      "properties": {
        "enabled": {
          "default": true,
          "title": "Enabled",
          "type": "boolean"
        },
        "persist_decider": {
          "default": true,
          "title": "Persist Decider",
          "type": "boolean"
        },
        "budget_tokens": {
          "default": 5000,
          "title": "Budget Tokens",
          "type": "integer"
        },
        "summarize_episodes": {
          "default": true,
          "title": "Summarize Episodes",
          "type": "boolean"
        },
        "summarize_max_tokens": {
          "default": 400,
          "title": "Summarize Max Tokens",
          "type": "integer"
        }
      },
      "title": "ReflectConfig",
      "type": "object"
    },
    "RoleConfig": {
      "additionalProperties": false,
      "description": "The authored YAML shape of one seat \u2014 a ``roles:`` entry.\n\nThis is the *wire format* a founder writes, not the runtime model:\n:func:`_parse_role` transforms it into a\n:class:`~crewlet.org.models.Role` (materialising\n``integrations.slack`` into ``Role.slack``, collapsing the ``llm``\ndict form into per-phase fields, and mapping the integration\nidentities onto ``jira_project`` / ``confluence_space`` /\n``plane_project``).\n\n``extra=\"forbid\"``: an unrecognised key here is a typo, and a\nsilently-ignored ``backstroy:`` would spawn the seat with an empty\nbackstory and no diagnostic.  Fields are rejected at\n``crewlet validate`` time instead.",
      "if": {
        "properties": {
          "kind": {
            "const": "human"
          }
        },
        "required": [
          "kind"
        ]
      },
      "properties": {
        "name": {
          "title": "Name",
          "type": "string"
        },
        "kind": {
          "$ref": "#/$defs/RoleKind",
          "default": "agent"
        },
        "contact": {
          "anyOf": [
            {
              "$ref": "#/$defs/HumanContact"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "availability": {
          "default": "",
          "title": "Availability",
          "type": "string"
        },
        "handle": {
          "default": "",
          "pattern": "^$|^[a-z0-9][a-z0-9-]*$",
          "title": "Handle",
          "type": "string"
        },
        "email": {
          "default": "",
          "title": "Email",
          "type": "string"
        },
        "unit": {
          "default": "",
          "title": "Unit",
          "type": "string"
        },
        "goal": {
          "default": "",
          "title": "Goal",
          "type": "string"
        },
        "backstory": {
          "default": "",
          "title": "Backstory",
          "type": "string"
        },
        "responsibilities": {
          "items": {
            "type": "string"
          },
          "title": "Responsibilities",
          "type": "array"
        },
        "manages": {
          "items": {
            "type": "string"
          },
          "title": "Manages",
          "type": "array"
        },
        "behavioral_guidelines": {
          "items": {
            "type": "string"
          },
          "title": "Behavioral Guidelines",
          "type": "array"
        },
        "token_budget": {
          "default": 0,
          "minimum": 0,
          "title": "Token Budget",
          "type": "integer"
        },
        "llm": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "$ref": "#/$defs/RolePhaseLLMConfig"
            }
          ],
          "default": "default",
          "title": "Llm"
        },
        "llm_plan": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Llm Plan"
        },
        "llm_execute": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Llm Execute"
        },
        "llm_review": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Llm Review"
        },
        "llm_subagent": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Llm Subagent"
        },
        "llm_auxiliary": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Llm Auxiliary"
        },
        "llm_judge": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Llm Judge"
        },
        "llm_sandbox": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Llm Sandbox"
        },
        "learning_enabled": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Learning Enabled"
        },
        "mcp_env": {
          "additionalProperties": {
            "additionalProperties": {
              "type": "string"
            },
            "type": "object"
          },
          "title": "Mcp Env",
          "type": "object"
        },
        "sandbox": {
          "anyOf": [
            {
              "$ref": "#/$defs/RoleSandboxConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "integrations": {
          "$ref": "#/$defs/RoleIntegrationsConfig"
        },
        "schedules": {
          "items": {
            "$ref": "#/$defs/Schedule"
          },
          "title": "Schedules",
          "type": "array"
        }
      },
      "required": [
        "name"
      ],
      "then": {
        "$comment": "A human seat is addressable-only and needs at least one contact identity.",
        "required": [
          "contact"
        ]
      },
      "title": "RoleConfig",
      "type": "object"
    },
    "RoleIntegrationsConfig": {
      "additionalProperties": false,
      "description": "Per-agent integration identity \u2014 the inbound / transport side.\n\nThe per-agent counterpart to the org-level ``integrations`` block:\nwhere an agent's *non-tool* identity for an external system lives \u2014\nthe Slack transport app (``slack``) and, for root-level roles, the\nJira project / Confluence space / Plane project the role owns for\nwebhook routing (``jira`` / ``confluence`` / ``plane``; units carry\nthese too \u2014 see :class:`UnitIntegrationsConfig`).\n\nPer-agent *tool* credentials are configured separately under\n``role.mcp_env`` (Atlassian / GitHub / Plane tokens, the Slack MCP's\n``SLACK_MCP_XOXB_TOKEN``) \u2014 those go straight to the MCP server that\nconsumes them.  The ``jira`` / ``confluence`` / ``plane`` identity\nhere is **not** a tool credential and **does not** scope knowledge\nreads (read scope is the org-wide\n:attr:`KnowledgeConfig.confluence_spaces` /\n:attr:`KnowledgeConfig.plane_projects` only).",
      "properties": {
        "slack": {
          "anyOf": [
            {
              "$ref": "#/$defs/RoleSlackIdentity"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "jira": {
          "anyOf": [
            {
              "$ref": "#/$defs/JiraIdentity"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "confluence": {
          "anyOf": [
            {
              "$ref": "#/$defs/ConfluenceIdentity"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "plane": {
          "anyOf": [
            {
              "$ref": "#/$defs/PlaneIdentity"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        }
      },
      "title": "RoleIntegrationsConfig",
      "type": "object"
    },
    "RoleKind": {
      "description": "What kind of individual holds a seat in the org chart.",
      "enum": [
        "agent",
        "human"
      ],
      "title": "RoleKind",
      "type": "string"
    },
    "RolePhaseLLMConfig": {
      "additionalProperties": false,
      "description": "Dict form of ``role.llm`` \u2014 one provider key per turn phase.\n\nThe scalar form (``llm: fast``) points every phase at one named\nprovider.  This mapping form splits them, with any omitted phase\nfalling back to :attr:`default`.  Equivalent to the flat\n``llm_plan`` / ``llm_execute`` / \u2026 fields on :class:`RoleConfig`;\nwhen both are given the flat field wins.  See\n``docs/concepts/turn-engine.md`` \u00a7 Per-phase LLM models.",
      "properties": {
        "default": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          ],
          "default": "default",
          "title": "Default"
        },
        "plan": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Plan"
        },
        "execute": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Execute"
        },
        "review": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Review"
        },
        "subagent": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Subagent"
        },
        "auxiliary": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Auxiliary"
        },
        "judge": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Judge"
        },
        "sandbox": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Sandbox"
        }
      },
      "title": "RolePhaseLLMConfig",
      "type": "object"
    },
    "RoleSandboxConfig": {
      "additionalProperties": false,
      "description": "Per-role code-runtime gate (``role.sandbox``).\n\nAbsent \u2192 the role never sees the sandbox option: the\n``run_sandbox`` Execute tool is not offered to it.",
      "properties": {
        "enabled": {
          "default": false,
          "title": "Enabled",
          "type": "boolean"
        },
        "coding_agent": {
          "default": "",
          "enum": [
            "",
            "claude-code",
            "opencode"
          ],
          "title": "Coding Agent",
          "type": "string"
        },
        "pause_ttl_seconds": {
          "default": -1.0,
          "title": "Pause Ttl Seconds",
          "type": "number"
        },
        "mcp": {
          "$ref": "#/$defs/RoleSandboxMCPConfig"
        },
        "env": {
          "additionalProperties": {
            "type": "string"
          },
          "title": "Env",
          "type": "object"
        },
        "setup": {
          "items": {
            "$ref": "#/$defs/SandboxSetupStep"
          },
          "title": "Setup",
          "type": "array"
        }
      },
      "title": "RoleSandboxConfig",
      "type": "object"
    },
    "RoleSandboxMCPConfig": {
      "additionalProperties": false,
      "description": "Which of the role's MCP servers to render into the sandbox.\n\nScoping is at the **server** level only. There is no per-tool allowlist:\nthe coding agent gets every tool the configured servers expose. OpenCode\nhas no allowlist flag, and Claude Code already runs ``bypassPermissions``\nheadless, so a curated allowlist couldn't be enforced uniformly \u2014 we let\neach platform deal with the servers it's given.",
      "properties": {
        "servers": {
          "items": {
            "type": "string"
          },
          "title": "Servers",
          "type": "array"
        }
      },
      "title": "RoleSandboxMCPConfig",
      "type": "object"
    },
    "RoleSlackIdentity": {
      "additionalProperties": false,
      "description": "Per-agent Slack *transport* identity (one Slack app per agent).\n\nThe credentials the :class:`SlackTransport` uses for this agent \u2014 its\nbot token (outbound Web API + ``send()`` fallback), signing secret\n(inbound webhook signature verification), and optional default\nchannel.  This is **not** the Slack MCP credential: the Slack MCP\nserver reads its own ``SLACK_MCP_XOXB_TOKEN`` from\n``role.mcp_env.slack`` \u2014 name the same ``${VAR}`` in both (two\nconsumers, one secret).",
      "properties": {
        "bot_token": {
          "default": "",
          "title": "Bot Token",
          "type": "string"
        },
        "signing_secret": {
          "default": "",
          "title": "Signing Secret",
          "type": "string"
        },
        "channel": {
          "default": "",
          "title": "Channel",
          "type": "string"
        }
      },
      "title": "RoleSlackIdentity",
      "type": "object"
    },
    "SandboxProviderConfig": {
      "additionalProperties": false,
      "description": "Engine-level sandbox provider config (``providers.sandbox``).\n\nDrives the [code sandbox](../concepts/code-sandbox.md): a\nsandbox-enabled role can run real code work as a coding agent\n(Claude Code / OpenCode) inside an isolated sandbox via the\n``run_sandbox`` Execute tool. Sibling of\n``providers.llm`` -- live-reloadable, secrets kept verbatim (``${ENV}``\nreferences) in the DB and resolved at construction time.",
      "properties": {
        "type": {
          "default": "e2b",
          "enum": [
            "e2b",
            "fake",
            "none"
          ],
          "title": "Type",
          "type": "string"
        },
        "api_key": {
          "default": "",
          "title": "Api Key",
          "type": "string"
        },
        "domain": {
          "default": "",
          "title": "Domain",
          "type": "string"
        },
        "template": {
          "default": "",
          "title": "Template",
          "type": "string"
        },
        "default_coding_agent": {
          "default": "claude-code",
          "enum": [
            "claude-code",
            "opencode"
          ],
          "title": "Default Coding Agent",
          "type": "string"
        },
        "default_timeout_seconds": {
          "default": 900.0,
          "exclusiveMinimum": 0,
          "title": "Default Timeout Seconds",
          "type": "number"
        },
        "default_pause_ttl_seconds": {
          "default": 1800.0,
          "minimum": 0,
          "title": "Default Pause Ttl Seconds",
          "type": "number"
        },
        "setup": {
          "items": {
            "$ref": "#/$defs/SandboxSetupStep"
          },
          "title": "Setup",
          "type": "array"
        }
      },
      "title": "SandboxProviderConfig",
      "type": "object"
    },
    "SandboxSetupStep": {
      "additionalProperties": false,
      "description": "One declarative provisioning unit applied to a fresh sandbox.\n\nConfig-loadable (``providers.sandbox.setup`` / ``role.sandbox.setup``);\nevery provisioning concern \u2014 git auth included \u2014 goes through this one\napply / env-merge / brief pipeline with no engine special cases.",
      "properties": {
        "name": {
          "title": "Name",
          "type": "string"
        },
        "files": {
          "additionalProperties": {
            "type": "string"
          },
          "title": "Files",
          "type": "object"
        },
        "commands": {
          "items": {
            "type": "string"
          },
          "title": "Commands",
          "type": "array"
        },
        "env": {
          "additionalProperties": {
            "type": "string"
          },
          "title": "Env",
          "type": "object"
        },
        "brief": {
          "default": "",
          "title": "Brief",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "title": "SandboxSetupStep",
      "type": "object"
    },
    "Schedule": {
      "additionalProperties": false,
      "description": "A recurring task an agent or unit owns, fired on a cron expression.\n\nSchedules live in the YAML org config (``Role.schedules`` and\n``OrgUnit.schedules``) \u2014 one source of truth, hot-reloadable via\n``engine.reload_config()``.  The engine's :class:`~crewlet.schedule.Scheduler`\npublishes a ``TaskAssigned`` to the resolved runner's inbox when a\nschedule is due; the agent runtime path is otherwise unchanged.\n\nSee ``docs/concepts/scheduling.md`` for the full design.",
      "properties": {
        "name": {
          "title": "Name",
          "type": "string"
        },
        "cron": {
          "pattern": "^\\s*\\S+(?:\\s+\\S+){4}\\s*$",
          "title": "Cron",
          "type": "string"
        },
        "task": {
          "title": "Task",
          "type": "string"
        },
        "timezone": {
          "default": "",
          "title": "Timezone",
          "type": "string"
        },
        "target": {
          "default": "each",
          "title": "Target",
          "type": "string"
        },
        "enabled": {
          "default": true,
          "title": "Enabled",
          "type": "boolean"
        },
        "timeout_seconds": {
          "default": 180,
          "title": "Timeout Seconds",
          "type": "integer"
        },
        "catchup": {
          "default": true,
          "title": "Catchup",
          "type": "boolean"
        }
      },
      "required": [
        "name",
        "cron",
        "task"
      ],
      "title": "Schedule",
      "type": "object"
    },
    "SchedulingConfig": {
      "additionalProperties": false,
      "description": "Configuration for the role/unit scheduler.\n\nThe scheduler is auto-enabled when ``enabled`` is true, a database\nis configured (it needs the ``scheduled_runs`` ledger for\nat-most-once delivery), and at least one role or unit declares a\n``schedules:`` block.  Orgs with no schedules never spin up the\ntick loop.\n\nSee ``docs/concepts/scheduling.md``.",
      "properties": {
        "enabled": {
          "default": true,
          "title": "Enabled",
          "type": "boolean"
        },
        "tick_seconds": {
          "default": 10,
          "title": "Tick Seconds",
          "type": "integer"
        },
        "default_timezone": {
          "default": "UTC",
          "title": "Default Timezone",
          "type": "string"
        },
        "jitter_seconds": {
          "default": 0,
          "title": "Jitter Seconds",
          "type": "integer"
        },
        "catchup_min_seconds": {
          "default": 120,
          "title": "Catchup Min Seconds",
          "type": "integer"
        },
        "catchup_max_seconds": {
          "default": 7200,
          "title": "Catchup Max Seconds",
          "type": "integer"
        }
      },
      "title": "SchedulingConfig",
      "type": "object"
    },
    "SkillCuratorConfig": {
      "additionalProperties": false,
      "description": "Skill-curator settings.\n\nThe ``SkillCuratorWorker`` walks ``synthesized_skills`` on an\ninterval and ages out unused skills (active \u2192 stale \u2192 archived).\nPinned skills are exempt. Archived skills are still in the table\n-- restoration is ``UPDATE synthesized_skills SET state='active'``.\n\nThe read-then-archive race against the Plan-phase prefetch is\nclosed in the store: ``transition_state`` only fires the demote\nUPDATE if the row's ``last_used_at`` still matches the curator's\nsnapshot, so no \"safety window\" knob is needed.",
      "properties": {
        "enabled": {
          "default": true,
          "title": "Enabled",
          "type": "boolean"
        },
        "interval_hours": {
          "default": 24,
          "title": "Interval Hours",
          "type": "integer"
        },
        "stale_after_days": {
          "default": 30,
          "title": "Stale After Days",
          "type": "integer"
        },
        "archive_after_days": {
          "default": 90,
          "title": "Archive After Days",
          "type": "integer"
        }
      },
      "title": "SkillCuratorConfig",
      "type": "object"
    },
    "SkillPromotionConfig": {
      "additionalProperties": false,
      "description": "Skill-promotion settings.\n\nAdds a second pass to the SkillClusteringScheduler: for each unit,\ncluster recent agent-scope synthesized skills across siblings by\ntool-sequence Jaccard; when \u2265 ``min_sibling_count`` distinct\nagents share a cluster (and no unit-scope skill already covers\nit), draft a unit-scope skill via the auxiliary model.",
      "properties": {
        "enabled": {
          "default": true,
          "title": "Enabled",
          "type": "boolean"
        },
        "min_sibling_count": {
          "default": 3,
          "title": "Min Sibling Count",
          "type": "integer"
        },
        "jaccard_threshold": {
          "default": 0.6,
          "title": "Jaccard Threshold",
          "type": "number"
        },
        "budget_tokens": {
          "default": 4000,
          "title": "Budget Tokens",
          "type": "integer"
        }
      },
      "title": "SkillPromotionConfig",
      "type": "object"
    },
    "SkillRefinementConfig": {
      "additionalProperties": false,
      "description": "Skill-refinement settings.\n\nRuns as a fourth worker inside the ReflectEngine: whenever a turn\nused one or more synthesized skills, the refiner decides whether\nto append an observation (on success) or a counter-example (on\nfailure) to the most-recently-used match.  Version history is\narchived automatically; rollback works via\n:meth:`SynthesizedSkillStore.rollback_to_version`.",
      "properties": {
        "enabled": {
          "default": true,
          "title": "Enabled",
          "type": "boolean"
        },
        "auto_refine_on_success": {
          "default": true,
          "title": "Auto Refine On Success",
          "type": "boolean"
        },
        "auto_refine_on_failure": {
          "default": true,
          "title": "Auto Refine On Failure",
          "type": "boolean"
        },
        "budget_tokens": {
          "default": 3000,
          "title": "Budget Tokens",
          "type": "integer"
        },
        "max_body_chars": {
          "default": 20000,
          "title": "Max Body Chars",
          "type": "integer"
        },
        "max_versions_kept": {
          "default": 10,
          "title": "Max Versions Kept",
          "type": "integer"
        }
      },
      "title": "SkillRefinementConfig",
      "type": "object"
    },
    "SkillSynthesisConfig": {
      "additionalProperties": false,
      "description": "Skill-synthesis settings.\n\nTwo triggers: single-turn (inline from the ReflectEngine when a\nturn used ``>= min_tool_calls`` tools and finished ``done``) and\nclustered (scheduled tick that groups recent successful turns by\ntool-sequence Jaccard similarity and drafts skills for recurring\npatterns).  Both paths write to the ``synthesized_skills`` table.",
      "properties": {
        "enabled": {
          "default": true,
          "title": "Enabled",
          "type": "boolean"
        },
        "min_tool_calls": {
          "default": 5,
          "title": "Min Tool Calls",
          "type": "integer"
        },
        "budget_tokens": {
          "default": 4000,
          "title": "Budget Tokens",
          "type": "integer"
        },
        "max_skills_per_agent": {
          "default": 50,
          "title": "Max Skills Per Agent",
          "type": "integer"
        },
        "duplicate_jaccard_threshold": {
          "default": 0.7,
          "title": "Duplicate Jaccard Threshold",
          "type": "number"
        },
        "scheduler_enabled": {
          "default": false,
          "title": "Scheduler Enabled",
          "type": "boolean"
        },
        "scheduler_interval_seconds": {
          "default": 3600,
          "title": "Scheduler Interval Seconds",
          "type": "integer"
        },
        "cluster_window_hours": {
          "default": 168,
          "title": "Cluster Window Hours",
          "type": "integer"
        },
        "cluster_min_size": {
          "default": 3,
          "title": "Cluster Min Size",
          "type": "integer"
        },
        "cluster_jaccard_threshold": {
          "default": 0.6,
          "title": "Cluster Jaccard Threshold",
          "type": "number"
        },
        "episode_fetch_limit": {
          "default": 200,
          "title": "Episode Fetch Limit",
          "type": "integer"
        }
      },
      "title": "SkillSynthesisConfig",
      "type": "object"
    },
    "SlackConfig": {
      "additionalProperties": false,
      "description": "Organisation-level Slack configuration.\n\nEvery *credential* is per-agent (``role.integrations.slack``); this\nblock is the transport-enable marker plus the genuinely org-wide\nSlack behaviour \u2014 ``typing_status`` and ``status_phrases``.\nDeclaring this section under the top-level ``integrations`` block\n(even as ``slack: {}``) turns on the outbound\n:class:`SlackTransport` and per-agent webhook routing; omit it to\ndisable Slack entirely.\n\n``typing_status`` controls the \"*<agent> is thinking\u2026*\" working\nindicator an agent shows while it reasons about a Slack message (see\n:mod:`crewlet.notifications.typing_status`):\n\n- ``addressed`` (default) \u2014 only when a human is plausibly waiting on\n  this agent: a DM / group DM, a direct ``@mention``, or a thread the\n  agent already follows.\n- ``always`` \u2014 on every Slack-triggered turn, including passive\n  top-level channel messages and ``@here`` broadcasts.\n- ``off`` \u2014 disabled.\n\n``status_phrases`` replaces the words it shows \u2014 see\n:class:`SlackStatusPhrases`.\n\nThe Slack *tool* server is declared separately in ``mcp_servers``\n(name it ``slack``, ``shared: false``).  A Slack-enabled agent needs\nits bot token in **two** places \u2014 they are two different consumers:\nthe transport identity (``role.integrations.slack``, whose\n``signing_secret`` also verifies inbound webhooks) and the Slack MCP\nsubprocess (``role.mcp_env.slack.SLACK_MCP_XOXB_TOKEN``).  Both\nreference the same ``${VAR}``, so there is no secret duplication.\nSee ``docs/concepts/tool-capabilities.md``.\n\nExample YAML::\n\n    integrations:\n      slack:\n        typing_status: addressed   # addressed | always | off\n        status_phrases:            # optional \u2014 built-ins otherwise\n          plan: [\"is nimbusing...\"]\n\n    mcp_servers:\n      - name: slack\n        shared: false\n        command: npm\n        args: [\"exec\", \"--yes\", \"--\", \"slack-mcp-server@latest\",\n               \"--transport\", \"stdio\"]\n        tool_prefix: \"slack_\"\n\n    roles:\n      - name: Engineer\n        integrations:                 # per-agent transport identity\n          slack:\n            bot_token: \"${SLACK_BOT_TOKEN_ENGINEER}\"\n            signing_secret: \"${SLACK_SIGNING_SECRET_ENGINEER}\"\n        mcp_env:                       # Slack MCP server identity\n          slack:\n            SLACK_MCP_XOXB_TOKEN: \"${SLACK_BOT_TOKEN_ENGINEER}\"",
      "properties": {
        "typing_status": {
          "$ref": "#/$defs/SlackTypingStatusMode",
          "default": "addressed"
        },
        "status_phrases": {
          "$ref": "#/$defs/SlackStatusPhrases"
        }
      },
      "title": "SlackConfig",
      "type": "object"
    },
    "SlackStatusPhrases": {
      "additionalProperties": false,
      "description": "Per-phase overrides for the Slack working-status lines.\n\nSlack renders each line suffixed to the agent's name, so every phrase\nmust complete that sentence: ``\"is crewleting...\"`` reads as \"*Agent\nSWE is crewleting\u2026*\".  Each phase draws from its whole list \u2014 the\nengine picks one per phase and holds it for that phase's duration \u2014\nso a list of one is a fixed label and a longer list gives the\nindicator some variety across turns.\n\nA phase you omit (or give an empty list) keeps the built-in pool from\n:mod:`crewlet.notifications.typing_status`.  ``default`` covers any\nphase the engine adds later that has no pool of its own.\n\nKeep every phrase generic to the phase.  The engine's pick is\narbitrary \u2014 it never inspects what the agent is doing \u2014 so a line\nnaming real work (\"is checking Jira...\") is a claim that is false most\nof the time it shows; see the phrase rule in\n:mod:`crewlet.notifications.typing_status`.\n\nExample YAML::\n\n    integrations:\n      slack:\n        status_phrases:\n          plan: [\"is nimbusing...\", \"is thinking very hard...\"]\n          execute: [\"is nimbusing it...\", \"is on the case...\"]",
      "properties": {
        "onboarding": {
          "items": {
            "type": "string"
          },
          "title": "Onboarding",
          "type": "array"
        },
        "plan": {
          "items": {
            "type": "string"
          },
          "title": "Plan",
          "type": "array"
        },
        "execute": {
          "items": {
            "type": "string"
          },
          "title": "Execute",
          "type": "array"
        },
        "review": {
          "items": {
            "type": "string"
          },
          "title": "Review",
          "type": "array"
        },
        "default": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "title": "Default"
        }
      },
      "title": "SlackStatusPhrases",
      "type": "object"
    },
    "SlackTypingStatusMode": {
      "description": "When an agent shows a working status on Slack.\n\n``addressed`` (the default) covers exactly the cases where a human is\nplausibly waiting on *this* agent: a DM or group DM, a direct\n``@mention``, or a thread the agent already follows.  A broadcast\n(``@here`` / ``@channel``) and a passive top-level channel message are\ndeliberately excluded \u2014 every bot in the channel is woken by those, and\nthe Slack triage prompt tells most of them to stay silent, so lighting\nup N indicators would be noise rather than signal.\n\n``always`` shows it on every Slack-triggered turn (useful for a\nsingle-agent workspace); ``off`` disables the feature entirely.",
      "enum": [
        "off",
        "addressed",
        "always"
      ],
      "title": "SlackTypingStatusMode",
      "type": "string"
    },
    "TurnEngineConfig": {
      "additionalProperties": false,
      "description": "Configuration for the Plan/Execute/Review turn engine.\n\nThe turn engine replaces the single-shot LLM + tool loop with three\nphases per agent turn: Plan produces an ``ExecutionPlan``, Execute\nruns the plan's tool surface, and Review decides whether the turn\nis done, should iterate, or hand off to a colleague.\n\nSee ``docs/concepts/turn-engine.md`` for details.",
      "properties": {
        "max_iterations": {
          "default": 3,
          "title": "Max Iterations",
          "type": "integer"
        },
        "subagent_max_turns": {
          "default": 20,
          "title": "Subagent Max Turns",
          "type": "integer"
        },
        "subagent_timeout_seconds": {
          "default": 120.0,
          "title": "Subagent Timeout Seconds",
          "type": "number"
        },
        "subagent_budget_fraction": {
          "default": 0.2,
          "title": "Subagent Budget Fraction",
          "type": "number"
        },
        "subagent_max_parallel": {
          "default": 3,
          "title": "Subagent Max Parallel",
          "type": "integer"
        },
        "subagent_batch_timeout_seconds": {
          "default": 120.0,
          "title": "Subagent Batch Timeout Seconds",
          "type": "number"
        },
        "subagent_min_per_child_tokens": {
          "default": 500,
          "title": "Subagent Min Per Child Tokens",
          "type": "integer"
        },
        "executor_always_on_tools": {
          "items": {
            "type": "string"
          },
          "title": "Executor Always On Tools",
          "type": "array"
        },
        "delegation_depth_limit": {
          "default": 3,
          "title": "Delegation Depth Limit",
          "type": "integer"
        },
        "max_tool_rounds": {
          "default": 20,
          "title": "Max Tool Rounds",
          "type": "integer"
        },
        "plan_max_tool_rounds": {
          "default": 16,
          "title": "Plan Max Tool Rounds",
          "type": "integer"
        },
        "onboarding_max_tool_rounds": {
          "default": 10,
          "title": "Onboarding Max Tool Rounds",
          "type": "integer"
        },
        "extension_enabled": {
          "default": true,
          "title": "Extension Enabled",
          "type": "boolean"
        },
        "plan_max_tool_rounds_ceiling": {
          "default": 32,
          "title": "Plan Max Tool Rounds Ceiling",
          "type": "integer"
        },
        "execute_max_tool_rounds_ceiling": {
          "default": 40,
          "title": "Execute Max Tool Rounds Ceiling",
          "type": "integer"
        },
        "onboarding_max_tool_rounds_ceiling": {
          "default": 20,
          "title": "Onboarding Max Tool Rounds Ceiling",
          "type": "integer"
        },
        "extension_round_step": {
          "default": 8,
          "title": "Extension Round Step",
          "type": "integer"
        },
        "sandbox_budget_fraction": {
          "default": 0.5,
          "title": "Sandbox Budget Fraction",
          "type": "number"
        },
        "sandbox_min_budget_tokens": {
          "default": 2000,
          "title": "Sandbox Min Budget Tokens",
          "type": "integer"
        }
      },
      "title": "TurnEngineConfig",
      "type": "object"
    },
    "UnitIntegrationsConfig": {
      "additionalProperties": false,
      "description": "Per-unit integration identity \u2014 the Jira project + Confluence space\n+ Plane project a unit owns.\n\nDrives inbound webhook routing (Jira project / Confluence space /\nPlane project activity with no better recipient routes to the unit\nlead \u2014 see :func:`build_project_key_lead_map` /\n:func:`build_space_key_lead_map` /\n:func:`build_plane_project_lead_map`) and serves as the team's write\n/ skill-promotion home.  Slack at the unit level is the\n``slack_channel`` field, so it is intentionally not part of this\nblock.  No field here is a tool credential, and none scopes\nknowledge reads.",
      "properties": {
        "jira": {
          "anyOf": [
            {
              "$ref": "#/$defs/JiraIdentity"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "confluence": {
          "anyOf": [
            {
              "$ref": "#/$defs/ConfluenceIdentity"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "plane": {
          "anyOf": [
            {
              "$ref": "#/$defs/PlaneIdentity"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        }
      },
      "title": "UnitIntegrationsConfig",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "allOf": [
    {
      "$comment": "The knowledge backend is single-homed: integrations.confluence and an enabled integrations.plane are mutually exclusive.",
      "not": {
        "properties": {
          "integrations": {
            "properties": {
              "confluence": {
                "type": "object"
              },
              "plane": {
                "properties": {
                  "enabled": {
                    "const": true
                  }
                },
                "required": [
                  "enabled"
                ],
                "type": "object"
              }
            },
            "required": [
              "confluence",
              "plane"
            ]
          }
        },
        "required": [
          "integrations"
        ]
      }
    },
    {
      "$comment": "knowledge.plane_projects requires an enabled integrations.plane.",
      "if": {
        "properties": {
          "knowledge": {
            "properties": {
              "plane_projects": {
                "minItems": 1
              }
            },
            "required": [
              "plane_projects"
            ]
          }
        },
        "required": [
          "knowledge"
        ]
      },
      "then": {
        "properties": {
          "integrations": {
            "properties": {
              "plane": {
                "properties": {
                  "enabled": {
                    "const": true
                  }
                },
                "required": [
                  "enabled"
                ]
              }
            },
            "required": [
              "plane"
            ]
          }
        },
        "required": [
          "integrations"
        ]
      }
    },
    {
      "$comment": "knowledge.confluence_spaces is a scope list for the Confluence backend; it is rejected when integrations.plane is enabled.",
      "if": {
        "properties": {
          "knowledge": {
            "properties": {
              "confluence_spaces": {
                "minItems": 1
              }
            },
            "required": [
              "confluence_spaces"
            ]
          }
        },
        "required": [
          "knowledge"
        ]
      },
      "then": {
        "not": {
          "properties": {
            "integrations": {
              "properties": {
                "plane": {
                  "properties": {
                    "enabled": {
                      "const": true
                    }
                  },
                  "required": [
                    "enabled"
                  ]
                }
              },
              "required": [
                "plane"
              ]
            }
          },
          "required": [
            "integrations"
          ]
        }
      }
    }
  ],
  "description": "Tier B \u2014 founder-owned, live-editable, versioned in PostgreSQL.\n\nEverything that defines *what the company is and how its agents\nbehave*: identity, providers, integrations, MCP servers, roles,\nunits, learning + turn-engine settings, budgets, extensions.\n\nStored as a JSONB document in the ``company_config`` table; one\nrow marked active at a time.  Delivered to the engine via\n:meth:`Engine.apply_config` either at boot (from the active DB\nrow) or at runtime (over Pulsar after a ``PUT /config``).\n\nSee ``docs/concepts/configuration.md``.",
  "properties": {
    "name": {
      "title": "Name",
      "type": "string"
    },
    "mission": {
      "default": "",
      "title": "Mission",
      "type": "string"
    },
    "vision": {
      "default": "",
      "title": "Vision",
      "type": "string"
    },
    "policies": {
      "items": {
        "type": "string"
      },
      "title": "Policies",
      "type": "array"
    },
    "integrations": {
      "$ref": "#/$defs/IntegrationsConfig"
    },
    "knowledge": {
      "$ref": "#/$defs/KnowledgeConfig"
    },
    "skill_variables": {
      "additionalProperties": false,
      "patternProperties": {
        "^[A-Za-z_][A-Za-z0-9_]*$": {
          "type": "string"
        }
      },
      "title": "Skill Variables",
      "type": "object"
    },
    "providers": {
      "$ref": "#/$defs/ProvidersConfig",
      "default": {
        "llm": {},
        "embeddings": null,
        "sandbox": null
      }
    },
    "turn_engine": {
      "$ref": "#/$defs/TurnEngineConfig",
      "default": {
        "max_iterations": 3,
        "subagent_max_turns": 20,
        "subagent_timeout_seconds": 120.0,
        "subagent_budget_fraction": 0.2,
        "subagent_max_parallel": 3,
        "subagent_batch_timeout_seconds": 120.0,
        "subagent_min_per_child_tokens": 500,
        "executor_always_on_tools": [
          "load_tool_skill"
        ],
        "delegation_depth_limit": 3,
        "max_tool_rounds": 20,
        "plan_max_tool_rounds": 16,
        "onboarding_max_tool_rounds": 10,
        "extension_enabled": true,
        "plan_max_tool_rounds_ceiling": 32,
        "execute_max_tool_rounds_ceiling": 40,
        "onboarding_max_tool_rounds_ceiling": 20,
        "extension_round_step": 8,
        "sandbox_budget_fraction": 0.5,
        "sandbox_min_budget_tokens": 2000
      }
    },
    "learning": {
      "$ref": "#/$defs/LearningConfig",
      "default": {
        "enabled": true,
        "episodic": {
          "retrieval_limit": 5
        },
        "reflect": {
          "budget_tokens": 5000,
          "enabled": true,
          "persist_decider": true,
          "summarize_episodes": true,
          "summarize_max_tokens": 400
        },
        "counterparty": {
          "budget_tokens": 3000,
          "enabled": true
        },
        "skill_synthesis": {
          "budget_tokens": 4000,
          "cluster_jaccard_threshold": 0.6,
          "cluster_min_size": 3,
          "cluster_window_hours": 168,
          "duplicate_jaccard_threshold": 0.7,
          "enabled": true,
          "episode_fetch_limit": 200,
          "max_skills_per_agent": 50,
          "min_tool_calls": 5,
          "scheduler_enabled": false,
          "scheduler_interval_seconds": 3600
        },
        "skill_refinement": {
          "auto_refine_on_failure": true,
          "auto_refine_on_success": true,
          "budget_tokens": 3000,
          "enabled": true,
          "max_body_chars": 20000,
          "max_versions_kept": 10
        },
        "skill_promotion": {
          "budget_tokens": 4000,
          "enabled": true,
          "jaccard_threshold": 0.6,
          "min_sibling_count": 3
        },
        "skill_curator": {
          "archive_after_days": 90,
          "enabled": true,
          "interval_hours": 24,
          "stale_after_days": 30
        },
        "personal_memory": {
          "max_refreshes_per_turn": 3
        },
        "episode_lifecycle": {
          "compacted_max_age_days": 0,
          "compaction_batch_size": 200,
          "compaction_budget_tokens": 4000,
          "compaction_jaccard_threshold": 0.6,
          "compaction_min_age_days": 30,
          "compaction_min_cluster_size": 3,
          "consolidated_grace_days": 30,
          "exemplar_count": 2,
          "max_raw_episodes_per_agent": 500,
          "non_terminal_max_age_days": 14,
          "write_check_every_n": 10
        }
      }
    },
    "scheduling": {
      "$ref": "#/$defs/SchedulingConfig",
      "default": {
        "enabled": true,
        "tick_seconds": 10,
        "default_timezone": "UTC",
        "jitter_seconds": 0,
        "catchup_min_seconds": 120,
        "catchup_max_seconds": 7200
      }
    },
    "mcp_servers": {
      "items": {
        "$ref": "#/$defs/MCPServerConfig"
      },
      "title": "Mcp Servers",
      "type": "array"
    },
    "token_budget": {
      "default": 0,
      "title": "Token Budget",
      "type": "integer"
    },
    "notification_rate_limit": {
      "default": 0,
      "title": "Notification Rate Limit",
      "type": "integer"
    },
    "notification_coalesce_window_seconds": {
      "default": 0.0,
      "maximum": 60.0,
      "minimum": 0.0,
      "title": "Notification Coalesce Window Seconds",
      "type": "number"
    },
    "notification_coalesce_max_batch": {
      "default": 20,
      "minimum": 1,
      "title": "Notification Coalesce Max Batch",
      "type": "integer"
    },
    "roles": {
      "items": {
        "$ref": "#/$defs/RoleConfig"
      },
      "title": "Roles",
      "type": "array"
    },
    "units": {
      "items": {
        "$ref": "#/$defs/OrgUnitConfig"
      },
      "title": "Units",
      "type": "array"
    },
    "extensions": {
      "items": {
        "additionalProperties": true,
        "type": "object"
      },
      "title": "Extensions",
      "type": "array"
    }
  },
  "required": [
    "name"
  ],
  "title": "CompanyConfig",
  "type": "object"
}
