{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://docs.crewlet.ai/schema/bootstrap.schema.json",
  "$defs": {
    "ApiAuthConfig": {
      "additionalProperties": false,
      "description": "Bearer-token auth policy for the API.\n\nGuards **every** route except the ones that authenticate by other\nmeans or must be reachable to obtain a token at all: ``/health``,\n``/ready``, the dashboard shell and its static assets, ``/webhooks/*``\n(HMAC-verified per source) and ``/otlp/*`` (signed per-run token).\n\nIt did not always: auth covered only ``/config/*``, which left\n``/events``, ``/agents/{id}/memory`` and ``/ws/stream`` serving full\nLLM transcripts \u2014 prompts, tool arguments, diary entries \u2014 to anyone\nwho could reach the port.  Survivable behind a firewall, and not\nsomething to bless as the shape of an internet-facing deployment.",
      "properties": {
        "tokens": {
          "items": {
            "$ref": "#/$defs/ApiAuthTokenConfig"
          },
          "title": "Tokens",
          "type": "array"
        },
        "disabled": {
          "default": false,
          "title": "Disabled",
          "type": "boolean"
        },
        "allow_anonymous_read": {
          "default": false,
          "title": "Allow Anonymous Read",
          "type": "boolean"
        },
        "allowed_origins": {
          "items": {
            "type": "string"
          },
          "title": "Allowed Origins",
          "type": "array"
        }
      },
      "title": "ApiAuthConfig",
      "type": "object"
    },
    "ApiAuthTokenConfig": {
      "additionalProperties": false,
      "description": "One bearer token gating ``/config/*`` access.",
      "properties": {
        "id": {
          "title": "Id",
          "type": "string"
        },
        "token": {
          "title": "Token",
          "type": "string"
        }
      },
      "required": [
        "id",
        "token"
      ],
      "title": "ApiAuthTokenConfig",
      "type": "object"
    },
    "ApiConfig": {
      "additionalProperties": false,
      "description": "Tier A API process settings \u2014 host, port, auth.",
      "properties": {
        "host": {
          "default": "0.0.0.0",
          "title": "Host",
          "type": "string"
        },
        "port": {
          "default": 0,
          "title": "Port",
          "type": "integer"
        },
        "auth": {
          "$ref": "#/$defs/ApiAuthConfig",
          "default": {
            "tokens": [],
            "disabled": false,
            "allow_anonymous_read": false,
            "allowed_origins": []
          }
        }
      },
      "title": "ApiConfig",
      "type": "object"
    },
    "BootstrapProvidersConfig": {
      "additionalProperties": false,
      "description": "Tier A provider settings \u2014 queue, database, knowledge.\n\nEverything the engine needs to talk to its own infrastructure\nbefore reading the founder-owned Tier B config from the DB.",
      "properties": {
        "queue": {
          "$ref": "#/$defs/QueueConfig",
          "default": {
            "type": "pulsar",
            "url": "pulsar://localhost:6650",
            "admin_url": "",
            "tenant": "public",
            "namespace": "default",
            "auth_token": "",
            "tls_trust_certs_path": ""
          }
        },
        "database": {
          "$ref": "#/$defs/DatabaseConfig",
          "default": {
            "dsn": ""
          }
        },
        "knowledge": {
          "$ref": "#/$defs/KnowledgeStoreConfig",
          "default": {
            "type": "pgvector"
          }
        }
      },
      "title": "BootstrapProvidersConfig",
      "type": "object"
    },
    "DatabaseConfig": {
      "additionalProperties": false,
      "description": "Configuration for the PostgreSQL database.",
      "properties": {
        "dsn": {
          "default": "",
          "title": "Dsn",
          "type": "string"
        }
      },
      "title": "DatabaseConfig",
      "type": "object"
    },
    "KnowledgeStoreConfig": {
      "additionalProperties": false,
      "description": "Configuration for the knowledge store backend (Tier A \u2014 bound to DB).",
      "properties": {
        "type": {
          "default": "pgvector",
          "enum": [
            "pgvector",
            "memory"
          ],
          "title": "Type",
          "type": "string"
        }
      },
      "title": "KnowledgeStoreConfig",
      "type": "object"
    },
    "NodeConfig": {
      "additionalProperties": false,
      "description": "Tier A identity of THIS process within the company.\n\nEvery Crewlet process has one.  Today it labels every log line and\nthe ``/health`` payload \u2014 already the difference between \"a config\napply failed\" and \"the config apply failed on node-2\" once more than\none process runs, and the only way a caller behind a load balancer\ncan tell which process answered.  It is also the\nidentity every future cross-process mechanism is keyed on (lease\nowner, per-node subscription name); see ``SCALING.md``.\n\n**It must be stable across restarts**, which is why it comes from the\ndeployment rather than being generated.  A fresh value per boot would\norphan whatever the previous incarnation registered under the old one\n\u2014 the failure the ``hook-{id(callback)}`` subscription naming already\ndemonstrates.  In Kubernetes use the pod name (a StatefulSet ordinal\nis ideal); under systemd, the host name.",
      "properties": {
        "id": {
          "default": "",
          "title": "Id",
          "type": "string"
        }
      },
      "title": "NodeConfig",
      "type": "object"
    },
    "QueueConfig": {
      "additionalProperties": false,
      "description": "Configuration for the event queue backend.",
      "properties": {
        "type": {
          "const": "pulsar",
          "default": "pulsar",
          "title": "Type",
          "type": "string"
        },
        "url": {
          "default": "pulsar://localhost:6650",
          "title": "Url",
          "type": "string"
        },
        "admin_url": {
          "default": "",
          "title": "Admin Url",
          "type": "string"
        },
        "tenant": {
          "default": "public",
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*$",
          "title": "Tenant",
          "type": "string"
        },
        "namespace": {
          "default": "default",
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*$",
          "title": "Namespace",
          "type": "string"
        },
        "auth_token": {
          "default": "",
          "title": "Auth Token",
          "type": "string"
        },
        "tls_trust_certs_path": {
          "default": "",
          "title": "Tls Trust Certs Path",
          "type": "string"
        }
      },
      "title": "QueueConfig",
      "type": "object"
    },
    "SecretKeyConfig": {
      "additionalProperties": false,
      "description": "One key in the Tier A encryption keyring.\n\n``material`` is a base64-encoded 32-byte key (or a ``${VAR}``\nreference, resolved from the environment at bootstrap load time like\nevery Tier A value). Generate one with ``crewlet secrets keygen``.\nSee ``docs/concepts/configuration.md`` \u00a7 Secrets.",
      "properties": {
        "id": {
          "minLength": 1,
          "pattern": "^[A-Za-z0-9._-]+$",
          "title": "Id",
          "type": "string"
        },
        "material": {
          "title": "Material",
          "type": "string"
        }
      },
      "required": [
        "id",
        "material"
      ],
      "title": "SecretKeyConfig",
      "type": "object"
    },
    "SecretsConfig": {
      "additionalProperties": false,
      "description": "Tier A encryption keyring for company-config secrets at rest.\n\nThe keyring is the sole root of trust: the database holds only\nciphertext, and the key material lives here (on disk / in env),\nnever in the DB. Empty (the default) means secret encryption is\ndisabled \u2014 the engine then fails closed only if the active revision\nactually contains sealed values.\n\nSee ``docs/concepts/configuration.md`` \u00a7 Secrets.",
      "properties": {
        "active_key_id": {
          "default": "",
          "title": "Active Key Id",
          "type": "string"
        },
        "keys": {
          "items": {
            "$ref": "#/$defs/SecretKeyConfig"
          },
          "title": "Keys",
          "type": "array"
        }
      },
      "title": "SecretsConfig",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Tier A \u2014 ops-owned, on-disk, restart-only.\n\nCarries only the keys the engine needs to bring up its\ninfrastructure (DB, Pulsar, API socket, auth, logging) plus the\nsecret-encryption keyring (the root of trust for Tier B secrets at\nrest).  Everything else lives in the versioned ``company_config``\ntable and is delivered to the engine via :meth:`Engine.apply_config`.\n\nSee ``docs/concepts/configuration.md``.",
  "properties": {
    "providers": {
      "$ref": "#/$defs/BootstrapProvidersConfig",
      "default": {
        "queue": {
          "admin_url": "",
          "auth_token": "",
          "namespace": "default",
          "tenant": "public",
          "tls_trust_certs_path": "",
          "type": "pulsar",
          "url": "pulsar://localhost:6650"
        },
        "database": {
          "dsn": ""
        },
        "knowledge": {
          "type": "pgvector"
        }
      }
    },
    "api": {
      "$ref": "#/$defs/ApiConfig",
      "default": {
        "host": "0.0.0.0",
        "port": 0,
        "auth": {
          "allow_anonymous_read": false,
          "allowed_origins": [],
          "disabled": false,
          "tokens": []
        }
      }
    },
    "secrets": {
      "$ref": "#/$defs/SecretsConfig",
      "default": {
        "active_key_id": "",
        "keys": []
      }
    },
    "node": {
      "$ref": "#/$defs/NodeConfig",
      "default": {
        "id": ""
      }
    },
    "debug": {
      "default": false,
      "title": "Debug",
      "type": "boolean"
    }
  },
  "title": "BootstrapConfig",
  "type": "object"
}
